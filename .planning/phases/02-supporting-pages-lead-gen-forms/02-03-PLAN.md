---
phase: 02-supporting-pages-lead-gen-forms
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/i18n/nl.ts
  - src/pages/mijn-verhaal.astro
  - src/components/layout/Nav.astro
  - src/components/layout/Footer.astro
  - src/components/sections/Hero.astro
  - src/components/sections/Samenwerken.astro
  - src/components/sections/Story.astro
autonomous: true
requirements: [PAGE-03, PAGE-05]
estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "/mijn-verhaal renders the expanded long-form founder story (opening dark band → expanded narrative sections → terminal CTA into lead flow → newsletter), built only from existing material (D-05, D-06)"
    - "The homepage Story section gains a 'Lees mijn hele verhaal →' link to /mijn-verhaal; the homepage keeps its short teaser (D-06)"
    - "The homepage Hero primary CTA routes to /contact; the Samenwerken 'Boek lezing' → /contact?type=lezing and 'Plan kennismaking' → /contact?type=coaching (PAGE-05)"
    - "Mijn verhaal nav + footer items are enabled as real <a> links with active-state; Nieuwsbrief and Boek remain honest disabled placeholders; newsletter/NL-EN stay honest placeholders"
    - "No CTA becomes a fake working button: every newly-enabled CTA has a real resolved destination; still-deferred items stay aria-disabled (never href=#)"
  artifacts:
    - "src/pages/mijn-verhaal.astro"
    - "nl.ts mijnVerhaal top-level key"
  key_links:
    - "Homepage Story teaser → /mijn-verhaal (BASE_URL prefixed)"
    - "Hero primary CTA → CONTACT_URL; Samenwerken CTAs → /contact?type=lezing|coaching"
    - "mijn-verhaal.astro opening band ← reused Story.astro component"
    - "Nav/Footer mijnVerhaal href → BASE_URL + mijn-verhaal; active state ← Astro.url.pathname"
---

<objective>
Complete the phase: build the **Mijn verhaal** page (PAGE-03) as the emotional/credibility centerpiece, and wire the remaining homepage CTAs into the lead flow (PAGE-05) — closing the loop so "Boek lezing" / "Plan kennismaking" / Contact all produce real inquiries, and the homepage teaser links through to the full story.

**Phase user story (MVP framing):** **As a** founder drawn in by Milan's homepage teaser, **I want to** read his full honest story and act on a homepage CTA that routes me into the right contact flow, **so that** I can connect for coaching or a lezing.

Purpose: After this plan every Phase 2 requirement is delivered — all four subpages exist, all lead CTAs route into /contact with the correct pre-selected Onderwerp, and the nav/footer fully reflect the shipped pages while keeping deferred items honestly disabled.
Output: `/mijn-verhaal` page; `nl.mijnVerhaal` copy; homepage Hero/Samenwerken/Story CTA wiring; Mijn verhaal enabled in Nav + Footer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-supporting-pages-lead-gen-forms/02-CONTEXT.md
@.planning/phases/02-supporting-pages-lead-gen-forms/02-RESEARCH.md
@.planning/phases/02-supporting-pages-lead-gen-forms/02-PATTERNS.md
@.planning/phases/02-supporting-pages-lead-gen-forms/02-UI-SPEC.md
@.planning/phases/02-supporting-pages-lead-gen-forms/02-01-SUMMARY.md
@.planning/phases/02-supporting-pages-lead-gen-forms/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Draft nl.mijnVerhaal + build /mijn-verhaal expanded story page</name>
  <files>src/i18n/nl.ts, src/pages/mijn-verhaal.astro</files>
  <read_first>
    - src/i18n/nl.ts (the file being modified — existing top-level key shape; story.body seed paragraphs at the existing story key)
    - src/components/sections/Story.astro (reused wholesale as the opening dark band; import + dark-band + eyebrow + signature + Picture pattern)
    - src/pages/index.astro (page shell + section composition analog)
    - src/components/ui/SectionWrapper.astro (variant + id/labelledBy contract), src/components/ui/Button.astro (CTA), src/components/sections/Newsletter.astro (reused bottom)
    - .planning/phases/02-supporting-pages-lead-gen-forms/02-UI-SPEC.md §"/mijn-verhaal (PAGE-03)" layout contract + §Typography long-form prose (.prose max-width 680px) + §Color dark-band rule
    - .planning/phases/02-supporting-pages-lead-gen-forms/02-RESEARCH.md §"Mijn Verhaal Content Seed" + §"Pattern 1"
    - .planning/phases/02-supporting-pages-lead-gen-forms/02-PATTERNS.md §pages and §Story.astro
  </read_first>
  <behavior>
    - nl gains a top-level mijnVerhaal key with pageTitle/pageDesc, expanded narrative paragraphs (arrays keyed by story-arc section), and terminal CTA labels — drafted only from existing material
    - /mijn-verhaal renders: reused Story.astro opening dark band → 2-4 expanded narrative SectionWrappers (.prose, max-width 680px) → terminal CTA section (Plan kennismaking → /contact?type=coaching, ghost Lees over spreken → /spreker) → reused Newsletter
    - At most one dark band on the page (the opening); remaining sections light
  </behavior>
  <action>
Extend `src/i18n/nl.ts`: add a top-level `mijnVerhaal` key (same `as const` level as `contact`/`coaching`/`spreker`; PATTERNS.md §nl.ts). Include `pageTitle`/`pageDesc` from the UI-SPEC §SEO table, and the expanded long-form narrative as string arrays keyed to the story arc (Soly founding + scale; international expansion; failure + lessons; today / why he coaches) — expanded from the existing `nl.story.body` seed and the Soly arc facts already in the project (9 landen, ~180 medewerkers, ~1M panelen, faillissement, USPs). Add terminal CTA labels ("Plan kennismaking", ghost "Lees over spreken"). Draft from existing material ONLY — invent no facts, numbers, or quotes (D-05); flag for Milan's review in the SUMMARY.

Create `src/pages/mijn-verhaal.astro` (PATTERNS.md §pages): wrap `BaseLayout` with `title={nl.mijnVerhaal.pageTitle}`/`description={nl.mijnVerhaal.pageDesc}` (D-12). Compose per UI-SPEC §/mijn-verhaal: import and render the existing `Story` component from `../components/sections/Story.astro` as the opening dark band (its `id="verhaal"` becomes the page-top anchor); below it, 2-4 `SectionWrapper variant="default"` sections containing the expanded prose — each prose column uses the `.prose` class (`max-width:680px`, never full-width, UI-SPEC §Typography); then a terminal CTA `SectionWrapper` with a primary `Button` "Plan kennismaking" href `${import.meta.env.BASE_URL}contact?type=coaching` and a ghost `Button` "Lees over spreken" href `${import.meta.env.BASE_URL}spreker`; then the reused `Newsletter`. Reuse the already-extracted `milan-story-portrait.jpg` / `milan-energy-mission.jpg` if additional imagery is wanted (no new photos). Enforce the at-most-one-dark-band rule and restrained-yellow rule. Unique `id`+`labelledBy` per SectionWrapper. Never `href="#"`; every internal href uses the BASE_URL template-literal form (Pitfall 2).
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/mijn-verhaal.astro` exists and imports Story from `../components/sections/Story.astro`
    - `npm run build` exits 0 and produces a /mijn-verhaal route in dist/
    - `grep -c "mijnVerhaal:" src/i18n/nl.ts` returns at least 1
    - `grep -c "type=coaching" src/pages/mijn-verhaal.astro` returns at least 1 (terminal CTA into lead flow)
    - `grep -c "prose" src/pages/mijn-verhaal.astro` returns at least 1 (reading-measure constraint applied)
    - `grep -c 'href="#"' src/pages/mijn-verhaal.astro` returns 0
  </acceptance_criteria>
  <done>The expanded Mijn verhaal page builds green: reused Story opening band, .prose narrative sections drafted only from existing material, and a terminal CTA into the contact flow.</done>
  <reversibility rating="reversible">Additive page + copy.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire homepage CTAs into the lead flow + Story teaser link (PAGE-05)</name>
  <files>src/components/sections/Hero.astro, src/components/sections/Samenwerken.astro, src/components/sections/Story.astro</files>
  <read_first>
    - src/components/sections/Hero.astro (the file being modified — current disabled Contact span ~lines 47-59 with data-placeholder)
    - src/components/sections/Samenwerken.astro (the file being modified — ladderCards; cards[1]/cards[2] referencing the old TODO_CONTACT_URL, disabled CTA pattern ~lines 40-65)
    - src/components/sections/Story.astro (the file being modified — bottom of the story text block; note this component is ALSO reused by /mijn-verhaal, so the teaser link must read well in both contexts)
    - src/config.ts (CONTACT_URL resolved in Plan 01)
    - .planning/phases/02-supporting-pages-lead-gen-forms/02-RESEARCH.md §"Pattern 7: Homepage CTA Wiring" + §Open Questions Q2 (teaser vs anchor)
    - .planning/phases/02-supporting-pages-lead-gen-forms/02-PATTERNS.md §BASE_URL Internal Link + §Honest Disabled CTA
    - .planning/phases/02-supporting-pages-lead-gen-forms/02-UI-SPEC.md §"Honest-CTA Pattern" + Copywriting Contract (story teaser copy "Lees mijn hele verhaal →")
  </read_first>
  <behavior>
    - Hero primary CTA is a real <a>/Button to CONTACT_URL (was a disabled span) — no longer aria-disabled
    - Samenwerken "Boek lezing" → /contact?type=lezing and "Plan kennismaking" → /contact?type=coaching, both enabled (disabled={false})
    - Story section (homepage context) gains a "Lees mijn hele verhaal →" link to /mijn-verhaal; homepage keeps its short teaser copy
    - The Story teaser link renders sensibly on both the homepage and the reused /mijn-verhaal opening band (guard so it does not create a self-link loop or is acceptable as a same-page anchor)
    - Still-deferred CTAs (Nieuwsbrief, NL/EN switch, Alle artikelen) remain honest disabled placeholders
  </behavior>
  <action>
`src/components/sections/Hero.astro`: replace the disabled Contact `<span aria-disabled data-placeholder=...>` (RESEARCH §Pattern 7 item 1) with a real primary CTA — a `Button variant="primary"` (or `<a class="btn btn--primary">`) with href `${import.meta.env.BASE_URL}contact` using the resolved `CONTACT_URL` symbol from config. Remove the `aria-disabled`/`tabindex="-1"`/`data-placeholder` for this CTA only. Keep the secondary "Mijn verhaal" hero CTA pointing at the in-page `#verhaal` anchor (RESEARCH Q2 — hero anchor stays in-page scroll; the new page link lives in the Story section teaser).

`src/components/sections/Samenwerken.astro`: for the two lead cards (cards[1] "Boek lezing", cards[2] "Plan kennismaking"), set real hrefs — `${import.meta.env.BASE_URL}contact?type=lezing` and `${import.meta.env.BASE_URL}contact?type=coaching` respectively — and enable the `Button` (`disabled={false}`). The Nieuwsbrief card CTA stays a disabled honest placeholder (still tied to TODO_SUBSTACK_URL, Phase 3). (Leave the Calendly seam note: when `TODO_CALENDLY_URL` is supplied, the "Plan kennismaking" href swaps to it — reference this in a comment near cards[2], D-09.)

`src/components/sections/Story.astro`: append a "Lees mijn hele verhaal →" ghost link at the bottom of the story text block, href `${import.meta.env.BASE_URL}mijn-verhaal` (RESEARCH Q2, UI-SPEC Copywriting Contract). Because Story.astro is reused as the /mijn-verhaal opening band, ensure the teaser link is acceptable in that context — either it is harmless as a same-page link, or gate it (e.g. render only when not already on /mijn-verhaal by checking `Astro.url.pathname`). Keep the homepage short teaser copy intact (D-06 — homepage keeps the teaser, full story lives on the page). Never `href="#"`; use the BASE_URL template-literal form (Pitfall 2).
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -c "BASE_URL}contact" src/components/sections/Hero.astro` returns at least 1 and the Hero Contact CTA is no longer `aria-disabled`
    - `grep -c "type=lezing" src/components/sections/Samenwerken.astro` returns at least 1
    - `grep -c "type=coaching" src/components/sections/Samenwerken.astro` returns at least 1
    - `grep -c "BASE_URL}mijn-verhaal" src/components/sections/Story.astro` returns at least 1
    - `grep -c 'href="#"' src/components/sections/Hero.astro src/components/sections/Samenwerken.astro src/components/sections/Story.astro` returns 0
    - Samenwerken Nieuwsbrief CTA still references TODO_SUBSTACK_URL and stays disabled (`grep -c "TODO_SUBSTACK_URL" src/components/sections/Samenwerken.astro` returns at least 1)
  </acceptance_criteria>
  <done>Homepage Hero + Samenwerken lead CTAs route into /contact with the correct type params, the Story teaser links to /mijn-verhaal, and still-deferred CTAs stay honest disabled placeholders; build green.</done>
  <reversibility rating="reversible">href edits are trivially revertible; Calendly seam left for later.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Enable Mijn verhaal in Nav + Footer (final nav reconciliation)</name>
  <files>src/components/layout/Nav.astro, src/components/layout/Footer.astro</files>
  <read_first>
    - src/components/layout/Nav.astro (the file being modified — navItems array after Plan 02 enabled coaching/spreker; mijnVerhaal still disabled)
    - src/components/layout/Footer.astro (the file being modified — footer navItems after Plan 02)
    - .planning/phases/02-supporting-pages-lead-gen-forms/02-PATTERNS.md §Nav.astro and §Footer.astro
    - .planning/phases/02-supporting-pages-lead-gen-forms/02-RESEARCH.md §"Pattern 2" + Pitfall 5
  </read_first>
  <behavior>
    - mijnVerhaal nav + footer items render as real <a href> (BASE_URL prefixed) with active-state on /mijn-verhaal
    - Nieuwsbrief (Phase 3) and Boek (Phase 5) remain the only disabled nav placeholders
    - No other nav item is changed or regressed (Contact CTA + coaching + spreker stay enabled)
  </behavior>
  <action>
In `src/components/layout/Nav.astro`, change the `mijnVerhaal` navItems entry from the disabled `reason` shape to `href: `${import.meta.env.BASE_URL}mijn-verhaal`` (PATTERNS.md §Nav.astro conditional render already added in Plan 02 — this is a one-line array change plus reliance on the existing active-state logic). Confirm `nieuwsbrief` (fase 3) and `boek` (fase 5) remain the only disabled entries. In `src/components/layout/Footer.astro`, make the same `mijnVerhaal` entry a real `<a>` link (BASE_URL prefixed). Do not regress the Contact CTA or the coaching/spreker links. Never `href="#"`.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -c "BASE_URL}mijn-verhaal" src/components/layout/Nav.astro` returns at least 1 and `grep -c "BASE_URL}mijn-verhaal" src/components/layout/Footer.astro` returns at least 1
    - Nav still contains disabled placeholders for Nieuwsbrief and Boek only (`grep -c "aria-disabled" src/components/layout/Nav.astro` returns at least 1)
    - `grep -c "BASE_URL}coaching" src/components/layout/Nav.astro` returns at least 1 (Plan 02 enablement not regressed)
    - `grep -c 'href="#"' src/components/layout/Nav.astro src/components/layout/Footer.astro` returns 0
  </acceptance_criteria>
  <done>Mijn verhaal is reachable from header nav (with active-state) and footer; Nieuwsbrief + Boek are the only remaining honest disabled placeholders; coaching/spreker/Contact enablement intact; build green.</done>
  <reversibility rating="reversible">One-line nav/footer array edits.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → static pages/homepage CTAs | Mijn verhaal and the homepage are static HTML; no new input surface (the only input surface remains the ContactForm on /contact) |
| homepage CTA hrefs → /contact | CTAs pass fixed `?type=` literals that /contact validates against its option set (Plan 01 T-2-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-08 | Info disclosure | Mijn verhaal content honesty (fabricated facts/quotes) | medium | mitigate | Expanded narrative drafted from existing material only (D-05/D-06); no invented facts/numbers/quotes; flagged for user review. |
| T-2-09 | Tampering | Homepage CTA `?type=` deep-links | low | mitigate | CTAs emit only fixed literals `type=lezing`/`type=coaching`; validated by the ContactForm option-set guard (Plan 01 T-2-04). |
| T-2-10 | Repudiation | Honest-CTA regression (fake working buttons) | medium | mitigate | Every newly-enabled CTA resolves to a real destination; still-deferred items (Nieuwsbrief, NL/EN, Boek) stay aria-disabled placeholders; `href="#"` grep must be 0 across all modified files (HOME-10 carry-forward). |
| T-2-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new packages this phase. No install task; gate does not fire. |
</threat_model>

<verification>
- `npm run build` exits 0 with /mijn-verhaal route present in dist/; all four subpages now build
- Homepage Hero + Samenwerken lead CTAs carry correct `?type=` deep-links; Story teaser links to /mijn-verhaal
- No `href="#"` on any modified file; deferred CTAs (Nieuwsbrief/NL-EN/Boek) stay honest disabled placeholders
- Nav/Footer fully reconciled: Coaching/Spreker/Mijn verhaal enabled with active-state; Nieuwsbrief/Boek disabled
- Content honesty: expanded story drafted from existing material only (visual/content review backstop before /gsd-verify-work)
</verification>

<success_criteria>
- ROADMAP Phase 2 #1: Coaching, Spreker, AND Mijn verhaal pages all exist, styled with the Phase 1 design system (Mijn verhaal delivered here)
- ROADMAP #3: "Boek lezing" and "Plan kennismaking" CTAs route into the contact/lead flow — from both the subpages (Plan 02) and the homepage (this plan)
- ROADMAP #4: Mijn verhaal is SEO-, a11y-, and responsive-consistent with the homepage
- Phase-complete: all of PAGE-01..05 delivered across Plans 01-03
</success_criteria>

<output>
Create `.planning/phases/02-supporting-pages-lead-gen-forms/02-03-SUMMARY.md` when done. Flag drafted `nl.mijnVerhaal` copy for Milan's review (D-05). Note the end-of-phase human-verify recommendation: user drops the real Web3Forms access key into config.ts and confirms an inbox delivery (RESEARCH §Open Questions Q1).
</output>
