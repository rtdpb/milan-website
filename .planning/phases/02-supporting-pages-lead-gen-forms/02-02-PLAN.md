---
phase: 02-supporting-pages-lead-gen-forms
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/assets/photos/milan-networking.jpg
  - src/assets/photos/milan-speaking-graph.jpg
  - src/i18n/nl.ts
  - src/pages/coaching.astro
  - src/pages/spreker.astro
  - src/components/layout/Nav.astro
  - src/components/layout/Footer.astro
autonomous: true
requirements: [PAGE-01, PAGE-02]
estimate:
  tokens: 66000
  raw_tokens: 33000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "/coaching renders an editorial landing page (hero → approach cards → single relevant testimonial → CTA into contact flow → newsletter), styled with the Phase 1 design system"
    - "/spreker renders an editorial landing page (hero → press strip → topics → single relevant testimonial → CTA into contact flow → newsletter)"
    - "Coaching shows exactly ONE testimonial (Yang Soo Kloosterhof); Spreker shows exactly ONE (Oranjewoud Export Academy) — verbatim attribution, no fabricated testimonials (D-07)"
    - "Coaching/Spreker primary CTAs route into the lead flow: Plan kennismaking → /contact?type=coaching, Boek lezing → /contact?type=lezing (via BASE_URL prefix)"
    - "Coaching and Spreker nav + footer items are enabled as real <a> links with active-state on the current page; Nieuwsbrief and Boek stay honest disabled placeholders"
    - "All page copy is drafted only from existing material (Soly arc, USPs, existing testimonials) — no invented facts/numbers/quotes (D-05)"
  artifacts:
    - "src/assets/photos/milan-networking.jpg and milan-speaking-graph.jpg (extracted, downscaled ≤2400px)"
    - "src/pages/coaching.astro and src/pages/spreker.astro"
    - "nl.ts coaching and spreker top-level keys"
  key_links:
    - "Coaching/Spreker photos → astro:assets <Picture> AVIF/WebP pipeline"
    - "Nav/Footer navItems href → BASE_URL + coaching|spreker; active state ← Astro.url.pathname"
    - "Single testimonial ← nl.testimonials.items[0] (coaching) / items[2] (spreker)"
---

<objective>
Expand the proven lead-capture slice into the two conversion landing pages: **Coaching** (PAGE-01) and **Spreker** (PAGE-02). Each is a full but restrained editorial page (D-10) composed from Phase 1 components, ending in a CTA that deep-links into the `/contact` flow proven by Plan 01.

**Phase user story (MVP framing):** **As a** founder evaluating Milan, **I want to** read a focused Coaching or Spreker page and click straight into the right contact flow, **so that** I can decide and reach out.

Purpose: Each page delivers a complete user-facing capability — a visitor can now navigate to Coaching/Spreker from the nav, read a real editorial page, and click a CTA that pre-selects the correct Onderwerp on /contact.
Output: two extracted photos; two new pages; `nl.coaching` + `nl.spreker` copy; Coaching/Spreker enabled in Nav and Footer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ASSETS.md
@.planning/phases/02-supporting-pages-lead-gen-forms/02-CONTEXT.md
@.planning/phases/02-supporting-pages-lead-gen-forms/02-RESEARCH.md
@.planning/phases/02-supporting-pages-lead-gen-forms/02-PATTERNS.md
@.planning/phases/02-supporting-pages-lead-gen-forms/02-UI-SPEC.md
@.planning/phases/02-supporting-pages-lead-gen-forms/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract Coaching/Spreker photos + draft nl.coaching + nl.spreker copy</name>
  <files>src/assets/photos/milan-networking.jpg, src/assets/photos/milan-speaking-graph.jpg, src/i18n/nl.ts</files>
  <read_first>
    - .planning/ASSETS.md (source archive path + section→source mapping: `_AVM2348.jpg`→networking, `10 Jaar Soly-78.jpg`→speaking-graph; downscale rules)
    - src/components/sections/Story.astro (Phase 1 photo downscale + astro:assets import convention; STATE.md records the ≤2400px sharp-downscale approach)
    - src/i18n/nl.ts (the file being modified — existing top-level key shape; story.body multi-paragraph array model; testimonials.items content)
    - .planning/phases/02-supporting-pages-lead-gen-forms/02-UI-SPEC.md §"Copywriting Contract" + §"Image Pipeline Contract" (alt text, widths, production names)
    - .planning/phases/02-supporting-pages-lead-gen-forms/02-PATTERNS.md §nl.ts and §Testimonials (index mapping)
  </read_first>
  <behavior>
    - Two web-ready source photos exist under src/assets/photos/ at ≤2400px wide, named milan-networking.jpg and milan-speaking-graph.jpg
    - nl gains top-level coaching and spreker keys with pageTitle/pageDesc/eyebrow/heading/body and CTA labels, drafted only from existing material
  </behavior>
  <action>
Extract `_AVM2348.jpg` and `10 Jaar Soly-78.jpg` from the source archive `fotos/wetransfer_10-jaar-soly-77-jpg_2026-08-18_1205.zip` (ASSETS.md). Downscale each with sharp to ≤2400px wide at quality ~80 (same pipeline STATE.md records for Phase 1 photos, keeping each derivative source well under the 300KB threshold), and write them as `src/assets/photos/milan-networking.jpg` (from `_AVM2348.jpg`, 1920w source) and `src/assets/photos/milan-speaking-graph.jpg` (from `10 Jaar Soly-78.jpg`). The full-res originals stay out of git (originals gitignored per ASSETS.md rules). Record the finalized mapping/derivative sizes in the SUMMARY.

Extend `src/i18n/nl.ts`: add top-level `coaching` and `spreker` keys (same `as const` level as `contact` from Plan 01; PATTERNS.md §nl.ts shape). For `coaching`: `pageTitle`/`pageDesc` from UI-SPEC §SEO table, `eyebrow` ("1:1 Coaching"), `heading`, a `body` string-array of drafted editorial paragraphs, and CTA labels ("Plan kennismaking", secondary "Meer over mij"). For `spreker`: `pageTitle`/`pageDesc` from the SEO table, `eyebrow` ("Spreker"), `heading`, a `body` array, topics/aanbod strings, and CTA label ("Boek lezing"). Draft ALL copy from existing material only — the homepage story, USPs (12+ jaar · 180 medewerkers · 9 markten), the Soly arc (9 landen, ~180 medewerkers, ~1M panelen, faillissement), and the 3 existing testimonials (D-05). Invent no facts, numbers, quotes, or testimonials. Use informal "je" tone. Flag the drafted copy for Milan's review in the SUMMARY.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/assets/photos/milan-networking.jpg` and `src/assets/photos/milan-speaking-graph.jpg` both exist
    - each new photo file is under 300KB (`ls -l` / stat size check)
    - `grep -c "coaching:" src/i18n/nl.ts` returns at least 1 and `grep -c "spreker:" src/i18n/nl.ts` returns at least 1
    - `grep -c "1:1 Coaching" src/i18n/nl.ts` returns 1
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Both subpage photos are extracted, downscaled, and committed as web sources; nl.ts carries drafted coaching + spreker copy built only from existing material; build green.</done>
  <reversibility rating="reversible">Photos and copy are additive assets.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build /coaching and /spreker editorial landing pages</name>
  <files>src/pages/coaching.astro, src/pages/spreker.astro</files>
  <read_first>
    - src/pages/index.astro (page shell + section composition analog)
    - src/pages/contact.astro (Plan 01 output — sibling page shell reference)
    - src/components/ui/SectionWrapper.astro, src/components/ui/Card.astro, src/components/ui/Button.astro, src/components/ui/RevealOnScroll.astro (reused primitives + prop interfaces)
    - src/components/sections/PressStrip.astro (reused on Spreker), src/components/sections/Newsletter.astro (reused page bottom)
    - src/components/sections/Testimonials.astro (data source nl.testimonials.items; single-card reuse note)
    - .planning/phases/02-supporting-pages-lead-gen-forms/02-UI-SPEC.md §"/coaching (PAGE-01)" and §"/spreker (PAGE-02)" layout contracts + §Color dark-band rule
    - .planning/phases/02-supporting-pages-lead-gen-forms/02-PATTERNS.md §pages, §Testimonials, §BASE_URL Internal Link, §RevealOnScroll Stagger
  </read_first>
  <behavior>
    - /coaching section order: hero (eyebrow/H1/subtext/primary CTA Plan kennismaking → /contact?type=coaching, ghost secondary Meer over mij → /mijn-verhaal) → approach cards → single Yang Soo Kloosterhof testimonial → CTA band → Newsletter
    - /spreker section order: hero (primary CTA Boek lezing → /contact?type=lezing, optional speaking photo) → PressStrip → topics/aanbod → single Oranjewoud testimonial → CTA band → Newsletter
    - Each page uses at most ONE dark band; one yellow signature per section maximum
    - Single testimonial per page rendered via Card, not the 3-card mosaic; attribution verbatim
    - All internal CTAs use the BASE_URL prefix template-literal form
  </behavior>
  <action>
Create `src/pages/coaching.astro` and `src/pages/spreker.astro` following the index.astro shell (PATTERNS.md §pages). Each wraps `BaseLayout` with `title={nl.coaching.pageTitle}`/`description={nl.coaching.pageDesc}` (resp. spreker) for per-page SEO (D-12). Compose from Phase 1 primitives only — no new bespoke components:

Coaching (UI-SPEC §/coaching): typographic hero `SectionWrapper variant="default"` (eyebrow, H1, subtext, primary `Button` "Plan kennismaking" href `${import.meta.env.BASE_URL}contact?type=coaching`, ghost secondary `Button` "Meer over mij" href `${import.meta.env.BASE_URL}mijn-verhaal`); an approach/offer `SectionWrapper` (variant `surface` OR the ONE `dark` band for the page) with 2-3 `Card variant="default"` items wrapped in `RevealOnScroll` staggered 80ms; a single-testimonial `SectionWrapper variant="default"` rendering `nl.testimonials.items[0]` (Yang Soo Kloosterhof) in one `Card` (verbatim attribution, D-07); a terminal CTA band ("Plan kennismaking" → `/contact?type=coaching`); reused `Newsletter`. Use `milan-networking.jpg` via `astro:assets` `<Picture>` (formats avif/webp, lazy, alt "Milan in gesprek met ondernemers tijdens een evenement.", widths 400/600/800, sizes per UI-SPEC §Image Pipeline).

Spreker (UI-SPEC §/spreker): hero `SectionWrapper variant="default"` (eyebrow "Spreker", H1, subtext, primary `Button` "Boek lezing" href `${import.meta.env.BASE_URL}contact?type=lezing`, optional `milan-speaking-graph.jpg` `<Picture>` in a right column, alt "Milan legt een grafiek uit tijdens een presentatie.", widths 400/600/800); reused `PressStrip` directly below hero; a topics/aanbod `SectionWrapper` (variant surface OR the one dark band) with cards or a structured list from `nl.spreker`; a single-testimonial section rendering `nl.testimonials.items[2]` (Oranjewoud Export Academy) in one `Card variant="dark"` (verbatim, D-07); a CTA band ("Boek lezing" → `/contact?type=lezing`); reused `Newsletter`.

Every `SectionWrapper` gets a unique `id` + `labelledBy` pointing at its `<h2>` id (a11y contract). Enforce the restrained-yellow rule (Pitfall 4) and the at-most-one-dark-band rule per page. Never `href="#"` — the Meer over mij / Mijn verhaal links point at `/mijn-verhaal` which ships in Plan 03; the build will 404-free because those are static hrefs resolved at request time, not build imports (the page ships in the same phase). Use the `${import.meta.env.BASE_URL}...` template-literal form for every internal href (Pitfall 2).
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/coaching.astro` and `src/pages/spreker.astro` exist; `npm run build` exits 0 and produces /coaching and /spreker routes in dist/
    - `grep -c "type=coaching" src/pages/coaching.astro` returns at least 1 (Plan kennismaking deep-link)
    - `grep -c "type=lezing" src/pages/spreker.astro` returns at least 1 (Boek lezing deep-link)
    - `grep -c "import.meta.env.BASE_URL" src/pages/coaching.astro src/pages/spreker.astro` returns at least 2
    - `grep -c 'href="#"' src/pages/coaching.astro src/pages/spreker.astro` returns 0
    - each page references exactly one relevant testimonial (grep the Kloosterhof attribution appears on coaching, Oranjewoud on spreker via nl.testimonials.items index usage)
    - `grep -c 'formats={' src/pages/coaching.astro src/pages/spreker.astro` returns at least 1 (astro:assets Picture used)
  </acceptance_criteria>
  <done>Both editorial landing pages build green, compose only Phase 1 primitives, deep-link CTAs into the contact flow with correct type params, and each shows exactly one relevant verbatim testimonial.</done>
  <reversibility rating="reversible">Pages are additive routes.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Enable Coaching + Spreker in Nav and Footer</name>
  <files>src/components/layout/Nav.astro, src/components/layout/Footer.astro</files>
  <read_first>
    - src/components/layout/Nav.astro (the file being modified — navItems array; conditional href-or-span render; active-state CSS already present; Plan 01 already enabled the Contact CTA)
    - src/components/layout/Footer.astro (the file being modified — footer navItems string array)
    - .planning/phases/02-supporting-pages-lead-gen-forms/02-PATTERNS.md §Nav.astro and §Footer.astro (exact href-or-reason refactor + conditional render)
    - .planning/phases/02-supporting-pages-lead-gen-forms/02-RESEARCH.md §"Pattern 2: Nav + Footer Enablement" + Pitfall 5 (active state)
  </read_first>
  <behavior>
    - Coaching and Spreker nav items render as real <a href> (BASE_URL prefixed), with nav-link--active + aria-current="page" on the current route
    - Nieuwsbrief and Boek stay disabled honest placeholders (Nieuwsbrief reason references Phase 3, Boek references Phase 5)
    - Mijn verhaal stays disabled in this plan (enabled in Plan 03)
    - Footer mirrors the same enablement for Coaching + Spreker
  </behavior>
  <action>
Refactor `src/components/layout/Nav.astro` navItems to the href-or-reason object shape (PATTERNS.md §Nav.astro): give `coaching` → `${import.meta.env.BASE_URL}coaching` and `spreker` → `${import.meta.env.BASE_URL}spreker`; leave `nieuwsbrief` (reason: Nieuwsbrief pagina volgt in fase 3), `mijnVerhaal` (reason: Mijn verhaal pagina volgt in fase 2 — still disabled here, enabled in Plan 03), and `boek` (reason: Boek pagina volgt in fase 5) as disabled entries. Update the map to the conditional render: items with `href` become `<a>` with `class` including `nav-link--active` and `aria-current="page"` when `Astro.url.pathname` starts with the item href (compute `const currentPath = Astro.url.pathname` in frontmatter, Pitfall 5); items without `href` keep the existing disabled `<span>` pattern. The same `navItems.map` drives desktop and the mobile panel — one array edit covers both. Reuse the existing `.nav-link--active` CSS (already defined, no new CSS).

Refactor `src/components/layout/Footer.astro` navItems from the plain string array to the same `{label, href?}` / `{label, reason?}` object shape (PATTERNS.md §Footer.astro): enable `coaching` and `spreker` as `<a>` links (BASE_URL prefixed), keep `nieuwsbrief`/`mijnVerhaal`/`boek` as disabled spans. Never `href="#"`.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -c "BASE_URL}coaching" src/components/layout/Nav.astro` returns at least 1 and `grep -c "BASE_URL}spreker" src/components/layout/Nav.astro` returns at least 1
    - `grep -c "aria-current" src/components/layout/Nav.astro` returns at least 1 (active-state wiring)
    - Nav still contains a disabled span for the Boek and Nieuwsbrief items (`grep -c "aria-disabled" src/components/layout/Nav.astro` returns at least 1)
    - `grep -c "BASE_URL}coaching" src/components/layout/Footer.astro` returns at least 1
    - `grep -c 'href="#"' src/components/layout/Nav.astro src/components/layout/Footer.astro` returns 0
  </acceptance_criteria>
  <done>Coaching and Spreker are reachable from both the header nav (with active-state) and the footer; Nieuwsbrief/Boek/Mijn-verhaal remain honest disabled placeholders; build green.</done>
  <reversibility rating="reversible">Nav/Footer array edits are trivially revertible.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → static pages | Coaching/Spreker are static HTML with no input surface of their own (the only input surface remains the ContactForm on /contact) |
| internal CTA hrefs → /contact | CTAs pass a fixed `?type=` value that /contact validates against its option set (see Plan 01 T-2-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-06 | Tampering | Coaching/Spreker CTA `?type=` deep-links | low | mitigate | CTAs emit only the fixed literals `type=coaching` / `type=lezing`; the /contact ContactForm validates the received value against its option set with a CSS.escape guard (Plan 01 T-2-04) — unknown values fall back to Algemeen, never reflected. |
| T-2-07 | Info disclosure | Content honesty (fabricated facts/testimonials) | medium | mitigate | All copy drafted from existing material only (D-05); testimonials reused verbatim from nl.testimonials.items (D-07); no invented facts/numbers/quotes. Drafted copy flagged for user review. |
| T-2-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new packages (sharp already installed; Web3Forms is external API only). No install task; gate does not fire. |
</threat_model>

<verification>
- `npm run build` exits 0 with /coaching, /spreker routes present in dist/
- Each subpage shows exactly one relevant verbatim testimonial (no fabrication)
- Coaching/Spreker CTAs carry the correct `?type=` deep-link with BASE_URL prefix; no `href="#"`
- Restrained-yellow rule and at-most-one-dark-band-per-page respected (visual backstop at `npm run preview`, desktop→320px)
- Nav/Footer: Coaching+Spreker enabled with active-state; Nieuwsbrief/Boek/Mijn-verhaal stay disabled placeholders
</verification>

<success_criteria>
- ROADMAP Phase 2 #1: Coaching and Spreker pages exist, styled with the Phase 1 design system (Mijn verhaal completes in Plan 03)
- ROADMAP #3 (partial): Boek lezing / Plan kennismaking CTAs on these pages route into the contact flow (homepage CTA wiring completes in Plan 03)
- ROADMAP #4: new pages are SEO-, a11y-, and responsive-consistent with the homepage
</success_criteria>

<output>
Create `.planning/phases/02-supporting-pages-lead-gen-forms/02-02-SUMMARY.md` when done. Flag drafted `nl.coaching` + `nl.spreker` copy and finalized photo derivatives for Milan's review (D-05).
</output>
