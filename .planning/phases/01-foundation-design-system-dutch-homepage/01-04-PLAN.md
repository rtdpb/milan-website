---
phase: 01-foundation-design-system-dutch-homepage
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/assets/photos/milan-story-portrait.jpg
  - src/assets/photos/milan-energy-mission.jpg
  - src/components/ui/RevealOnScroll.astro
  - src/components/sections/Testimonials.astro
  - src/components/sections/Story.astro
autonomous: true
requirements: [FND-04, FND-08, HOME-05, HOME-06, HOME-11]

estimate:
  tokens: 66000
  raw_tokens: 33000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A visitor sees a testimonials section with the 3 supplied quotes and exact attributions (Yang Soo Kloosterhof, Ruud Koornstra, Oranjewoud Export Academy) in a mixed-size mosaic"
    - "A visitor sees the full-width dark 'Waarom ik nu mijn lessen deel' story band with the full supplied story copy, a signature, and a supporting portrait photo"
    - "Sections below the fold fade/translate in on scroll, and animations are suppressed under prefers-reduced-motion"
    - "The story reading column stays within a readable measure and does not overflow on mobile"
  artifacts:
    - path: "src/components/ui/RevealOnScroll.astro"
      provides: "IntersectionObserver scroll-reveal island that respects prefers-reduced-motion"
      contains: "prefers-reduced-motion"
    - path: "src/components/sections/Testimonials.astro"
      provides: "Mixed-size testimonial mosaic, 3 quotes + attributions, one dark accent card"
      contains: "Kloosterhof"
    - path: "src/components/sections/Story.astro"
      provides: "Full-width dark band personal story with copy, signature, portrait image, #verhaal anchor"
      contains: "Waarom ik nu mijn lessen deel"
  key_links:
    - from: "src/components/sections/Story.astro"
      to: "src/assets/photos/milan-story-portrait.jpg"
      via: "astro:assets Picture (lazy) generates AVIF/WebP portrait"
      pattern: "milan-story-portrait"
    - from: "src/components/sections/Testimonials.astro"
      to: "src/components/ui/RevealOnScroll.astro"
      via: "cards wrapped in reveal island (staggered)"
      pattern: "RevealOnScroll"
  prohibitions:
    - "No fabricated 'trusted by N clients' stat or star ratings — only the 3 supplied quotes (D-08)"
    - "Attributions and quote text preserved verbatim from source — no name/claim changes (HOME-11)"
    - "Story band is the only full-width dark band besides potential footer treatment (D-01); yellow limited to one accent in the story (D-02)"
    - "No original jpg (>300KB) emitted to dist/ from story photos (FND-10)"
    - "Yellow #FFDD11 not used as body text on cream (contrast) — decorative quote mark on the dark card only"
---

<objective>
Add the two narrative-credibility sections: the testimonials mosaic and the dramatic full-width dark personal-story band, plus the reusable scroll-reveal island that delivers the phase's subtle animations.

Purpose: Testimonials + Story are the emotional core that makes Milan credible and human. The Story is the designated dark feature band (D-01) and the target of the hero's "Mijn verhaal" anchor. This plan also builds the RevealOnScroll island (FND-08) — reused by later sections — and proves reduced-motion suppression. Owns distinct component files ONLY (it does NOT edit src/pages/index.astro) so it runs fully parallel with plans 03 and 05 in wave 3; the final assembly plan (06, wave 4) wires all sections in document order.

Output: RevealOnScroll, Testimonials and Story components, with scroll-reveal animation that respects prefers-reduced-motion — ready for wiring.

Implements FND-04 (responsive), FND-08 (animations + reduced-motion per D-15), HOME-05 (testimonials per D-08), HOME-06 (story per D-01), HOME-11 (verbatim copy/attributions).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ASSETS.md
@.planning/phases/01-foundation-design-system-dutch-homepage/SKELETON.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-RESEARCH.md
@src/components/ui/SectionWrapper.astro
@src/components/ui/Card.astro
@src/i18n/nl.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RevealOnScroll island (FND-08, reduced-motion safe)</name>
  <files>src/components/ui/RevealOnScroll.astro</files>
  <read_first>
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-RESEARCH.md §Pattern 5 (RevealOnScroll), §Animations
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md §Interaction Contracts (Scroll-Reveal Animations)
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md D-15
  </read_first>
  <action>
    Create src/components/ui/RevealOnScroll.astro per RESEARCH §Pattern 5: a wrapper with a slot and a data-reveal attribute. Scoped CSS: initial opacity 0 + transform translateY 16px transitioning to visible on an is-visible class (duration ~400ms, --ease-out per UI-SPEC). A script island reads matchMedia reduce — if reduced-motion is set it does NOT observe (elements stay visible via the media-query CSS fallback that sets opacity/transform to none); otherwise an IntersectionObserver (threshold ~0.15, rootMargin bottom -40px) adds is-visible and unobserves. Include the reduced-motion media block that forces data-reveal elements to full opacity / no transform / no transition (so content is never hidden for reduced-motion users). No framework (D-14).
  </action>
  <verify>
    <automated>npm run build; echo "exit:$?"; grep -c 'prefers-reduced-motion' src/components/ui/RevealOnScroll.astro; grep -c 'IntersectionObserver' src/components/ui/RevealOnScroll.astro</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep 'prefers-reduced-motion' src/components/ui/RevealOnScroll.astro` matches (both the JS guard and the CSS fallback).
    - `grep 'IntersectionObserver' src/components/ui/RevealOnScroll.astro` matches.
    - `grep 'data-reveal' src/components/ui/RevealOnScroll.astro` matches.
  </acceptance_criteria>
  <done>A reusable scroll-reveal island exists; reduced-motion users get fully-visible, unanimated content; others get a subtle fade/translate on scroll.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Testimonials mixed-size mosaic (3 supplied quotes)</name>
  <files>src/components/sections/Testimonials.astro</files>
  <read_first>
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md §Component Inventory `<TestimonialsSection>`, §Copywriting Contract, §UI Considerations (zero-one-many: exactly 3)
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md D-08, D-01
    - .planning/PROJECT.md §Context (testimonial names); Input homepage Milan.docx (source of exact quote text — preserve verbatim)
    - src/components/ui/Card.astro, src/components/ui/RevealOnScroll.astro, src/i18n/nl.ts
  </read_first>
  <action>
    Create src/components/sections/Testimonials.astro per UI-SPEC `<TestimonialsSection>` (HOME-05, D-08): a light section (SectionWrapper) with heading "Wat anderen zeggen" (or the exact docx wording if supplied — preserve verbatim). A mixed-size mosaic (Sevora-style): one LARGE card + two smaller cards, one card per supplied testimonial. Attributions preserved EXACTLY: Yang Soo Kloosterhof, Ruud Koornstra, Oranjewoud Export Academy (with the supplied role/context). The large card uses the dark accent surface (--color-dark-surface, white quote text) — the single dark accent card allowed in an otherwise light section (D-01). Small cards use --color-surface. A large decorative open-quote glyph in --color-accent (~64px) top-left of the dark card — a COUNTED yellow signature moment (D-02). Wrap the cards in RevealOnScroll (stagger). Hard-code exactly 3 cards (UI-SPEC zero-one-many: fixed count in v1). No fabricated "trusted by N" stat and no star ratings (D-08). Source the quote text from the docx and preserve it verbatim; if a quote is ambiguous in extraction, keep the safest verbatim rendering and add a code comment flagging it for user confirmation (HOME-11).
  </action>
  <verify>
    <automated>npm run build; echo "exit:$?"; grep -oi 'Kloosterhof\|Koornstra\|Oranjewoud' dist/index.html | sort -u | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - All 3 attributions appear in dist/index.html: `grep 'Yang Soo Kloosterhof' dist/index.html`, `grep 'Ruud Koornstra' dist/index.html`, `grep 'Oranjewoud Export Academy' dist/index.html` each match.
    - Exactly 3 testimonial cards render (verify markup count in Testimonials.astro).
    - No fabricated stat: `grep -iE 'trusted by|[0-9]+\+? (clients|klanten|reviews)' dist/index.html` returns nothing from this section.
    - One dark accent card present (--color-dark-surface used once here).
  </acceptance_criteria>
  <done>Testimonials renders exactly 3 supplied quotes with verbatim attributions in a mixed-size mosaic with one dark accent card and a single yellow decorative quote mark; no fabricated numbers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Story dark feature band (full copy, signature, portrait)</name>
  <files>src/assets/photos/milan-story-portrait.jpg, src/assets/photos/milan-energy-mission.jpg, src/components/sections/Story.astro</files>
  <read_first>
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md §Component Inventory `<StorySection>`, §Image Pipeline Contract, §UI Considerations (long-text: story body)
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md D-01, D-02
    - .planning/ASSETS.md §Section→source mapping (story portrait = 10 Jaar Soly-76 kopie → milan-story-portrait; mission = Foto.jpeg → milan-energy-mission)
    - Input homepage Milan.docx (source of full story copy — preserve verbatim)
    - src/components/ui/SectionWrapper.astro (dark variant), src/i18n/nl.ts
  </read_first>
  <precondition>src/assets/photos/milan-story-portrait.jpg (from 10 Jaar Soly-76 kopie.jpg, 4311x6467 portrait) exists, extracted from the gitignored fotos zip. The mission photo milan-energy-mission.jpg (from Foto.jpeg, 1440x960) is optional. Halt only if the portrait is required and absent; otherwise render with a labelled placeholder slot.</precondition>
  <action>
    Create src/components/sections/Story.astro per UI-SPEC `<StorySection>` (HOME-06, D-01): a FULL-WIDTH DARK BAND via SectionWrapper variant dark (--color-dark-band, all text --color-dark-text). Give the section id verhaal (this is the hero secondary CTA anchor target). Heading "Waarom ik nu mijn lessen deel" (nl.story) in var(--font-display) in dark-text. Desktop two-column: reading column left (max ~640px measure — long-text backstop, must not overflow on mobile) + portrait photo right. Body = the FULL supplied story copy from the docx, 17px/400, line-height 1.6, preserved verbatim (HOME-11) — obvious extraction glitches (run-together spacing, mojibake) may be corrected but factual claims/names left intact; flag anything suspect in a code comment. Signature block "Milan van der Meulen" in var(--font-display) 20px/600 below the final paragraph. ONE yellow accent only (D-02): a 4px left-border on a pull-quote OR the eyebrow label background — one usage. Portrait: astro:assets Picture importing milan-story-portrait.jpg, formats avif/webp, lazy, widths [400,600,900] (RESEARCH), sizes per UI-SPEC, alt "Milan van der Meulen op het podium.". Optional milan-energy-mission.jpg inline at ~50% column width if it strengthens the narrative (lazy, widths [400,800]).
  </action>
  <verify>
    <automated>npm run build; echo "exit:$?"; grep -c 'Waarom ik nu mijn lessen deel' dist/index.html; grep -c 'id="verhaal"' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep 'Waarom ik nu mijn lessen deel' dist/index.html` matches.
    - `grep 'id="verhaal"' dist/index.html` matches (hero "Mijn verhaal" anchor target exists).
    - `grep 'Milan van der Meulen' dist/index.html` matches within the story signature.
    - Story portrait serves optimized: `ls dist/_astro/ | grep milan-story-portrait` shows avif/webp derivatives; no >300KB jpg for it in dist/.
    - The dark band renders (--color-dark-band used) and only ONE yellow accent appears in the story (source review of Story.astro).
  </acceptance_criteria>
  <done>The full-width dark story band renders the full verbatim story copy, the signature, and the optimized portrait, with id=verhaal and a single yellow accent; no original jpg served.</done>
</task>

</tasks>

> Wiring note: Testimonials and Story are NOT rendered into src/pages/index.astro by this plan. The final assembly plan (01-06, wave 4) owns src/pages/index.astro and imports/renders all sections in document order. This keeps wave-3 plans free of any shared-file conflict on index.astro. The `id="verhaal"` on Story is the hero secondary-CTA anchor target and must survive assembly.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist → visitor browser | Static narrative sections + one tiny IntersectionObserver island (scroll reveal). No user input, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Repudiation | fabricated testimonial stats / altered quotes | medium | mitigate | Only the 3 supplied quotes with verbatim attributions (D-08, HOME-11); no invented numbers; ambiguous extractions flagged in code comments for user confirmation |
| T-01-09 | Denial of Service (client) | scroll-reveal island jank / hidden content | low | mitigate | Reduced-motion path skips the observer and CSS forces content visible — content never trapped hidden if JS fails or motion is reduced (FND-08) |
| T-01-02 | Information Disclosure | story-photo originals | high | mitigate | Portrait/mission imported from src/assets/ (optimized derivatives only); originals stay gitignored; no >300KB jpg in dist/ |
</threat_model>

<verification>
- `npm run build` exits 0 (all three components compile).
- Testimonials: exactly 3 supplied quotes + verbatim attributions; one dark accent card; single yellow quote mark; no fabricated stats/ratings.
- Story: full-width dark band, full verbatim copy, signature, optimized portrait, id=verhaal, one yellow accent, readable measure on mobile.
- Scroll-reveal respects prefers-reduced-motion (JS guard + CSS fallback); no story/testimonial original jpg >300KB in dist/.
- Final document order (Testimonials → Story after Samenwerken) is asserted in plan 01-06 (assembly).
</verification>

<success_criteria>
A visitor reaches the emotional core: three honest testimonials in a mosaic and a dramatic dark personal-story band with the full verbatim copy and an optimized portrait. Below-fold content reveals subtly on scroll and is fully accessible under reduced-motion.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-system-dutch-homepage/01-04-SUMMARY.md` when done.
</output>
