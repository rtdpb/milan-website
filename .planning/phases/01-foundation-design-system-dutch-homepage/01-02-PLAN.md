---
phase: 01-foundation-design-system-dutch-homepage
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/config.ts
  - src/i18n/nl.ts
  - src/components/ui/Button.astro
  - src/components/ui/Card.astro
  - src/components/ui/SectionWrapper.astro
  - src/components/ui/PlaceholderBadge.astro
  - src/components/layout/Nav.astro
  - src/components/layout/Footer.astro
  - src/layouts/BaseLayout.astro
autonomous: true
requirements: [FND-03, FND-09, HOME-01, HOME-09, HOME-10, HOME-11]

estimate:
  tokens: 82000
  raw_tokens: 41000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A visitor sees the sticky site header with wordmark, the 5 nav items (Coaching, Spreker, Nieuwsbrief, Mijn verhaal, Boek), a Contact CTA, an NL|EN affordance and a LinkedIn icon"
    - "On mobile the hamburger toggle opens and closes a nav panel via keyboard and click"
    - "A visitor sees the site footer with wordmark, nav links, LinkedIn, and legal/copyright"
    - "Every nav item and CTA with an unknown destination is rendered disabled (aria-disabled), never a fake working link"
    - "Reusable Button, Card, SectionWrapper and PlaceholderBadge components exist and are token-styled"
    - "All Dutch UI strings and all placeholder/TODO destinations live in one place (nl.ts + config.ts)"
  artifacts:
    - path: "src/components/ui/Button.astro"
      provides: "CTA variants (primary/secondary/ghost/ghost-on-dark) + honest disabled placeholder state"
      contains: "aria-disabled"
    - path: "src/components/ui/Card.astro"
      provides: "Reusable rounded card shell (surface bg, border, radius, padding)"
      min_lines: 8
    - path: "src/components/ui/SectionWrapper.astro"
      provides: "Max-width container + section padding + optional dark-band variant + section landmark"
      contains: "section"
    - path: "src/components/ui/PlaceholderBadge.astro"
      provides: "Visible [Placeholder] marker pill"
      contains: "Placeholder"
    - path: "src/components/layout/Nav.astro"
      provides: "Site header: wordmark, nav items, Contact CTA, NL|EN affordance, LinkedIn icon, mobile toggle island"
      contains: "aria-disabled"
    - path: "src/components/layout/Footer.astro"
      provides: "Site footer: wordmark, nav links, LinkedIn, legal/copyright"
      contains: "footer"
    - path: "src/i18n/nl.ts"
      provides: "All Dutch UI strings as a typed const object (nav, hero, sections, CTAs, footer)"
      contains: "export const nl"
    - path: "src/config.ts"
      provides: "Single source for placeholder destinations (TODO_SUBSTACK_URL, TODO_CONTACT_URL, TODO_LINKEDIN_URL) + site constants"
      contains: "TODO_SUBSTACK_URL"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/layout/Nav.astro"
      via: "renders Nav in header slot + Footer before closing body"
      pattern: "Nav"
    - from: "src/components/layout/Nav.astro"
      to: "src/i18n/nl.ts"
      via: "imports nav strings from the nl translation object"
      pattern: "nl\\."
    - from: "src/components/ui/Button.astro"
      to: "src/config.ts"
      via: "disabled CTAs reference placeholder-destination constants / placeholderReason"
      pattern: "placeholderReason"
  prohibitions:
    - "No href=\"#\" or href=\"\" placeholder links anywhere — disabled nav/CTAs use aria-disabled + tabindex handling (HOME-10)"
    - "No fake success state or JS click handler on any disabled CTA"
    - "The NL|EN switch is a disabled affordance only — no functional language switching (D-16, Phase 4)"
    - "Yellow #FFDD11 used only for the primary CTA fill, active-nav underline and focus ring — never nav backgrounds or footer fills (D-02)"
    - "Nav/footer must NOT invent factual claims or destinations not present in source artifacts (HOME-11)"
---

<objective>
Build the reusable UI kit and the global site chrome (header + footer) on top of the Walking Skeleton, and establish the honest-CTA system and the single-source content/config modules that every later section consumes.

Purpose: Sections in plans 03-05 are horizontal expansions that must compose from a consistent component library (Button/Card/SectionWrapper/PlaceholderBadge) and pull strings from one place (nl.ts) and placeholder destinations from one place (config.ts). This plan defines those contracts first (interface-first ordering) so no later executor guesses. It also delivers the header/footer and the honest disabled-CTA pattern that HOME-10/D-12 require globally.

Output: A homepage that renders BaseLayout with a working sticky header (desktop + mobile toggle), a footer, a reusable UI kit, and one-source content/config — all CTAs honestly disabled where destinations are unknown.

Implements FND-03 (reusable components), FND-09 (nl.ts i18n strings per D-16), HOME-01 (header), HOME-09 (footer), HOME-10 (honest CTA/link handling per D-12), HOME-11 (preserve supplied copy).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/ASSETS.md
@.planning/phases/01-foundation-design-system-dutch-homepage/SKELETON.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-RESEARCH.md
@src/layouts/BaseLayout.astro
@src/styles/tokens.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Content + config single-source modules + reusable UI kit (contracts first)</name>
  <files>src/config.ts, src/i18n/nl.ts, src/components/ui/Button.astro, src/components/ui/Card.astro, src/components/ui/SectionWrapper.astro, src/components/ui/PlaceholderBadge.astro</files>
  <read_first>
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-RESEARCH.md §i18n-Ready Structure (nl.ts pattern), §Pattern 7 (Button disabled), §Open Questions Q4 (TODO_ constants)
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md §Interaction Contracts (Button variants, disabled/placeholder pattern), §Copywriting Contract, §Component Inventory
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md D-07, D-09, D-10, D-12, D-17
    - src/styles/tokens.css (token names to reference)
  </read_first>
  <action>
    Create src/config.ts exporting single-source placeholder destinations as string constants (RESEARCH Q4): TODO_SUBSTACK_URL, TODO_CONTACT_URL, TODO_LINKEDIN_URL (each a clearly non-navigable sentinel with a comment naming the phase that wires it), plus SITE_NAME set to Milan van der Meulen and the disabled-CTA tooltip constant. These are the ONLY place these unknown destinations appear (grep-replaceable when supplied).
    Create src/i18n/nl.ts exporting a typed const object `nl` (RESEARCH §i18n-Ready Structure) containing every Dutch string from UI-SPEC §Copywriting Contract, grouped: nav (coaching/spreker/nieuwsbrief/mijnVerhaal/boek/contact), hero, pressStrip, samenwerken (heading + 3 ladder eyebrows + 3 card titles/desc/CTAs), testimonials, story, newsletter, articles, footer, common (placeholderLabel bracket-Placeholder-bracket, disabledTooltip Binnenkort beschikbaar, nlEnSwitch NL bar EN). Preserve supplied factual claims/names verbatim (HOME-11). Header comment: Phase 4 adds en.ts alongside (D-16) — this const shape is the future-proof contract.
    Create the UI kit (FND-03), all token-styled scoped CSS, no utility framework:
    - Button.astro (RESEARCH §Pattern 7, UI-SPEC §Button Variants): typed Props with href, variant (primary/secondary/ghost/ghost-on-dark), disabled, placeholderReason, anchor. When disabled render a span with role button, aria-disabled true, tabindex -1, data-placeholder set to placeholderReason and title set to the disabled tooltip; else render an anchor with the href. Never emit href hash. Variants use the exact colours from UI-SPEC (primary = accent fill + near-black text; hover accent-dark + scale 1.02; disabled opacity 0.5, cursor not-allowed). Min 44px touch target.
    - Card.astro: rounded card shell (--color-surface bg, --color-border border, --radius-lg, --space-lg padding) with a slot; optional variant dark uses --color-dark-surface.
    - SectionWrapper.astro: a section element supporting aria-labelledby, max-width 1280px inner, --section-padding-y; optional variant dark = full-bleed dark band (--color-dark-band, text --color-dark-text) and optional variant surface.
    - PlaceholderBadge.astro: small pill rendering the Placeholder label (nl.common.placeholderLabel), --color-text-secondary border, italic — the visible placeholder marker used by press logos + article cards.
  </action>
  <verify>
    <automated>npm run build; echo "exit:$?"; grep -c 'TODO_SUBSTACK_URL' src/config.ts; grep -c 'nl' src/i18n/nl.ts; grep -c 'aria-disabled' src/components/ui/Button.astro</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep 'TODO_SUBSTACK_URL' src/config.ts`, `grep 'TODO_CONTACT_URL' src/config.ts`, `grep 'TODO_LINKEDIN_URL' src/config.ts` all match.
    - `grep 'export const nl' src/i18n/nl.ts` matches and the object contains nav + samenwerken + newsletter groups.
    - `grep 'aria-disabled' src/components/ui/Button.astro` matches; `grep 'href="#"' src/components/ui/Button.astro` returns nothing.
    - All four UI components exist under src/components/ui/.
  </acceptance_criteria>
  <done>Single-source config + nl.ts strings exist and the four reusable UI-kit components build; the honest disabled-CTA contract is implemented in Button.astro; no href hash anywhere.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Site header (Nav) with mobile toggle island + honest disabled nav/CTAs</name>
  <files>src/components/layout/Nav.astro</files>
  <read_first>
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md §Component Inventory `<SiteHeader>`, §Interaction Contracts (Navigation — Mobile, Disabled/Placeholder CTA), §Accessibility Contract
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md D-12, D-16
    - src/components/ui/Button.astro, src/i18n/nl.ts, src/config.ts (from Task 1)
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-RESEARCH.md §Architecture Patterns (Nav = vanilla JS island), §Accessibility Baseline
  </read_first>
  <action>
    Create src/components/layout/Nav.astro implementing UI-SPEC `<SiteHeader>` (HOME-01): a `<header>` with `<nav aria-label="Hoofdnavigatie">`. Left: wordmark SITE_NAME in var(--font-display) 18px/600. Center/right: the 5 nav items from nl.nav (Coaching, Spreker, Nieuwsbrief, Mijn verhaal, Boek) — all rendered but DISABLED per D-12 (destinations are future pages): use the Button/anchor honest-disabled pattern (aria-disabled true, title = disabled tooltip, class nav-link--disabled, cursor not-allowed, opacity per SPEC). Right: Contact CTA = primary Button disabled (placeholderReason references TODO_CONTACT_URL / Phase 2). NL|EN switch = disabled affordance (aria-disabled true, opacity 0.4, label from nl.common.nlEnSwitch) — no functional switching (D-16). LinkedIn icon = inline SVG rendered but disabled (aria-disabled true, references TODO_LINKEDIN_URL) — never href hash; colour --color-text-secondary not yellow.
    Sticky/scroll-aware background: transparent at top, --color-bg + subtle shadow after ~60px scroll — implement via a tiny vanilla-JS island toggling a class on scroll (respect it degrades gracefully).
    Mobile (UI-SPEC §Navigation — Mobile): hamburger button at 44x44 touch target with aria-expanded + aria-controls; a vanilla-JS island toggles a right-slide panel (transform translateX 100% to 0, 240ms ease-out) containing the same nav items; Escape and the close button close it; focus moves into the panel on open. Respect prefers-reduced-motion (instant show/hide). All disabled states preserved on mobile. No framework — small `<script>` island only (D-14).
  </action>
  <verify>
    <automated>npm run build; echo "exit:$?"; grep -c 'aria-expanded' dist/index.html; grep -c 'aria-label="Hoofdnavigatie"' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - After build, `grep 'aria-label="Hoofdnavigatie"' dist/index.html` matches.
    - `grep 'aria-expanded' dist/index.html` matches (mobile toggle button present).
    - `grep -c 'aria-disabled="true"' dist/index.html` returns >= 6 (5 nav items + Contact CTA + NL|EN + LinkedIn all honestly disabled).
    - `grep 'href="#"' src/components/layout/Nav.astro` returns nothing.
    - `grep -oi 'coaching\|spreker\|nieuwsbrief\|mijn verhaal\|boek' dist/index.html | sort -u` shows the 5 nav labels.
  </acceptance_criteria>
  <done>The sticky header renders the wordmark, 5 disabled nav items, disabled Contact CTA, disabled NL|EN switch and disabled LinkedIn icon; the mobile hamburger opens/closes an accessible nav panel; no fake links.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Site footer + wire Nav/Footer into BaseLayout</name>
  <files>src/components/layout/Footer.astro, src/layouts/BaseLayout.astro</files>
  <read_first>
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md §Component Inventory `<SiteFooter>`, §Copywriting Contract
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md D-01 (footer is light, dark band reserved for story), D-12
    - src/layouts/BaseLayout.astro (from plan 01 — add header/footer slots), src/components/layout/Nav.astro (Task 2), src/i18n/nl.ts, src/config.ts
  </read_first>
  <action>
    Create src/components/layout/Footer.astro implementing UI-SPEC `<SiteFooter>` (HOME-09): a `<footer>` with three columns on desktop (wordmark + short descriptor | nav links | legal/social). Nav links reuse the same honest-disabled treatment as the header (aria-disabled). LinkedIn icon = same disabled inline-SVG treatment (references TODO_LINKEDIN_URL). Legal: copyright 2026 Milan van der Meulen + disabled Privacy / Algemene voorwaarden placeholder links. Background --color-bg with a --color-border top divider (LIGHT footer — the dark band is reserved for the story section per D-01). Typography at label size. Strings from nl.footer; no invented content (HOME-11).
    Edit src/layouts/BaseLayout.astro to import and render Nav.astro immediately inside `<body>` (before the main landmark) and Footer.astro after the `<slot />`/main. This is the key link that puts the global chrome on every page. Keep the skip-link before Nav so keyboard users can bypass it.
  </action>
  <verify>
    <automated>npm run build; echo "exit:$?"; grep -c '<footer' dist/index.html; grep -c 'Milan van der Meulen' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - After build, `grep '<footer' dist/index.html` matches and `grep '© 2026' dist/index.html` (or the copyright string) matches.
    - `grep '<header' dist/index.html` and `grep '<footer' dist/index.html` both present (Nav + Footer wired via BaseLayout).
    - `grep 'href="#"' src/components/layout/Footer.astro` returns nothing.
    - `grep 'skip-link' dist/index.html` still present and precedes the header in source order.
  </acceptance_criteria>
  <done>The footer renders with wordmark, disabled nav links, disabled LinkedIn, and copyright/legal placeholders (light background); Nav + Footer appear on the page via BaseLayout on every route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist → visitor browser | Static header/footer HTML + tiny vanilla-JS island (mobile toggle, scroll class). No user input is submitted; external links (LinkedIn) are disabled placeholders in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering / Open Redirect | future external links (LinkedIn, Substack) opened in a new tab | medium | mitigate | All external destinations are disabled placeholders now; when wired (later phases) any target="_blank" MUST carry rel="noopener noreferrer" — record this as a contract in config.ts comment |
| T-01-05 | Spoofing | honest-CTA integrity | low | mitigate | Disabled CTAs use aria-disabled + no click handler, preventing fake "working" affordances that mislead users (HOME-10) |
| T-01-06 | Tampering | inline SVG (LinkedIn icon) | low | accept | Inline SVG is authored static markup, not user-supplied; no injection surface |
</threat_model>

<verification>
- `npm run build` exits 0; `npm run preview` shows header + footer on `/`.
- Header renders wordmark + 5 nav labels + Contact CTA + NL|EN + LinkedIn; all unknown-destination items are aria-disabled (>=6 disabled elements in dist/index.html).
- Mobile hamburger has aria-expanded/aria-controls and opens an accessible panel.
- Footer renders wordmark, disabled nav links, disabled LinkedIn, copyright/legal placeholders on a light background.
- No `href="#"`/`href=""` in Nav/Footer/Button; NL|EN is non-functional; yellow used only for CTA/active-underline/focus.
</verification>

<success_criteria>
The global chrome and reusable UI kit exist and compose from single-source content/config. A visitor sees a polished sticky header and footer; every future-destination CTA/nav item is honestly disabled (never a fake link); Button/Card/SectionWrapper/PlaceholderBadge are ready for plans 03-05 to consume.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-system-dutch-homepage/01-02-SUMMARY.md` when done.
</output>
