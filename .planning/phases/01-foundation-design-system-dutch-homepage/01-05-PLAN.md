---
phase: 01-foundation-design-system-dutch-homepage
plan: 05
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/components/sections/Newsletter.astro
  - src/components/sections/Articles.astro
autonomous: true
requirements: [FND-04, HOME-07, HOME-08, HOME-10, HOME-11]

estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A visitor sees a newsletter signup section with a fully-designed naam + mailadres form"
    - "The newsletter submit is a clearly-marked disabled placeholder (Substack URL pending) — never a fake success/thank-you state"
    - "A visitor sees a 'Recente artikelen' section with 3 designed article cards, each clearly flagged as a placeholder"
    - "Both sections are responsive and the form fields have explicit labels and 16px inputs (no iOS zoom)"
  artifacts:
    - path: "src/components/sections/Newsletter.astro"
      provides: "Newsletter signup: heading, copy, naam+mailadres labelled form, disabled Substack CTA"
      contains: "Mailadres"
    - path: "src/components/sections/Articles.astro"
      provides: "3 placeholder article cards with visible [Placeholder] badges + TODO comments"
      contains: "Recente artikelen"
  key_links:
    - from: "src/components/sections/Newsletter.astro"
      to: "src/config.ts"
      via: "disabled submit references TODO_SUBSTACK_URL (Phase 3 wires real Substack)"
      pattern: "TODO_SUBSTACK_URL"
    - from: "src/components/sections/Articles.astro"
      to: "src/components/ui/PlaceholderBadge.astro"
      via: "each card shows the visible placeholder marker"
      pattern: "PlaceholderBadge"
  prohibitions:
    - "No fake success/thank-you state on newsletter submit — disabled placeholder only (D-09, HOME-10)"
    - "No href=\"#\" or href=\"\" on the submit or 'Lees verder'/'Alle artikelen' links (HOME-10)"
    - "Article placeholder titles are clearly fictional placeholder copy — not asserted as real published articles (D-10)"
    - "No real Substack/network call in Phase 1 (D-09, out of scope)"
---

<objective>
Add the two lead-capture/nurture sections: the newsletter signup and the "Recente artikelen" placeholder grid.

Purpose: These are the newsletter-growth beats. Per D-09 the inline form is fully designed but its submit is an honest disabled placeholder until the Substack URL is supplied (Phase 3 wires the real integration); per D-10 the articles are 3 clearly-flagged placeholder cards until the live Substack feed lands (Phase 3). This plan owns distinct component files ONLY (no index edits) so it runs fully parallel with plans 03 and 04 in wave 3; the assembly plan (06) wires them into the homepage.

Output: Newsletter and Articles components — honest, responsive, accessible — ready for wiring.

Implements FND-04 (responsive), HOME-07 (newsletter per D-09), HOME-08 (articles per D-10), HOME-10 (honest CTAs), HOME-11 (no fabricated real-article claims).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-design-system-dutch-homepage/SKELETON.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-RESEARCH.md
@src/components/ui/Card.astro
@src/components/ui/Button.astro
@src/components/ui/SectionWrapper.astro
@src/components/ui/PlaceholderBadge.astro
@src/i18n/nl.ts
@src/config.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Newsletter signup section (honest disabled submit)</name>
  <files>src/components/sections/Newsletter.astro</files>
  <read_first>
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md §Component Inventory `<NewsletterSection>`, §Interaction Contracts (Form Input Focus States), §Accessibility Contract, §Copywriting Contract
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md D-09, D-12
    - src/components/ui/SectionWrapper.astro, src/components/ui/Button.astro, src/i18n/nl.ts, src/config.ts
  </read_first>
  <action>
    Create src/components/sections/Newsletter.astro per UI-SPEC `<NewsletterSection>` (HOME-07, D-09): a LIGHT section (SectionWrapper, --color-bg — not dark). Heading (nl.newsletter, preserve exact docx wording) + sub-copy body. An inline form with two fields side-by-side on desktop, stacked on mobile, each with an EXPLICIT `<label>` (a11y — not placeholder-only): "Naam" (input type text, autocomplete given-name, id nl-naam) and "Mailadres" (input type email, autocomplete email, id nl-email). Field styling per UI-SPEC: 1px --color-border, --radius-md, height 48px, --space-md horizontal padding, font-size 16px (prevents iOS zoom), focus ring outline 2px --color-accent with 2px offset. Submit "Schrijf je in" = a DISABLED placeholder (D-09): render via Button disabled (placeholderReason references TODO_SUBSTACK_URL, title "Link volgt zodra Substack-URL bekend is"). NO fake success/thank-you state, NO JS submit handler, NO network call, NO href hash. Browser-native `required` only. Add a code comment TODO noting Phase 3 replaces this with real Substack redirect (target _blank must then carry rel noopener noreferrer — see config.ts comment).
  </action>
  <verify>
    <automated>npm run build; echo "exit:$?"; grep -c 'for="nl-naam"\|for="nl-email"' src/components/sections/Newsletter.astro; grep -c 'TODO_SUBSTACK_URL' src/components/sections/Newsletter.astro</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - The form has two explicit labels: `grep 'for="nl-naam"' src/components/sections/Newsletter.astro` and `grep 'for="nl-email"' src/components/sections/Newsletter.astro` both match.
    - Email input uses type email + autocomplete + 16px font (`grep 'type="email"' src/components/sections/Newsletter.astro` matches).
    - Submit references the single-source Substack sentinel: `grep 'TODO_SUBSTACK_URL' src/components/sections/Newsletter.astro` matches; submit is aria-disabled.
    - No fake success/handler: `grep -iE 'thank|bedankt|success|onsubmit|addEventListener\(.submit' src/components/sections/Newsletter.astro` returns nothing; `grep 'href="#"' src/components/sections/Newsletter.astro` returns nothing.
  </acceptance_criteria>
  <done>A fully-designed, accessible naam+mailadres signup renders; the submit is an honest disabled placeholder tied to TODO_SUBSTACK_URL with no fake success state.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Recente artikelen — 3 placeholder cards clearly flagged</name>
  <files>src/components/sections/Articles.astro</files>
  <read_first>
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md §Component Inventory `<ArticlesSection>`, §Copywriting Contract, §UI Considerations (empty: articles = placeholder cards)
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md D-10, D-12
    - src/components/ui/Card.astro, src/components/ui/PlaceholderBadge.astro, src/components/ui/SectionWrapper.astro, src/i18n/nl.ts
  </read_first>
  <action>
    Create src/components/sections/Articles.astro per UI-SPEC `<ArticlesSection>` (HOME-08, D-10): a section (SectionWrapper) with heading "Recente artikelen" (nl.articles). Three placeholder article cards (Card.astro) in a row on desktop, 2-col/stacked on tablet/mobile. Each card: placeholder category eyebrow, a plausible fictional founder-topic title (e.g. "Hoe ik leerde delegeren zonder controle te verliezen", "Schalen in 9 markten: wat ik anders zou doen", "De vraag die ik mezelf elke maand stel als ondernemer" — clearly fictional placeholder copy, NOT asserted as real published pieces), a one-sentence excerpt, a meta row (date + "X min leestijd"), and a "Lees verder →" text link that is DISABLED/placeholder (no real article URL — aria-disabled, no href hash). Each card MUST carry a visible placeholder marker: a PlaceholderBadge pill in the corner AND a code comment `<!-- TODO: replace with real Substack article (Phase 3) -->` (D-10). An "Alle artikelen" link below the grid = disabled/placeholder (future newsletter/blog page). Data shape note (schema-gate): model each card as a small typed object (title, excerpt, category, date, readingTime, isPlaceholder true) so Phase 3 can swap the array source for a real Substack collection without restructuring markup — record this shape in a comment as the future content-collection contract.
  </action>
  <verify>
    <automated>npm run build; echo "exit:$?"; grep -c 'Recente artikelen' dist/index.html; grep -oc 'TODO: replace with real Substack' src/components/sections/Articles.astro</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (component compiles).
    - `grep 'Recente artikelen' src/components/sections/Articles.astro` matches.
    - Exactly 3 article cards render (verify markup/loop count in Articles.astro), each with a visible PlaceholderBadge.
    - Each card has the TODO placeholder comment: `grep -c 'TODO: replace with real Substack' src/components/sections/Articles.astro` returns 3 (or a single loop with one comment covering the placeholder array — acceptable if the array is marked isPlaceholder).
    - No real article link: `grep 'href="#"' src/components/sections/Articles.astro` returns nothing; "Lees verder" and "Alle artikelen" are aria-disabled.
  </acceptance_criteria>
  <done>Three clearly-flagged placeholder article cards render with visible badges and TODO markers, disabled "Lees verder"/"Alle artikelen" links, and a documented data shape for the Phase-3 Substack swap.</done>
</task>

</tasks>

> Wiring note: Newsletter and Articles are NOT rendered into src/pages/index.astro by this plan. The final assembly plan (01-06, wave 4) owns src/pages/index.astro and wires all sections in document order (… → Story → Newsletter → Articles). This keeps wave-3 plans free of any shared-file conflict on index.astro.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor → newsletter form | The only input surface on the site. In Phase 1 the form does NOT submit anywhere (disabled placeholder) — no data leaves the browser, no endpoint exists yet. |
| dist → visitor browser | Static article placeholders; disabled links only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Spoofing | fake newsletter success state | medium | mitigate | Submit is aria-disabled with no handler and no success UI (D-09) — the user is never falsely told they subscribed; real Substack integration is Phase 3 |
| T-01-04 | Tampering / Open Redirect | future Substack redirect in new tab | medium | mitigate | Not wired in Phase 1; config.ts comment records that Phase 3's target=_blank redirect MUST use rel="noopener noreferrer" |
| T-01-11 | Repudiation | placeholder articles read as real posts | low | mitigate | Visible [Placeholder] badge + fictional titles + TODO comments (D-10) prevent misrepresenting placeholder content as published work |
</threat_model>

<verification>
- `npm run build` exits 0 (both components compile).
- Newsletter: explicit labels for naam+mailadres, type=email, 16px inputs, focus ring; submit aria-disabled tied to TODO_SUBSTACK_URL; no fake success/handler/href-hash.
- Articles: heading + exactly 3 clearly-flagged placeholder cards with visible badges + TODO comments; disabled "Lees verder"/"Alle artikelen"; documented data shape.
- Final document order (Story → Newsletter → Articles) is asserted in plan 01-06 (assembly).
</verification>

<success_criteria>
A visitor sees a polished, accessible newsletter signup and a "Recente artikelen" grid — both fully honest: the form never fakes a subscription and the articles are unmistakably placeholders, ready for the Phase-3 Substack swap.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-system-dutch-homepage/01-05-SUMMARY.md` when done.
</output>
