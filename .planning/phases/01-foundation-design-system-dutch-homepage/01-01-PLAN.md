---
phase: 01-foundation-design-system-dutch-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - .gitignore
  - public/robots.txt
  - public/favicon.svg
  - public/og-image.jpg
  - public/fonts/Fraunces-subset.woff2
  - public/fonts/DMSans-subset.woff2
  - src/styles/reset.css
  - src/styles/tokens.css
  - src/styles/global.css
  - src/components/layout/BaseHead.astro
  - src/layouts/BaseLayout.astro
  - src/assets/photos/milan-hero-stage.jpg
  - src/components/sections/Hero.astro
  - src/pages/index.astro
autonomous: true
requirements: [FND-01, FND-02, FND-05, FND-07, FND-09, FND-10, HOME-02]
user_setup:
  - service: naste-font
    why: "Preferred brand typeface Naste is not yet supplied — interim Fraunces+DM Sans self-hosted now; swap via --font-display later"
    dashboard_config:
      - task: "When Naste WOFF2 files arrive, drop into public/fonts/ and update --font-display in tokens.css (one line)"
        location: "src/styles/tokens.css"
  - service: photo-originals
    why: "The 6 source photos live in fotos/*.zip (~86 MB, gitignored). The executor must extract 10 Jaar Soly-77.jpg locally into src/assets/photos/ before build."
    dashboard_config:
      - task: "Extract 10 Jaar Soly-77.jpg from fotos/wetransfer_10-jaar-soly-77-jpg_2026-08-18_1205.zip, copy+rename to src/assets/photos/milan-hero-stage.jpg (originals stay gitignored)"
        location: "fotos/ → src/assets/photos/"

estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A visitor loading / sees the split-card Hero with H1 'Je bedrijf schalen, zonder jezelf te verliezen', subtext, two CTAs and the USP row"
    - "The hero photo renders as an optimized AVIF/WebP derivative, eager-loaded with fetchpriority high (LCP image)"
    - "npm run build exits 0 and produces dist/index.html"
    - "dist/ contains no full-res original jpg (>300KB) and no committed originals"
    - "Built HTML head contains title, canonical, og:title and a JSON-LD Person block"
    - "Design tokens (--color-*, --font-display, --font-body, --text-*, --space-*) are defined at :root and the interim display+body fonts load self-hosted"
    - "astro.config.mjs sets site, output static, i18n defaultLocale nl with no /nl/ route prefix, and the sitemap integration"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Astro config: site, static output, i18n (nl default, no prefix), sitemap integration, sharp image service"
      contains: "defaultLocale"
    - path: "src/styles/tokens.css"
      provides: "All design tokens as CSS custom properties + @font-face for interim fonts"
      contains: "--font-display"
    - path: "src/components/layout/BaseHead.astro"
      provides: "SEO head: title, description, canonical, OG, Twitter, JSON-LD Person, font preloads"
      contains: "application/ld+json"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Page shell importing tokens/global CSS, BaseHead, html lang=nl, main landmark, slot"
      contains: "lang=\"nl\""
    - path: "src/components/sections/Hero.astro"
      provides: "Split-card hero with Picture (priority), H1, subtext, CTA row, USP row"
      contains: "priority"
    - path: "src/pages/index.astro"
      provides: "NL homepage route at / rendering BaseLayout + Hero"
      min_lines: 5
    - path: ".gitignore"
      provides: "Excludes fotos/, assets/originals/, dist/, node_modules, *.zip"
      contains: "assets/originals"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "imports and wraps content in BaseLayout"
      pattern: "BaseLayout"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/tokens.css"
      via: "global CSS import so tokens apply to all components"
      pattern: "tokens.css"
    - from: "src/components/sections/Hero.astro"
      to: "src/assets/photos/milan-hero-stage.jpg"
      via: "import + astro:assets Picture generates AVIF/WebP derivatives"
      pattern: "milan-hero-stage"
  prohibitions:
    - "No full-res original jpg (>300KB) or *.zip committed to git or emitted into dist/"
    - "No href=\"#\" or href=\"\" placeholder links — disabled CTAs use aria-disabled (Button component arrives in plan 02; hero CTAs here may anchor to on-page sections or render disabled)"
    - "Yellow #FFDD11 is not used as a section/background fill (only the hero photo's own backdrop + at most the primary CTA)"
    - "No fonts.googleapis.com <link> in production — fonts are self-hosted WOFF2"
    - "No /nl/ prefix on any Phase-1 route"
---

<objective>
Scaffold the Astro 7 foundation and prove the entire Phase-1 architecture end-to-end with one production-quality vertical tracer slice: config + design tokens + self-hosted fonts + SEO head + BaseLayout + the optimized image pipeline + a real `/` route rendering a real Hero section that consumes the real optimized hero photo.

Purpose: This is the Walking Skeleton (see SKELETON.md). Proving the full stack (config → layout → tokens → image pipeline → route → section) on the executor's best early-context tokens catches any architectural dead-end after one commit instead of after every section is built. Every later plan is a horizontal expansion into this proven skeleton.

Output: A buildable Astro project whose `/` renders the split-card Hero with an optimized AVIF/WebP hero image, correct SEO head, and the design-system tokens applied — `npm run build` exits 0.

Implements FND-01 (scaffold), FND-02 (design tokens + swappable font var per D-13), FND-05 (baseline SEO), FND-07 (perf: self-hosted fonts, priority LCP), FND-09 (i18n-ready per D-16), FND-10 (image pipeline per D-05), HOME-02 (hero per D-04/D-05).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ASSETS.md
@.planning/phases/01-foundation-design-system-dutch-homepage/SKELETON.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-RESEARCH.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Scaffold Astro project + config + gitignore (environment + build tooling)</name>
  <files>package.json, astro.config.mjs, tsconfig.json, .gitignore, public/robots.txt, public/favicon.svg, public/og-image.jpg</files>
  <read_first>
    - .gitignore (already excludes fotos/, assets/originals/, dist/, node_modules, *.zip — preserve these)
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-RESEARCH.md §Standard Stack, §Pattern 3 (i18n config), §Environment Availability, §Wave 0 Gaps
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md D-16 (i18n-ready)
  </read_first>
  <precondition>Node.js >= 22.12.0 is available (`node --version`). Astro 7 will not install on older Node — halt and report if unmet (RESEARCH §Environment Availability, assumption A5).</precondition>
  <action>
    Scaffold Astro 7 into the repo root (the repo already exists; init in place). Install pinned deps per RESEARCH §Standard Stack: astro@7.2.3, sharp@0.35.3, @astrojs/sitemap@3.7.3, @astrojs/check@0.9.10, typescript (strict). Do NOT add Tailwind or any utility framework (D-14).
    Create astro.config.mjs per RESEARCH §Pattern 3: set `site: 'https://milanvandermeulen.nl'` (record as ASSUMPTION per RESEARCH Open Q1 — add a TODO comment that the production domain must be confirmed before deploy), `output: 'static'`, `trailingSlash: 'never'`, `i18n: { defaultLocale: 'nl', locales: ['nl'] }` (leave `prefixDefaultLocale` at its default so NL stays at root — add a commented block showing the Phase-4 EN switch), `integrations: [sitemap({ i18n: { defaultLocale: 'nl', locales: { nl: 'nl-NL' } } })]`, and the sharp image service entrypoint. Set tsconfig to Astro strict + @astrojs/check.
    Confirm `.gitignore` already excludes fotos/, assets/originals/, dist/, node_modules, *.zip (it does — do not remove those lines; add node_modules/ and .astro/ only if missing).
    Add public/robots.txt (allow all + Sitemap: line pointing at the site sitemap-index.xml), a placeholder public/favicon.svg (simple "MvdM" or generic mark — clearly interim), and a placeholder 1200x630 public/og-image.jpg (may be a derivative of the hero or a solid brand-colour placeholder, clearly interim).
    Set npm scripts: dev, build (`astro check && astro build`), preview.
  </action>
  <verify>
    <automated>node --version && npm install && npm run build; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node --version` >= 22.12.0 (halt if not).
    - `npm run build` exits 0 (a bare index will exist once Task 4 lands; if run before Task 4, at least `astro check` passes and config loads).
    - `grep 'defaultLocale' astro.config.mjs` returns `defaultLocale: 'nl'`; no `prefixDefaultLocale: true` active.
    - `grep -n 'site:' astro.config.mjs` shows the site URL with a TODO/ASSUMPTION comment nearby.
    - `.gitignore` still contains `fotos/`, `assets/originals/`, `dist/`, `*.zip`.
    - `public/robots.txt` exists and contains a `Sitemap:` line.
  </acceptance_criteria>
  <done>Astro 7 scaffolds and its config loads; i18n is NL-default with no route prefix; sitemap+robots+favicon+og placeholders exist; originals stay gitignored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Design tokens, CSS reset, global styles + self-hosted interim fonts</name>
  <files>src/styles/reset.css, src/styles/tokens.css, src/styles/global.css, public/fonts/Fraunces-subset.woff2, public/fonts/DMSans-subset.woff2</files>
  <read_first>
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-RESEARCH.md §Pattern 1 (tokens), §Pattern 6 (@font-face), §Fonts, §Animations (prefers-reduced-motion global rule)
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md §Spacing Scale, §Typography (Type Scale), §Color (Palette + 60/30/10), §Responsive Breakpoints
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md D-01, D-02, D-03, D-13, D-15
  </read_first>
  <action>
    Create src/styles/tokens.css defining ALL design tokens as CSS custom properties at `:root`, using the exact values from UI-SPEC §Color (palette table: `--color-bg` #FAF8F4, `--color-surface` #F0EDE7, `--color-text` #1A1A18, `--color-text-secondary` #5A5A52, `--color-border` #E2DDD6, `--color-accent` #FFDD11, `--color-accent-dark` #C9AC00, `--color-dark-band` #111110, `--color-dark-text` #F5F2EC, `--color-dark-surface` #222220, `--color-destructive` #C0392B), UI-SPEC §Typography type scale (fluid clamp() ramp per RESEARCH §Pattern 1: `--text-xs`..`--text-5xl`) and UI-SPEC §Spacing scale (map the SPEC's `--space-xs`..`--space-5xl` plus `--space-press-strip-y: 20px` and `--space-usps-gap: 12px`), plus radii (`--radius-card: 20px` etc.), `--shadow-card`, motion tokens (`--duration-*`, `--ease-out`), `--max-width: 1280px`, `--content-width` and `--section-padding-y`.
    Define the swappable brand-font variables (D-13): `--font-display: 'Fraunces', Georgia, 'Times New Roman', serif` and `--font-body: 'DM Sans', system-ui, -apple-system, sans-serif`. Add a clearly-marked comment showing the single-line Naste swap (per RESEARCH §Fonts).
    Add `@font-face` blocks for the two interim fonts self-hosted from public/fonts/ WOFF2 with `font-display: swap` and a Latin+Latin-1 `unicode-range` (RESEARCH §Pattern 6). Download the Fraunces + DM Sans WOFF2 files (Google Fonts, OFL) into public/fonts/; subsetting is optional. Record the [ASSUMED] OFL licensing note (RESEARCH A1/A2) in a comment.
    Create src/styles/reset.css: minimal modern reset (box-sizing border-box, margin 0, `img,picture { max-width:100%; display:block }`, `:focus-visible` outline using `--color-accent`) PLUS the global `@media (prefers-reduced-motion: reduce)` rule that neutralises animation/transition durations (RESEARCH §Animations — this satisfies FND-08's reduced-motion baseline site-wide).
    Create src/styles/global.css importing reset + tokens, setting body base (`background: var(--color-bg)`, `color: var(--color-text)`, `font-family: var(--font-body)`), heading font (`var(--font-display)`), body text measure (max reading width 680px utility), and a `.skip-link` visually-hidden-until-focus style (a11y, wired in BaseLayout).
  </action>
  <verify>
    <automated>grep -c -- '--font-display\|--color-accent\|--space-lg\|--text-4xl' src/styles/tokens.css; grep -c 'prefers-reduced-motion' src/styles/reset.css; ls public/fonts/*.woff2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -- '--font-display' src/styles/tokens.css` matches; `grep -- '--font-body' src/styles/tokens.css` matches.
    - `grep -- '--color-accent' src/styles/tokens.css` returns `#FFDD11`.
    - tokens.css contains the full spacing ramp (`--space-xs`..`--space-5xl`) and type ramp (`--text-xs`..`--text-5xl`).
    - `grep 'prefers-reduced-motion' src/styles/reset.css` matches (global reduced-motion rule present).
    - `ls public/fonts/` lists two `.woff2` files.
    - `grep -c 'fonts.googleapis.com' src/styles/*.css` returns 0 (no CDN font links).
  </acceptance_criteria>
  <done>All design tokens exist at :root with the exact SPEC values; interim display+body fonts self-host via @font-face; reduced-motion global rule present; font swap is a single documented variable change.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: BaseHead (SEO) + BaseLayout page shell</name>
  <files>src/components/layout/BaseHead.astro, src/layouts/BaseLayout.astro</files>
  <read_first>
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-RESEARCH.md §Pattern 4 (BaseHead), §SEO / Metadata Checklist, §Accessibility Baseline
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md §SEO / Metadata Contract, §Accessibility Contract
    - src/styles/global.css (from Task 2 — imported here)
  </read_first>
  <action>
    Create src/components/layout/BaseHead.astro per RESEARCH §Pattern 4 with a typed `Props { title: string; description: string; ogImage?: string }`. Emit: charset, viewport, `<title>`, meta description, canonical (`new URL(Astro.url.pathname, Astro.site)`), Open Graph (og:type website, og:url, og:title, og:description, og:image resolved against Astro.site, og:locale `nl_NL`), Twitter `summary_large_image`, a JSON-LD Person block (name "Milan van der Meulen", jobTitle "Ondernemer, coach en spreker", url = Astro.site, `sameAs: []` with a TODO comment for the LinkedIn URL per D-12), favicon link, and `<link rel="preload" as="font" type="font/woff2" crossorigin>` for both WOFF2 files (FND-07 / RESEARCH pitfall 7).
    Create src/layouts/BaseLayout.astro: `<html lang="nl">` (D-17/a11y), import src/styles/global.css globally (this is the key link that applies tokens to every component), render `<head>` via BaseHead (pass through title/description/ogImage props with sensible NL defaults from UI-SPEC §SEO: title "Milan van der Meulen — Founder Coach & Spreker"), a visually-hidden `.skip-link` to `#main-content`, then `<slot />` wrapped so pages can compose header/sections/footer. Provide a `<main id="main-content">` landmark region (the slot content or a wrapper) so every page has one main landmark. Nav/Footer are added in plan 02 — leave clearly-marked slots/comments for them, do NOT stub fake nav here.
  </action>
  <verify>
    <automated>npm run build && grep -c 'application/ld+json\|og:title\|rel="canonical"' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - After build, `grep 'application/ld+json' dist/index.html` matches (Person schema present).
    - `grep 'og:title' dist/index.html` and `grep 'rel="canonical"' dist/index.html` match.
    - `grep 'lang="nl"' dist/index.html` matches.
    - `grep 'rel="preload"' dist/index.html` matches for the two WOFF2 fonts.
    - `grep 'id="main-content"' dist/index.html` matches (main landmark present).
    - No `href="#"` or `href=""` introduced by this task (`grep -rn 'href="#"\|href=""' src/components/layout/BaseHead.astro src/layouts/BaseLayout.astro` returns nothing).
  </acceptance_criteria>
  <done>BaseHead emits full SEO metadata + JSON-LD + font preloads; BaseLayout provides html[lang=nl], global token CSS, skip-link and a main landmark; head verified in built HTML.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Hero section (LCP image pipeline) + index route — end-to-end tracer close</name>
  <files>src/assets/photos/milan-hero-stage.jpg, src/components/sections/Hero.astro, src/pages/index.astro</files>
  <read_first>
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-RESEARCH.md §Pattern 2 (LCP Hero Picture), §Image Pipeline Details (widths/sizes table), §Common Pitfalls 1 & 2
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md §Component Inventory `<HeroSection>`, §Image Pipeline Contract, §Copywriting Contract
    - .planning/ASSETS.md §Source inventory + §Section→source mapping (hero = 10 Jaar Soly-77.jpg)
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md D-04, D-05
  </read_first>
  <precondition>src/assets/photos/milan-hero-stage.jpg exists — extracted from fotos/wetransfer_10-jaar-soly-77-jpg_2026-08-18_1205.zip (source `10 Jaar Soly-77.jpg`, 6588x4392). The zip is gitignored; the derived src/assets copy is what Astro optimises. Halt if the file is absent.</precondition>
  <action>
    Extract `10 Jaar Soly-77.jpg` from the gitignored zip and place it at src/assets/photos/milan-hero-stage.jpg (importing from src/assets/ — NOT public/ — is what triggers Astro's build optimization; RESEARCH pitfall 1).
    Create src/components/sections/Hero.astro implementing the split-card layout (D-04, UI-SPEC `<HeroSection>`): left column = optional eyebrow, H1 (the single page `<h1>`) "Je bedrijf schalen, zonder jezelf te verliezen" in `var(--font-display)`, subtext body, a CTA row (primary "Contact" + secondary "Mijn verhaal"), and the USP row "12+ jaar · 180 medewerkers · 9 markten" (`--space-usps-gap`). For the CTAs in this tracer: render the secondary "Mijn verhaal" as an in-page anchor to `#verhaal` (the story section arrives in plan 04 — this is a legitimate on-page anchor per D-12) and render the primary "Contact" as a disabled affordance (real Button component arrives in plan 02; here use `<span role="button" aria-disabled="true">` with `title="Binnenkort beschikbaar"` — do NOT use `href="#"`). Right column = `<Picture>` from astro:assets importing milan-hero-stage.jpg, `formats={['avif','webp']}`, `fallbackFormat="jpeg"`, `widths={[400,800,1200,1600]}`, `sizes="(max-width: 768px) 100vw, 50vw"`, `priority` (sets loading=eager + fetchpriority=high — the LCP requirement, D-05/HOME-02), rounded card via `--radius-card`, `object-fit: cover` with `object-position: center top`, and the Dutch alt text from UI-SPEC ("Milan van der Meulen spreekt op het podium met geel en blauw decor."). Scoped `<style>` uses tokens only; stacks text-then-image on mobile (RESEARCH Open Q3 recommendation: text first for LCP).
    Create src/pages/index.astro: import BaseLayout and Hero, render `<BaseLayout title="Milan van der Meulen — Founder Coach & Spreker" description="{NL description from UI-SPEC, <=160 chars}">` wrapping `<Hero />`. This closes the tracer: config → layout → tokens → image pipeline → route → section, end-to-end.
  </action>
  <verify>
    <automated>npm run build && grep -o 'loading="eager"' dist/index.html && grep -o 'fetchpriority="high"' dist/index.html && ls dist/_astro/ | grep -c '\.avif$' && ! ls dist/_astro/ | grep -iE 'milan-hero.*\.jpg$'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep 'loading="eager"' dist/index.html` matches (hero eager) and `grep 'fetchpriority="high"' dist/index.html` matches.
    - `ls dist/_astro/ | grep '.avif'` returns at least one AVIF derivative (image pipeline works — FND-10).
    - No `milan-hero*.jpg` larger than ~300KB is emitted into dist/ (originals not served — FND-10 prohibition).
    - `grep -c '<h1' dist/index.html` returns exactly 1 (single page h1 — a11y).
    - `grep 'Je bedrijf schalen, zonder jezelf te verliezen' dist/index.html` matches (supplied copy intact — HOME-11).
    - `grep -rn 'href="#"\|href=""' src/components/sections/Hero.astro src/pages/index.astro` returns nothing (honest CTA — HOME-10).
  </acceptance_criteria>
  <done>`/` renders the split-card Hero with the H1, subtext, CTAs, USP row and the optimized AVIF/WebP hero image (eager, high priority); `npm run build` exits 0; no original jpg served. The Walking Skeleton is proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build toolchain → dist | npm packages processed at build time; sharp processes local images. Supply-chain is the only meaningful surface (static, no backend, no user input). |
| dist → visitor browser | Static HTML/CSS/optimized images served read-only; no forms submit anywhere in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | npm dependency install (astro, sharp, @astrojs/*) | high | mitigate | Pin exact versions per RESEARCH §Standard Stack; all packages vetted in RESEARCH §Package Legitimacy Audit (all OK/approved false-positive SUS, no SLOP) — no [ASSUMED]/[SUS] blocking human checkpoint needed |
| T-01-02 | Information Disclosure | secrets / originals in repo | high | mitigate | No secrets in Phase 1 (no backend/keys); `.gitignore` excludes fotos/, assets/originals/, *.zip so ~86 MB originals and any future secret files never commit |
| T-01-03 | Information Disclosure | production domain in `site` config | low | accept | `milanvandermeulen.nl` is an ASSUMPTION (RESEARCH A6); wrong value only affects canonical/sitemap URLs, corrected before deploy (TODO in config) |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | package-legitimacy gate satisfied by RESEARCH §Package Legitimacy Audit; no [ASSUMED]/[SUS] packages require a blocking human checkpoint (astro/@astrojs/check SUS verdicts are confirmed false positives) |
</threat_model>

<verification>
- `node --version` >= 22.12.0.
- `npm run build` exits 0; `npm run preview` serves `/` locally end-to-end.
- Built `dist/index.html` head contains title, canonical, og:title, JSON-LD Person, font preloads; `<html lang="nl">`; exactly one `<h1>`.
- `dist/_astro/` contains AVIF/WebP derivatives; no `milan-hero*.jpg` >300KB in dist/.
- No `href="#"`/`href=""` in Hero/BaseLayout/BaseHead/index; no `fonts.googleapis.com` link; no `/nl/` route.
</verification>

<success_criteria>
The Walking Skeleton is proven: a visitor loading `/` sees the split-card Hero (H1, subtext, CTAs, USP row) with the real hero photo served as optimized AVIF/WebP (eager, high priority), correct SEO head, and design tokens applied — all via the intended architecture (config → BaseLayout → tokens → astro:assets → index → Hero). `npm run build` exits 0 and originals are never committed/served.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-system-dutch-homepage/01-01-SUMMARY.md` when done.
</output>
