---
phase: 01-foundation-design-system-dutch-homepage
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/assets/photos/milan-speaking-graph.jpg
  - src/assets/photos/milan-networking.jpg
  - src/components/sections/PressStrip.astro
  - src/components/sections/Samenwerken.astro
autonomous: true
requirements: [FND-04, HOME-03, HOME-04, HOME-10, HOME-11]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A visitor sees a slim 'Bekend van' press strip directly under the hero with 6 clearly-marked placeholder logo chips (MT Sprout, Quote, NOS, EenVandaag, Telegraaf, De Ondernemer)"
    - "A visitor sees the 'Samenwerken' section as a 3-card commitment ladder: Nieuwsbrief, Presentatie/lezing, 1:1 Coaching, each with its own CTA"
    - "Each Samenwerken card's CTA is honestly disabled (Substack/contact destinations unknown), never a fake working button"
    - "Both new sections are responsive across desktop, tablet and mobile and stack without overflow"
  artifacts:
    - path: "src/components/sections/PressStrip.astro"
      provides: "Slim credibility band with 6 placeholder logo chips + [Placeholder] marking"
      contains: "Bekend van"
    - path: "src/components/sections/Samenwerken.astro"
      provides: "3-card services ladder with eyebrows, titles, descriptions, disabled CTAs"
      contains: "Samenwerken"
  key_links:
    - from: "src/components/sections/Samenwerken.astro"
      to: "src/components/ui/Button.astro"
      via: "each card uses disabled Button with placeholderReason"
      pattern: "Button"
    - from: "src/components/sections/Samenwerken.astro"
      to: "src/i18n/nl.ts"
      via: "card copy + CTA labels from nl.samenwerken"
      pattern: "nl\\.samenwerken"
  prohibitions:
    - "No real outlet logo images — press chips are text/wordmark placeholders only (D-11)"
    - "No href=\"#\" or fake success state on any Samenwerken CTA (HOME-10)"
    - "No fabricated stats or client counts in either section (D-08 honesty rule)"
    - "Yellow #FFDD11 not used as a card or press-strip background fill (D-02)"
    - "No original jpg (>300KB) emitted to dist/ from the optional card photos (FND-10)"
---

<objective>
Expand the proven skeleton with the two credibility/offer sections directly below the hero: the slim "Bekend van" press strip and the "Samenwerken" 3-card commitment ladder.

Purpose: These are the first credibility + conversion beats a visitor reads after the hero. They must compose from the plan-02 UI kit (Card, Button, PlaceholderBadge, SectionWrapper) and honor the honesty rules (placeholder logos, disabled CTAs). This plan owns distinct section-component files ONLY (it does NOT edit src/pages/index.astro) so it runs fully parallel with plans 04 and 05 in wave 3; the final assembly plan (06, wave 4) wires all sections into the homepage in document order.

Output: PressStrip and Samenwerken components, responsive and honest, ready for wiring.

Implements FND-04 (responsive layouts), HOME-03 (press strip per D-06/D-11), HOME-04 (Samenwerken per D-07), HOME-10 (honest CTAs), HOME-11 (preserve copy).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ASSETS.md
@.planning/phases/01-foundation-design-system-dutch-homepage/SKELETON.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-RESEARCH.md
@src/components/ui/Card.astro
@src/components/ui/Button.astro
@src/components/ui/SectionWrapper.astro
@src/components/ui/PlaceholderBadge.astro
@src/i18n/nl.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PressStrip credibility band (placeholder logo chips)</name>
  <files>src/components/sections/PressStrip.astro</files>
  <read_first>
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md §Component Inventory `<PressStrip>`, §UI Considerations (overflow: press strip chips)
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md D-06, D-11
    - src/components/ui/PlaceholderBadge.astro, src/components/ui/SectionWrapper.astro, src/i18n/nl.ts
  </read_first>
  <action>
    Create src/components/sections/PressStrip.astro per UI-SPEC `<PressStrip>` (HOME-03): a slim band using SectionWrapper (surface/subtle tint variant, NOT a dark band) with `--space-press-strip-y` (20px) vertical padding, sitting directly under the hero. Content: eyebrow label from nl.pressStrip ("Bekend van") + a flex row of 6 logo CHIPS for MT Sprout, Quote, NOS, EenVandaag, Telegraaf, De Ondernemer. Each chip = wordmark TEXT (not an image) in --color-text-secondary ~14px/600, inside a ~36px pill with --color-border border + --color-surface fill, styled greyscale/placeholder (title="Logo volgt") — clearly not final logos (D-11). Include the PlaceholderBadge or a visible marker so it reads as placeholder, plus a code comment TODO to swap real logos when supplied.
    Mobile: horizontal scroll (overflow-x auto; scroll-snap) so the 6 chips never clip or overflow the page on a 375px viewport (UI-SPEC §UI Considerations overflow backstop).
  </action>
  <verify>
    <automated>npm run build; echo "exit:$?"; grep -c 'Bekend van' dist/index.html; grep -oi 'MT Sprout\|Quote\|NOS\|EenVandaag\|Telegraaf\|De Ondernemer' dist/index.html | sort -u | grep -v '^#' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep 'Bekend van' dist/index.html` matches.
    - All 6 outlet wordmarks appear in dist/index.html (MT Sprout, Quote, NOS, EenVandaag, Telegraaf, De Ondernemer).
    - The strip contains a visible placeholder marker (PlaceholderBadge text or title="Logo volgt") — `grep -i 'placeholder\|Logo volgt' dist/index.html` matches.
    - `grep '<img' src/components/sections/PressStrip.astro` returns nothing (chips are text, not real logo images — D-11).
  </acceptance_criteria>
  <done>The press strip renders directly under the hero with 6 clearly-marked text placeholder chips and horizontal-scroll behavior on mobile; no real logo images, no overflow.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Samenwerken 3-card commitment ladder with disabled CTAs + optional card photos</name>
  <files>src/assets/photos/milan-speaking-graph.jpg, src/assets/photos/milan-networking.jpg, src/components/sections/Samenwerken.astro</files>
  <read_first>
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md §Component Inventory `<SamenwerkSection>`, §Copywriting Contract, §Image Pipeline Contract
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md D-07, D-09, D-12
    - .planning/ASSETS.md §Section→source mapping (Presentatie card = 10 Jaar Soly-78 → milan-speaking-graph; coaching = _AVM2348 → milan-networking)
    - src/components/ui/Card.astro, src/components/ui/Button.astro, src/i18n/nl.ts, src/config.ts
  </read_first>
  <precondition>If card photos are used: src/assets/photos/milan-speaking-graph.jpg (from 10 Jaar Soly-78.jpg) and src/assets/photos/milan-networking.jpg (from _AVM2348.jpg) exist, extracted from the gitignored fotos zip. Photos are OPTIONAL per ASSETS.md — if not extracted, render the cards without photos (do not block).</precondition>
  <action>
    Create src/components/sections/Samenwerken.astro per UI-SPEC `<SamenwerkSection>` (HOME-04, D-07): a light section (SectionWrapper) with heading "Samenwerken" (nl.samenwerken.heading) and three EQUAL-HEIGHT cards (Card.astro) in a row on desktop, stacked on mobile — ordered as the commitment ladder: (1) Nieuwsbrief (gratis), (2) Presentatie / Lezing, (3) 1:1 Coaching. Each card: numbered Dutch eyebrow ("01 — Begin hier gratis" / "02 — Ga dieper" / "03 — Werk met mij" from nl.samenwerken — adapt the ladder concept, Dutch, NOT the English reference wording), a Subheading title, a Body description, and its CTA:
    - Nieuwsbrief CTA "Schrijf je gratis in" → disabled Button, placeholderReason references TODO_SUBSTACK_URL (Phase 3).
    - Presentatie/Lezing CTA "Boek lezing" → disabled Button, placeholderReason references TODO_CONTACT_URL (Phase 2).
    - 1:1 Coaching CTA "Plan kennismaking" → disabled Button, placeholderReason references TODO_CONTACT_URL (Phase 2).
    All CTAs use the honest disabled pattern (aria-disabled, no href hash, no fake success). OPTIONAL photos (ASSETS.md): if extracted, use astro:assets `<Picture>` (formats avif/webp, lazy, widths per RESEARCH §Responsive Widths: speaking-graph and networking both [400,600,800], sizes per UI-SPEC) — milan-speaking-graph on the Lezing card, milan-networking on the Coaching card — only if they enhance without overcrowding; otherwise omit. Scoped token-styled CSS; no fabricated stats (D-08).
  </action>
  <verify>
    <automated>npm run build; echo "exit:$?"; grep -c 'Samenwerken' dist/index.html; grep -oi 'Schrijf je gratis in\|Boek lezing\|Plan kennismaking' dist/index.html | sort -u | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep 'Samenwerken' dist/index.html` matches.
    - The three CTA labels all appear: `grep -o 'Schrijf je gratis in' dist/index.html`, `grep -o 'Boek lezing' dist/index.html`, `grep -o 'Plan kennismaking' dist/index.html` each match.
    - Cards appear in ladder order Nieuwsbrief → Presentatie/Lezing → 1:1 Coaching (verify source order in Samenwerken.astro).
    - `grep 'href="#"' src/components/sections/Samenwerken.astro` returns nothing; the three CTAs carry aria-disabled.
    - If card photos used: `ls dist/_astro/ | grep -E 'milan-speaking-graph|milan-networking'` shows avif/webp derivatives and no >300KB jpg for them.
  </acceptance_criteria>
  <done>Samenwerken renders 3 equal-height ladder cards in the correct order, each with Dutch eyebrow/title/description and its honestly-disabled CTA; optional card photos, if present, are optimized; no fabricated stats.</done>
</task>

</tasks>

> Wiring note: PressStrip and Samenwerken are NOT rendered into src/pages/index.astro by this plan. The final assembly plan (01-06, wave 4) owns src/pages/index.astro and imports/renders all sections in document order (Hero → PressStrip → Samenwerken → Testimonials → Story → Newsletter → Articles). This keeps wave-3 plans free of any shared-file conflict on index.astro. Verify these components render standalone via `npm run build` (they compile) even before wiring.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist → visitor browser | Static credibility/offer sections; disabled CTAs only, no form/network in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Spoofing | placeholder press logos implying endorsements | medium | mitigate | Chips are clearly-marked text placeholders (D-11) with a visible [Placeholder]/"Logo volgt" marker — no real outlet logo asserts a false endorsement; swap only when real permission/asset supplied |
| T-01-08 | Repudiation | fabricated stats / social proof | low | mitigate | No "trusted by N" numbers (D-08); supplied copy preserved verbatim (HOME-11) |
| T-01-05 | Spoofing | disabled Samenwerken CTAs | low | mitigate | aria-disabled + no click handler — honest non-functional affordances (HOME-10) |
</threat_model>

<verification>
- `npm run build` exits 0 (both components compile).
- Press strip: "Bekend van" + all 6 outlet wordmarks as placeholder text chips with visible marker; horizontal scroll on mobile; no `<img>` logos.
- Samenwerken: 3 equal-height ladder cards in order with the 3 disabled CTAs; no href hash; no fabricated stats.
- Any optional card photos serve as AVIF/WebP; no original jpg >300KB in dist/.
- Final Hero → PressStrip → Samenwerken document order is asserted in plan 01-06 (assembly).
</verification>

<success_criteria>
Directly below the hero, a visitor sees an honest credibility band (placeholder outlet chips) and a clear 3-step commitment ladder with disabled-but-clearly-labelled CTAs — responsive and composed from the plan-02 UI kit.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-system-dutch-homepage/01-03-SUMMARY.md` when done.
</output>
