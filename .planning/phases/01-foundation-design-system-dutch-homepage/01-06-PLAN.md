---
phase: 01-foundation-design-system-dutch-homepage
plan: 06
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05]
files_modified:
  - src/pages/index.astro
autonomous: false
requirements: [FND-04, FND-06, FND-07, FND-08, HOME-10, HOME-11]

estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The homepage renders all docx sections in order: Header → Hero → PressStrip → Samenwerken → Testimonials → Story → Newsletter → Articles → Footer"
    - "The hero's secondary 'Mijn verhaal' CTA scrolls to the story section (#verhaal)"
    - "The full page passes a baseline a11y check (one h1, landmarks, alt text, focus states, labelled form) and a strong Lighthouse/CWV run"
    - "No CTA or link goes nowhere: every unknown-destination CTA is disabled; no href=# or href= empty exists anywhere in dist/"
    - "Supplied factual claims, names and quotes are preserved; any suspected inconsistency is flagged, not silently changed"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Full homepage assembly: all sections wired in document order under BaseLayout"
      contains: "Newsletter"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/sections/Newsletter.astro"
      via: "imports and renders every section component in the correct order"
      pattern: "Newsletter"
    - from: "src/components/sections/Hero.astro"
      to: "src/components/sections/Story.astro"
      via: "hero secondary CTA anchors to #verhaal which is the Story section id"
      pattern: "#verhaal"
  prohibitions:
    - "No href=\"#\" or href=\"\" anywhere in dist/ — verified by grep across the built HTML (HOME-10)"
    - "No original jpg (>300KB) or *.zip anywhere in dist/ or git (FND-10)"
    - "No fabricated stats/claims added during assembly; docx facts/names unchanged without flagging (HOME-11)"
    - "Yellow #FFDD11 must not exceed the 5 reserved uses site-wide (hero backdrop, primary CTA, active-nav underline, one story accent, focus ring) — no section/background fills (D-02)"
    - "No /nl/ route prefix introduced"
---

<objective>
Assemble the full homepage — wire every section component into src/pages/index.astro in document order — and run the final baseline verification for accessibility, SEO/metadata, performance/CWV, honest-CTA integrity, and content integrity. Ends with a human-verify checkpoint (the responsive/visual quality bar cannot be fully automated).

Purpose: Plans 01-05 produced sections in isolation. This plan is the single owner of index.astro (avoiding shared-file conflicts in wave 3) and the phase's quality gate: it proves the assembled page meets ROADMAP success criteria 1-5 before the phase is called done.

Output: A complete, responsive Dutch homepage rendering all docx sections, verified for a11y/SEO/perf/honesty, with the human confirming visual quality across breakpoints.

Implements FND-04 (responsive across breakpoints), FND-06 (a11y baseline), FND-07 (perf/CWV baseline), FND-08 (animation restraint), HOME-10 (honest CTAs sitewide), HOME-11 (content integrity). Confirms all prior FND/HOME requirements integrate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-design-system-dutch-homepage/SKELETON.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-CONTEXT.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md
@.planning/phases/01-foundation-design-system-dutch-homepage/01-RESEARCH.md
@src/pages/index.astro
@src/layouts/BaseLayout.astro
@src/components/sections/Hero.astro
@src/components/sections/PressStrip.astro
@src/components/sections/Samenwerken.astro
@src/components/sections/Testimonials.astro
@src/components/sections/Story.astro
@src/components/sections/Newsletter.astro
@src/components/sections/Articles.astro
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Assemble the full homepage in document order</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (currently renders BaseLayout + Hero from plan 01)
    - all seven section components (Hero, PressStrip, Samenwerken, Testimonials, Story, Newsletter, Articles)
    - .planning/PROJECT.md §Context (canonical docx section order), .planning/ROADMAP.md success criterion 1
  </read_first>
  <action>
    Edit src/pages/index.astro to import and render every section component inside `<BaseLayout>` in the exact docx document order: Hero → PressStrip → Samenwerken → Testimonials → Story → Newsletter → Articles. (Header/Nav and Footer are supplied by BaseLayout from plan 02, so the page body renders these seven sections between them.) Keep the plan-01 BaseLayout title/description props. Confirm the hero secondary "Mijn verhaal" CTA anchors to `#verhaal` and that Story carries `id="verhaal"` so the on-page anchor resolves (D-12 in-page anchor). Do not modify the section components themselves here — only compose them.
  </action>
  <verify>
    <automated>npm run build; echo "exit:$?"; grep -oE 'Hero|PressStrip|Samenwerken|Testimonials|Story|Newsletter|Articles' src/pages/index.astro | sort -u | wc -l; grep -c 'id="verhaal"' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - All 7 section components are imported/rendered in index.astro (the grep returns 7 unique names).
    - In dist/index.html the sections appear in order: hero H1 → "Bekend van" → "Samenwerken" → testimonial attributions → "Waarom ik nu mijn lessen deel" → newsletter heading → "Recente artikelen".
    - `grep 'id="verhaal"' dist/index.html` matches and the hero secondary CTA targets `#verhaal` (`grep '#verhaal' dist/index.html` matches).
  </acceptance_criteria>
  <done>The complete homepage renders all seven sections in docx order between the global header and footer; the "Mijn verhaal" anchor resolves; build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Automated baseline audit — a11y, SEO, honest-CTA, image pipeline, content integrity</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-RESEARCH.md §Validation Architecture (Phase Requirements → Test Map), §Accessibility Baseline, §Performance Baseline, §SEO / Metadata Checklist
    - .planning/ROADMAP.md §Phase 1 Success Criteria (all 5)
    - .planning/phases/01-foundation-design-system-dutch-homepage/01-UI-SPEC.md §Accessibility Contract, §UI Considerations
  </read_first>
  <action>
    Run the automated baseline gates over the assembled build (RESEARCH §Validation Architecture) and fix any failure by correcting the offending component/config (record cross-component fixes in the SUMMARY). Assert, from dist/:
    - Single h1; landmarks present (header, nav, main, footer); all images have non-empty alt (except intentionally decorative alt=""); form fields have explicit labels.
    - Honest-CTA integrity sitewide: NO `href="#"` and NO `href=""` anywhere in dist/; every unknown-destination CTA/nav item is aria-disabled.
    - Image pipeline: dist/_astro contains AVIF/WebP derivatives; hero is eager+fetchpriority=high; no `*.jpg` >300KB served; no committed originals/zip.
    - SEO: title, description, canonical, og:*, twitter:*, JSON-LD Person, sitemap-index.xml generated, robots.txt present; `<html lang="nl">`; no `/nl/` route.
    - Yellow discipline: audit that #FFDD11 usage stays within the 5 reserved uses (D-02) — grep component styles for accent usage and confirm no section/background fill.
    - Content integrity (HOME-11): verify the supplied hero title, USP row, testimonial attributions, and story heading appear verbatim; add a code comment / SUMMARY note flagging any docx extraction artifact (e.g. mojibake, run-together spacing) that was corrected, and any suspected factual inconsistency for user confirmation (do NOT silently change names/claims).
    Then run Lighthouse against the preview (RESEARCH full-suite command) and record LCP/CLS/perf/a11y scores in the SUMMARY; fix regressions against the RESEARCH §Performance targets where feasible.
  </action>
  <verify>
    <automated>npm run build && ! grep -rEq 'href="#"|href=""' dist/ && grep -c 'application/ld+json' dist/index.html && grep -c '<h1' dist/index.html && ls dist/sitemap-index.xml && ls dist/_astro/ | grep -c '\.avif$'</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rE 'href="#"|href=""' dist/` passes (no placeholder links anywhere in the built site — HOME-10).
    - `grep -c '<h1' dist/index.html` returns exactly 1 (a11y — FND-06).
    - `grep 'application/ld+json' dist/index.html` and `grep 'rel="canonical"' dist/index.html` match; `ls dist/sitemap-index.xml` exists; `ls dist/robots.txt` exists (FND-05).
    - `ls dist/_astro/ | grep '.avif'` returns >=1 and no served `*.jpg` exceeds ~300KB (FND-10).
    - Hero eager: `grep 'fetchpriority="high"' dist/index.html` matches (FND-07/HOME-02).
    - `grep 'lang="nl"' dist/index.html` matches; no `/nl/` directory in dist/.
    - Lighthouse performance and accessibility scores recorded in SUMMARY (targets: LCP <2.5s, CLS <0.1 per RESEARCH §Performance).
  </acceptance_criteria>
  <done>The assembled page passes the automated a11y/SEO/honest-CTA/image-pipeline gates; Lighthouse CWV/a11y scores are recorded; content integrity verified with any artifacts/inconsistencies flagged, not silently changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — responsive visual quality + honest CTA behavior across breakpoints</name>
  <what-built>
    The complete Dutch homepage: all docx sections (Header, Hero, "Bekend van" press strip, Samenwerken 3-card ladder, Testimonials mosaic, dark Story band, Newsletter signup, Recente artikelen, Footer) assembled and rendered under BaseLayout, responsive with subtle scroll-reveal animations, restrained yellow accent, self-hosted editorial fonts, optimized AVIF/WebP imagery, honest disabled CTAs, and baseline SEO/a11y/perf verified automatically.
  </what-built>
  <how-to-verify>
    1. Run `npm run build && npm run preview` and open the printed local URL (typically http://localhost:4321).
    2. DESKTOP (>=1200px): confirm the split-card hero (text left, rounded photo right), all sections in order, the mixed-size testimonials mosaic, and the full-width dark story band. Confirm yellow reads as restrained "signature moments" only — the page is NOT covered in yellow (D-02).
    3. TABLET (~768px) and MOBILE (~375px): resize/emulate. Confirm the hero stacks text-then-image, cards stack, the press strip scrolls horizontally without clipping, the hero H1 wraps gracefully, the story reading column does not overflow, and the mobile hamburger opens/closes an accessible nav panel.
    4. Click the hero "Mijn verhaal" secondary CTA — it should smooth-scroll to the story section.
    5. Confirm every future-destination CTA/nav item (Contact, Coaching, Spreker, Nieuwsbrief, Boek, NL|EN, LinkedIn, Boek lezing, Plan kennismaking, Schrijf je in, Lees verder, Alle artikelen) is clearly disabled — no button silently goes nowhere; the newsletter form does NOT fake a success state.
    6. Enable "Reduce motion" in OS/DevTools and reload — scroll-reveal animations should not fire; all content stays visible.
    7. Read the supplied copy (hero title, USPs, testimonials, story) and confirm names/claims look correct; note anything the SUMMARY flagged for confirmation.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (layout/copy/CTA/animation) to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist → visitor browser | The fully-assembled static site. Only surfaces: disabled newsletter form (no submit) and disabled external links. No backend, no auth, no DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Spoofing | any CTA/link silently going nowhere after assembly | high | mitigate | Task-2 gate `! grep -rE 'href="#"|href=""' dist/` fails the build if any dead link exists; every unknown destination is aria-disabled (HOME-10) |
| T-01-02 | Information Disclosure | originals/secrets leaking into the shipped bundle | high | mitigate | Task-2 asserts no `*.jpg` >300KB and no zip in dist/; `.gitignore` keeps originals out of git; no secrets exist in Phase 1 |
| T-01-13 | Repudiation | altered factual claims/quotes during assembly | medium | mitigate | Content-integrity check verifies verbatim hero/USP/testimonial/story copy; suspected inconsistencies flagged for user, never silently changed (HOME-11) |
| T-01-SC | Tampering | dependency supply chain (inherited) | high | mitigate | No new packages added in this plan; earlier package-legitimacy audit stands (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm run build` exits 0; `npm run preview` serves the full homepage.
- All 7 sections render in docx order between header and footer; "Mijn verhaal" anchors to #verhaal.
- `! grep -rE 'href="#"|href=""' dist/`; exactly one h1; landmarks + labelled form; JSON-LD + canonical + og + sitemap-index.xml + robots.txt; `lang="nl"`; no `/nl/` route.
- AVIF/WebP derivatives present; hero eager+high priority; no `*.jpg` >300KB served; no originals/zip committed.
- Lighthouse CWV/a11y recorded; yellow within the 5 reserved uses; reduced-motion suppresses animation.
- Human confirms responsive visual quality across desktop/tablet/mobile and honest CTA behavior.
</verification>

<success_criteria>
The Dutch homepage is complete and meets all five ROADMAP Phase-1 success criteria: (1) all docx sections render with supplied copy intact; (2) high-quality responsive layout with subtle, reduced-motion-respecting animations; (3) only optimized AVIF/WebP imagery served, originals never committed; (4) every known-destination CTA works and unknown ones are clearly disabled (no fake buttons), placeholders clearly marked; (5) baseline SEO/a11y/perf pass and the brand font is a swappable variable. The human approves the visual/responsive quality.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-design-system-dutch-homepage/01-06-SUMMARY.md` when done, recording Lighthouse scores, any content-integrity flags for the user, and confirmation that all 5 ROADMAP success criteria pass.
</output>
