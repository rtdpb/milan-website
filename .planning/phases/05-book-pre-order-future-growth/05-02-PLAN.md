---
phase: 05-book-pre-order-future-growth
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/analytics/PlausibleScript.astro
  - src/components/layout/BaseHead.astro
  - src/components/forms/ContactForm.astro
  - src/components/sections/Newsletter.astro
  - src/components/forms/BookInterestForm.astro
  - src/components/ui/StickyCTA.astro
  - src/components/sections/Hero.astro
  - src/pages/index.astro
  - src/pages/en/index.astro
  - src/pages/boek.astro
  - src/pages/en/book.astro
autonomous: true
requirements: [GROW-01]
user_setup:
  - service: plausible
    why: "Plausible analytics stays inert until a real domain is supplied (D-06). The script only injects in production builds when TODO_PLAUSIBLE_DOMAIN is replaced. The 4 custom-event goals must be registered in the Plausible dashboard, character-for-character, before the domain goes live."
    env_vars:
      - name: TODO_PLAUSIBLE_DOMAIN
        source: "Plausible dashboard — the exact production domain (e.g. milanvandermeulen.nl, NOT the GitHub Pages URL). Added as a config seam in Plan 01; replace when the domain + account are confirmed."
    dashboard_config:
      - task: "Register 4 Custom Event Goals (exact strings): 'Contact: Submit', 'Newsletter: Subscribe', 'Book: Interest', 'Sticky CTA: Click'"
        location: "Plausible dashboard → Site Settings → Goals → Add goal → Custom event"
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The Plausible script is ABSENT from the built HTML in development and while TODO_PLAUSIBLE_DOMAIN is unreplaced; it is present with a data-domain attribute only in a production build once a real domain is supplied (D-06)."
    - "Every window.plausible(...) call is guarded by `typeof window.plausible === 'function'` and lives inside a client-side <script> island, never in Astro frontmatter."
    - "A successful contact-form submit fires the 'Contact: Submit' goal; a click on the live Substack anchor fires 'Newsletter: Subscribe'; a successful book-interest submit fires 'Book: Interest'; a sticky-CTA click fires 'Sticky CTA: Click' (D-05)."
    - "A persistent sticky primary CTA appears on the homepage and book page when the hero CTA scrolls out of view, and hides when it returns (D-07 conversion optimization)."
    - "The sticky CTA is invisible and out of the tab order when hidden (hidden attribute + aria-hidden='true'), and respects prefers-reduced-motion (no slide transition) (UI-SPEC ## UI Considerations / Accessibility Contract)."
    - "The sticky CTA sits below the nav header (z-index 90 vs 100) and below the mobile nav panel (200), so it never overlaps navigation (UI-SPEC §3)."
    - "No cookie/consent banner is added anywhere on the site (D-04 cookieless analytics)."
  truths_backstop:
    - statement: "The sticky CTA button reflows to near-full-width (max 360px) at <768px and centred auto-width at >=768px without layout shift of page content."
      verification: backstop
  artifacts:
    - src/components/analytics/PlausibleScript.astro
    - src/components/ui/StickyCTA.astro
    - "src/components/layout/BaseHead.astro PlausibleScript injection"
    - "4 Plausible goal call sites (ContactForm, Newsletter, BookInterestForm, StickyCTA)"
    - "StickyCTA placement on index.astro, en/index.astro, boek.astro, en/book.astro"
    - "id='hero-cta' on the homepage Hero primary CTA anchor"
  key_links:
    - "PlausibleScript queue guard (is:inline) must load so window.plausible.q exists before any event island fires — else events are dropped."
    - "StickyCTA observes the hero CTA element by id ('hero-cta' on homepage, 'boek-hero-cta' on book page); a wrong/missing id means the sticky bar never toggles."
    - "Goal name strings in code must match the dashboard goal names character-for-character or events are tracked but unattributed."
---

<objective>
Expand the proven book slice with GROW-01: privacy-first analytics and one concrete conversion optimization. Add production-gated Plausible (D-04/D-06), wire the four conversion goals (D-05), and ship the sticky primary CTA (D-07) on the two long pages — each measurable via its Plausible goal.

Purpose: Measures what actually converts and adds a subtle, buildable-now conversion lever that respects the restrained design.
Output: `PlausibleScript.astro`, BaseHead injection, four guarded `window.plausible()` call sites, `StickyCTA.astro`, and sticky-CTA placement on homepage + book page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-book-pre-order-future-growth/05-CONTEXT.md
@.planning/phases/05-book-pre-order-future-growth/05-RESEARCH.md
@.planning/phases/05-book-pre-order-future-growth/05-PATTERNS.md
@.planning/phases/05-book-pre-order-future-growth/05-UI-SPEC.md
@.planning/phases/05-book-pre-order-future-growth/COVERAGE.md
</context>

<artifacts_this_phase_produces>
This plan (GROW-01) creates/modifies:
- `src/components/analytics/PlausibleScript.astro` — production+real-domain-gated Plausible script + queue guard.
- `src/components/layout/BaseHead.astro` — `<PlausibleScript />` injection.
- `src/components/forms/ContactForm.astro` — `Contact: Submit` goal on success.
- `src/components/sections/Newsletter.astro` — `Newsletter: Subscribe` goal on live-anchor click.
- `src/components/forms/BookInterestForm.astro` — `Book: Interest` goal on success (fills the Plan-01 placeholder).
- `src/components/ui/StickyCTA.astro` — sticky bottom CTA island; `Sticky CTA: Click` goal.
- `src/components/sections/Hero.astro` — `id="hero-cta"` on the homepage primary CTA anchor.
- `src/pages/index.astro`, `src/pages/en/index.astro`, `src/pages/boek.astro`, `src/pages/en/book.astro` — StickyCTA placement.

Goal names (register in Plausible dashboard verbatim): `Contact: Submit`, `Newsletter: Subscribe`, `Book: Interest`, `Sticky CTA: Click`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PlausibleScript component + BaseHead injection (production + real-domain gated)</name>
  <files>src/components/analytics/PlausibleScript.astro, src/components/layout/BaseHead.astro</files>
  <read_first>
    - src/config.ts (TODO_PLAUSIBLE_DOMAIN constant added in Plan 01)
    - src/components/layout/BaseHead.astro (frontmatter imports ~line 10; the JSON-LD is:inline script block ~96–104; the hreflang alternates block ~116–120 — inject PlausibleScript after it; note the existing `import.meta.env.PROD` usage and absolute-vs-BASE_URL src handling ~27–28)
    - src/components/sections/Newsletter.astro (IS_SENTINEL guard ~line 31 — the pattern PlausibleScript adapts)
    - 05-PATTERNS.md §src/components/analytics/PlausibleScript.astro (build-from-scratch instructions; the two-script structure; is:inline queue guard)
    - 05-RESEARCH.md §Pattern 1 + Example 1 (shouldInject = isProd && !IS_PLACEHOLDER; the queue-guard snippet; anti-pattern: never BASE_URL-prefix the plausible.io CDN src)
    - COVERAGE.md (standard `script.js` variant chosen; tagged-events opted out)
  </read_first>
  <action>
    Create src/components/analytics/PlausibleScript.astro. Import TODO_PLAUSIBLE_DOMAIN from config. Compute `isProd = import.meta.env.PROD`, `IS_PLACEHOLDER = TODO_PLAUSIBLE_DOMAIN === 'TODO_PLAUSIBLE_DOMAIN'`, `shouldInject = isProd && !IS_PLACEHOLDER`. When shouldInject is true, render two tags: the deferred Plausible tag `<script defer data-domain={TODO_PLAUSIBLE_DOMAIN} src="https://plausible.io/js/script.js">` (absolute URL — do NOT prefix with BASE_URL) and an `is:inline` queue-guard script that defines `window.plausible` as a queue-pushing shim so early calls are buffered. Render nothing when shouldInject is false.
    In BaseHead.astro, add `import PlausibleScript from '../analytics/PlausibleScript.astro'` to the frontmatter import block and place `<PlausibleScript />` in `<head>` after the x-default hreflang block. No other BaseHead change.
  </action>
  <verify>
    <automated>npm run build && node scripts/smoke-i18n.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build && node scripts/smoke-i18n.js` exits 0.
    - `grep -c "plausible.io/js/script.js" src/components/analytics/PlausibleScript.astro` returns 1 and the src is NOT prefixed with BASE_URL (`grep -c "BASE_URL.*plausible" src/components/analytics/PlausibleScript.astro` returns 0).
    - `grep -c "shouldInject" src/components/analytics/PlausibleScript.astro` >= 1 and the guard combines `import.meta.env.PROD` with the IS_PLACEHOLDER sentinel (`grep -c "TODO_PLAUSIBLE_DOMAIN === 'TODO_PLAUSIBLE_DOMAIN'" src/components/analytics/PlausibleScript.astro` returns 1).
    - `grep -c "is:inline" src/components/analytics/PlausibleScript.astro` >= 1 (queue guard is inline).
    - With TODO_PLAUSIBLE_DOMAIN unreplaced, the production build output contains NO `plausible.io/js/script.js` reference: `grep -rc "plausible.io/js/script.js" dist/` returns 0 across dist.
    - `grep -c "PlausibleScript" src/components/layout/BaseHead.astro` >= 2 (import + usage).
  </acceptance_criteria>
  <done>PlausibleScript exists and is injected via BaseHead; it emits nothing while the domain is a placeholder (verified inert in the dist build); the queue guard is is:inline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire the 4 Plausible conversion goals (guarded, client-side only)</name>
  <files>src/components/forms/ContactForm.astro, src/components/sections/Newsletter.astro, src/components/forms/BookInterestForm.astro</files>
  <read_first>
    - src/components/forms/ContactForm.astro (submit island; the success branch at ~line 273 `if (json && json.success === true)` — insert the goal call inside it, before hiding the form)
    - src/components/sections/Newsletter.astro (the live redirect anchor rendered when IS_SENTINEL is false, ~lines 108–115, class `newsletter__submit`; note there is NO existing <script> island — one must be added)
    - src/components/forms/BookInterestForm.astro (the single-line placeholder left in Plan 01's success branch — replace it with the guarded call)
    - 05-PATTERNS.md §Shared Patterns → Plausible event firing (the 4 exact goal names; typeof guard; client-side-only rule)
    - 05-RESEARCH.md §Pattern 3 (per-file insertion points) + §Common Pitfalls 5 (queue guard) + Assumptions A2 (colon-space goal names)
    - 05-UI-SPEC.md §5 Analytics (the event→trigger→location table)
  </read_first>
  <action>
    ContactForm.astro: inside the existing `json.success === true` success branch, before hiding the form, add a `typeof window.plausible === 'function'` guarded call firing `'Contact: Submit'`.
    Newsletter.astro: add a client-side `<script>` island (the section currently has none) that queries the live redirect anchor (`.newsletter__submit:not(.newsletter__submit--disabled)`), and if found, attaches a click listener that fires the guarded `'Newsletter: Subscribe'` call. Do not alter the `onsubmit="return false;"` behavior or the disabled-span branch.
    BookInterestForm.astro: replace the Plan-01 placeholder in the success branch with the guarded `'Book: Interest'` call, fired before the form is hidden.
    All three calls MUST be inside client-side `<script>` islands and MUST use the `typeof window.plausible === 'function'` guard. Goal name strings must be exact (colon-space format).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -c "Contact: Submit" src/components/forms/ContactForm.astro` returns 1; `grep -c "Newsletter: Subscribe" src/components/sections/Newsletter.astro` returns 1; `grep -c "Book: Interest" src/components/forms/BookInterestForm.astro` returns 1.
    - Every plausible call is guarded: in each of the three files, `grep -c "typeof window.plausible === 'function'" <file>` >= 1, and there is no unguarded `window.plausible(` call (each `window.plausible(` occurrence is within a typeof-guarded block — verify by inspection that no call precedes its guard).
    - Newsletter.astro now contains a `<script>` island (`grep -c "<script" src/components/sections/Newsletter.astro` >= 1) and the disabled branch selector excludes the disabled class (`grep -c "newsletter__submit--disabled" src/components/sections/Newsletter.astro` >= 1, i.e. the `:not()` selector is used).
    - No plausible call appears in any Astro frontmatter: none of the three `window.plausible(` calls sit above the component's template/`<script>` boundary (verify each is inside a `<script>` block).
  </acceptance_criteria>
  <done>All four goals fire from guarded client-side islands with exact names; contact + book fire on success, newsletter on live-anchor click; build green. (The StickyCTA goal is wired in Task 3.)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: StickyCTA component + placement on homepage + book page</name>
  <files>src/components/ui/StickyCTA.astro, src/components/sections/Hero.astro, src/pages/index.astro, src/pages/en/index.astro, src/pages/boek.astro, src/pages/en/book.astro</files>
  <read_first>
    - src/components/ui/RevealOnScroll.astro (the IntersectionObserver island IIFE ~50–99, the prefers-reduced-motion guard ~58–59, the reduced-motion CSS block ~124–131)
    - src/components/ui/Button.astro (the `btn btn--primary` class contract — height 48px, focus ring)
    - src/components/sections/Hero.astro (the primary CTA anchor ~lines 59–64 `class="hero__cta-primary btn btn--primary"` — add `id="hero-cta"`)
    - src/pages/index.astro + src/pages/en/index.astro (where a page-level component is placed; getStrings/locale; contactHref pattern)
    - src/pages/boek.astro + src/pages/en/book.astro (the hero Substack anchor carries `id="boek-hero-cta"` from Plan 01 — the sticky CTA observes it here)
    - 05-PATTERNS.md §src/components/ui/StickyCTA.astro (Props interface; define:vars; z-index 90; typeof-guarded click event; initial hidden+aria-hidden)
    - 05-RESEARCH.md §Pattern 6 + §Common Pitfalls 4 (z-index 90 below nav 100 / mobile panel 200) + Example 4 (observer threshold 0, show = !isIntersecting)
    - 05-UI-SPEC.md §3 Sticky CTA (dimensions, colors NO yellow bar bg, entrance animation, mobile near-full-width max 360px, aria-hidden toggling, no dismiss button) + Accessibility Contract
  </read_first>
  <action>
    Create src/components/ui/StickyCTA.astro with a Props interface `{ label: string; href: string; targetId: string; plausibleGoal?: string }` (plausibleGoal default `'Sticky CTA: Click'`). Render a fixed-bottom `<div class="sticky-cta" hidden aria-hidden="true">` containing one `btn btn--primary` anchor to `href`. In a `<script define:vars={{ targetId, plausibleGoal }}>` island: get the banner, button, and the hero element by `targetId`; bail if any is missing; read prefers-reduced-motion; create an IntersectionObserver (threshold 0) that on `!entry.isIntersecting` removes `hidden` + sets `aria-hidden="false"` (and toggles the `sticky-cta--visible` class only when motion is allowed), and reverses on re-entry; attach a `typeof window.plausible === 'function'` guarded click listener firing `plausibleGoal`. Style: `position:fixed; bottom:0; left:0; right:0; z-index:90`, background `var(--color-bg)`, `border-top:1px solid var(--color-border)`, `padding-block: var(--space-md)`; inner `max-width: var(--max-width)` centered; translateY(100%)→0 transition on `--duration-base`/`--ease-out` with a `@media (prefers-reduced-motion: reduce) { transition: none }` block; NO yellow background on the bar (accent stays on the button only). Mobile: button `width:100%; max-width:360px`; >=768px auto-width centered.
    Add `id="hero-cta"` to the Hero.astro primary CTA anchor (do not change its href/classes).
    Place `<StickyCTA label={t.hero.ctaPrimary} href={contactHref} targetId="hero-cta" />` on index.astro and en/index.astro (EN uses getRelativeLocaleUrl for href). Place `<StickyCTA label={t.boek.stickyCta} href={contactHref} targetId="boek-hero-cta" />` on boek.astro and en/book.astro. Do not add the sticky CTA to coaching/spreker/mijn-verhaal (out of scope this phase).
  </action>
  <verify>
    <automated>npm run build && node scripts/smoke-i18n.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build && node scripts/smoke-i18n.js` exits 0.
    - `grep -c "z-index: 90" src/components/ui/StickyCTA.astro` returns 1 (below nav 100 / panel 200).
    - `grep -c "prefers-reduced-motion" src/components/ui/StickyCTA.astro` >= 1 and `grep -c "transition: none" src/components/ui/StickyCTA.astro` >= 1.
    - The sticky-CTA click is guarded: `grep -c "typeof window.plausible === 'function'" src/components/ui/StickyCTA.astro` >= 1 and `grep -c "Sticky CTA: Click" src/components/ui/StickyCTA.astro` >= 1 (via the default prop).
    - Root element starts hidden: `grep -c 'aria-hidden="true"' src/components/ui/StickyCTA.astro` >= 1 and the `hidden` attribute is present on the root div.
    - `grep -c 'id="hero-cta"' src/components/sections/Hero.astro` returns 1.
    - StickyCTA is placed on all four pages: `grep -lc "StickyCTA" src/pages/index.astro src/pages/en/index.astro src/pages/boek.astro src/pages/en/book.astro` lists all four; boek pages use `targetId="boek-hero-cta"` and index pages use `targetId="hero-cta"`.
    - The sticky bar background is NOT accent yellow: `grep -c "color-accent" src/components/ui/StickyCTA.astro` returns 0 in the `.sticky-cta` background rule (accent only via the button's btn--primary class).
  </acceptance_criteria>
  <done>StickyCTA appears on scroll on the homepage and book page (both locales), fires the guarded Sticky CTA goal, respects reduced-motion, sits at z-index 90, and uses no yellow bar background; build + smoke green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → plausible.io CDN | A third-party `<script defer>` from plausible.io is loaded (production + real domain only). |
| build config → rendered HTML | `TODO_PLAUSIBLE_DOMAIN` sentinel + `import.meta.env.PROD` decide whether the external script is emitted at all. |
| visitor browser → window.plausible | Client-side event calls push named goals into the Plausible queue. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering (supply-chain) | Plausible CDN `<script>` | medium | mitigate | Exact, pinned URL `https://plausible.io/js/script.js` (established EU-hosted cookieless provider); loaded ONLY in production behind the domain sentinel, so it is inert on the placeholder/dev build (verified by dist grep in Task 1). No secret is exposed. UI-SPEC Registry Safety recorded the CDN vetting 2026-08-19. |
| T-05-06 | Information disclosure (privacy) | Plausible pageview/event tracking | low | accept | Plausible is cookieless and GDPR-aligned by design (D-04) — no PII, no cookies, no consent banner required. Accepted per the phase's privacy-first decision. |
| T-05-07 | Tampering (event injection) | window.plausible goal calls | low | mitigate | Goal names are hard-coded string literals in code; no user input flows into `window.plausible(...)`. Each call is `typeof`-guarded and client-side only. |

No high-severity threat is introduced. The Plausible CDN script is the only new supply-chain surface; it is inert until a real domain is confirmed and loaded from a pinned, established provider URL. No consent banner is added (D-04), which is correct for cookieless analytics rather than a gap.
</threat_model>

<verification>
- `npm run build && node scripts/smoke-i18n.js` green after each task.
- Inert-analytics proof: with TODO_PLAUSIBLE_DOMAIN unreplaced, `grep -rc "plausible.io/js/script.js" dist/` returns 0 (Task 1 gate).
- Manual (deferred to /gsd-verify-work): scroll homepage + /boek and confirm the sticky CTA appears/hides and respects reduced-motion; after the domain is supplied + goals registered, confirm all four goals fire in the Plausible dashboard.
</verification>

<success_criteria>
- GROW-01 satisfied: privacy-first cookieless analytics installed behind a config seam (no consent banner), four conversion goals wired and guarded, and one concrete conversion optimization (sticky primary CTA) shipped on the two long pages with its own tracked goal.
- All must_haves truths observable; the backstop truth verified at <768px during /gsd-verify-work.
</success_criteria>

<output>
Create `.planning/phases/05-book-pre-order-future-growth/05-02-SUMMARY.md` when done.
</output>
