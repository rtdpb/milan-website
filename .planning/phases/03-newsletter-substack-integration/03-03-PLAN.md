---
phase: 03-newsletter-substack-integration
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/components/layout/Nav.astro
  - src/components/layout/Footer.astro
  - src/components/sections/Samenwerken.astro
autonomous: false
requirements: [NEWS-01]
estimate:
  tokens: 40000
  raw_tokens: 24000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The Nav 'Nieuwsbrief' item is a real internal `<a href={BASE_URL}nieuwsbrief>` (flipped from reason to href) on both desktop and mobile nav, with active-state aria-current='page' + nav-link--active when on /nieuwsbrief."
    - "The Footer 'Nieuwsbrief' link is a real `<a href={BASE_URL}nieuwsbrief>` (flipped from reason to href)."
    - "The Samenwerken 'Nieuwsbrief' commitment-ladder card CTA is enabled (disabled:false, href={BASE_URL}nieuwsbrief) and renders a primary Button."
    - "Boek nav/footer items remain disabled (Phase 5); the NL|EN switch remains a placeholder (Phase 4)."
    - "`npm run build` exits 0 and clicking Nieuwsbrief in nav, footer, and the Samenwerken card all resolve to /nieuwsbrief under the GitHub Pages subpath."
    # --- Edge-coverage report (spec-less probe fallback) — no silent drops ---
    - { statement: "NEWS-01 adjacency: enabling the Nieuwsbrief nav/footer/card entries does not merge with or collide with the already-enabled Coaching/Spreker/Mijn-verhaal entries — each remains a distinct link; Boek stays distinct and disabled", verification: explicit }
    - { statement: "NEWS-01 empty/single: the nav/footer link targets a page that exists in every build state (sentinel or live), so the link is never a dead end (no empty/null destination)", verification: explicit }
    - { statement: "NEWS-01 ordering: nav/footer item order is unchanged (Coaching, Spreker, Nieuwsbrief, Mijn verhaal, Boek) — enabling does not reorder", verification: explicit }
    - { statement: "NEWS-01 idempotency: the href-or-reason flip is a static data change — rebuilding yields identical nav/footer/card HTML", verification: backstop }
    - { statement: "NEWS-01 concurrency: no runtime state; build-time only", verification: backstop }
    # --- UI-SPEC ## UI Considerations lifted (no silent drops) ---
    - { statement: "disabled (Nav/Footer/Samenwerken sentinel): all three flip from disabled to enabled via the href-or-reason union; no intermediate half-enabled state", verification: explicit }
    - { statement: "Nav/Footer/Samenwerken (E5): a nav link has no data-bearing empty/loading/error/partial/populated states; only enabled vs disabled applies and is covered", verification: explicit }
  artifacts:
    - "src/components/layout/Nav.astro — Nieuwsbrief item enabled (desktop + mobile)"
    - "src/components/layout/Footer.astro — Nieuwsbrief link enabled"
    - "src/components/sections/Samenwerken.astro — Nieuwsbrief card CTA enabled (primary Button)"
  key_links:
    - "All three surfaces point at `${import.meta.env.BASE_URL}nieuwsbrief` — the page created in Plan 02; enabling before the page existed would 404, hence wave 3."
  prohibitions:
    - "must never use a bare '/nieuwsbrief' href (404s under the GitHub Pages subpath)"
    - "must never enable the Boek item (Phase 5) or the NL|EN switch (Phase 4)"
    - "must never use href=\"#\" for any still-disabled item"
---

<objective>
Complete the phase by enabling every navigation entry point into the new `/nieuwsbrief` page: the Nav item (desktop + mobile), the Footer link, and the Samenwerken commitment-ladder card CTA. Each flips from the honest disabled `reason` state to a real BASE_URL-prefixed internal link now that the page exists (Plan 02). Then verify the whole phase end-to-end with a human checkpoint.

Purpose: close the loop so users can actually discover and reach the newsletter page from anywhere on the site. This is the final expansion slice — it depends on the page existing, which is why it runs last.
Output: enabled Nieuwsbrief entries in Nav, Footer, and Samenwerken; phase-level verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-newsletter-substack-integration/03-PATTERNS.md
@.planning/phases/03-newsletter-substack-integration/03-UI-SPEC.md
@.planning/phases/03-newsletter-substack-integration/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable Nieuwsbrief in Nav.astro and Footer.astro</name>
  <files>src/components/layout/Nav.astro, src/components/layout/Footer.astro</files>
  <read_first>
    - src/components/layout/Nav.astro (self — navItems array line ~37 Nieuwsbrief entry; the item.href conditional render with active state; mobile nav panel driven by the same navItems array)
    - src/components/layout/Footer.astro (self — navItems array line 38 Nieuwsbrief entry; the item.href ? <a> : <span> conditional lines 59-77)
    - .planning/phases/03-newsletter-substack-integration/03-PATTERNS.md (Nav.astro lines 489-518; Footer.astro lines 522-543 — href-or-reason flip; active-state already handled by existing conditional)
  </read_first>
  <action>
    In src/components/layout/Nav.astro, change the Nieuwsbrief navItems entry from `{ label: nl.nav.nieuwsbrief, reason: 'Nieuwsbrief pagina volgt in fase 3' }` to `{ label: nl.nav.nieuwsbrief, href: `${import.meta.env.BASE_URL}nieuwsbrief` }`. No markup change is needed — the existing `item.href ? <a ...active-state...> : <span aria-disabled>` conditional already renders the enabled anchor with `nav-link--active` + `aria-current="page"` when `currentPath.startsWith(item.href)`, and the same `navItems` array drives both the desktop and mobile panels, so this one-line change covers both. Leave the Boek entry disabled (Phase 5) and the NL|EN switch a placeholder (Phase 4) untouched.

    In src/components/layout/Footer.astro, change the Nieuwsbrief navItems entry (line 38) the same way: `reason: 'Nieuwsbrief pagina volgt in fase 3'` → `href: `${import.meta.env.BASE_URL}nieuwsbrief``. The existing `item.href ? <a href={item.href} class="site-footer__nav-link"> : <span aria-disabled>` conditional (lines 61-75) renders the enabled link with no further change. Leave Boek disabled.

    Both hrefs MUST use `${import.meta.env.BASE_URL}nieuwsbrief` — never a bare `/nieuwsbrief` (CR-01, 404s under the GitHub Pages subpath). Update the stale "mijnVerhaal enabled ... nieuwsbrief/boek still disabled" trailing comments in both files so they reflect that nieuwsbrief is now enabled and only Boek remains disabled.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build 2>&1 | tail -3 && grep -q 'BASE_URL}nieuwsbrief' src/components/layout/Nav.astro && grep -q 'BASE_URL}nieuwsbrief' src/components/layout/Footer.astro && ! grep -q "Nieuwsbrief pagina volgt in fase 3" src/components/layout/Nav.astro && ! grep -q "Nieuwsbrief pagina volgt in fase 3" src/components/layout/Footer.astro && echo NAVFOOTER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - Nav.astro and Footer.astro Nieuwsbrief entries use `${import.meta.env.BASE_URL}nieuwsbrief` (grep confirms `BASE_URL}nieuwsbrief` in both).
    - The old `reason: 'Nieuwsbrief pagina volgt in fase 3'` string is gone from both files.
    - Boek entries still use a `reason:` (disabled) in both files; NL|EN switch unchanged.
    - Built /nieuwsbrief page shows the Nieuwsbrief nav item with `aria-current="page"` (active state).
  </acceptance_criteria>
  <done>Nieuwsbrief is reachable from the header (desktop + mobile) and footer with correct active state; Boek and NL|EN remain honest placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Enable the Samenwerken 'Nieuwsbrief' card CTA</name>
  <files>src/components/sections/Samenwerken.astro</files>
  <read_first>
    - src/components/sections/Samenwerken.astro (self — ladderCards[0] Nieuwsbrief card lines ~41-49 with disabled:true/placeholderReason/href:undefined; the Button render block using variant="secondary" ~line 112-118)
    - .planning/phases/03-newsletter-substack-integration/03-PATTERNS.md (Samenwerken.astro lines 547-581 — the 3-field flip + the variant='primary' note for card 1)
    - .planning/phases/03-newsletter-substack-integration/03-UI-SPEC.md (§Surface 4 Samenwerken — enabled internal link, primary yellow Button, one yellow per card)
  </read_first>
  <action>
    In src/components/sections/Samenwerken.astro, change the three fields on the Nieuwsbrief card (ladderCards[0]): `disabled: true` → `disabled: false`; `placeholderReason: \`...fase 3...\`` → `placeholderReason: undefined`; `href: undefined` → `href: `${import.meta.env.BASE_URL}nieuwsbrief``. Keep eyebrow/title/description/cta/ariaId unchanged (CTA label stays nl.samenwerken.cards[0].cta = "Schrijf je gratis in").

    The Nieuwsbrief card CTA must render as `Button variant="primary"` (UI-SPEC §Surface 4 — the one yellow usage in that card), while the other two cards keep `variant="secondary"`. Implement per 03-PATTERNS.md lines 579: add a `variant` field to the ladderCards data objects (card 0 → 'primary', cards 1 and 2 → 'secondary') and pass `variant={card.variant ?? 'secondary'}` to the `<Button>` in the render loop. This is an internal on-site link, so do NOT pass the `external` prop (no target=_blank; it is not a Substack redirect). No CSS change — Button.astro's disabled/href props already drive rendering.

    Use `${import.meta.env.BASE_URL}nieuwsbrief` (never bare `/nieuwsbrief`, CR-01). Remove/update the stale Nieuwsbrief-disabled comment so it reflects the enabled state.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build 2>&1 | tail -3 && grep -q 'BASE_URL}nieuwsbrief' src/components/sections/Samenwerken.astro && echo SAMENWERKEN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - Samenwerken.astro ladderCards[0] has `disabled: false`, `placeholderReason: undefined`, and `href: `${import.meta.env.BASE_URL}nieuwsbrief``.
    - The Nieuwsbrief card Button renders `variant="primary"`; the other two cards render `variant="secondary"`.
    - No `external` prop / `target="_blank"` on the Nieuwsbrief card CTA (internal link).
    - The built homepage Samenwerken Nieuwsbrief card is a clickable link to /nieuwsbrief (no aria-disabled span).
  </acceptance_criteria>
  <done>The Samenwerken commitment ladder's first rung ("Nieuwsbrief") is now a live yellow CTA into /nieuwsbrief, completing the enable-everywhere wiring.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — Phase 3 end-to-end newsletter + Substack integration</name>
  <what-built>
    Phase 3 end-to-end: the full Substack integration wired behind the TODO_SUBSTACK_URL sentinel.
    - src/lib/rss.ts build-time RSS fetch/parse utility (Plan 01)
    - homepage Recente-artikelen fed from RSS behind the sentinel (Plan 01)
    - .github/workflows/deploy.yml with daily cron rebuild (Plan 01)
    - /nieuwsbrief editorial landing page (Plan 02)
    - email-only honest Substack redirect signup on homepage + /nieuwsbrief (Plan 02)
    - Nieuwsbrief enabled in Nav (desktop + mobile), Footer, and the Samenwerken card (this plan)
    In the current sentinel state, the honest observable behavior is: build is green, articles show placeholder cards, signup is an aria-disabled span — everything flips live the instant the real Substack URL replaces the sentinel.
  </what-built>
  <how-to-verify>
    1. Run `npm run build` — confirm it exits 0 with no errors.
    2. Run `npm run preview` and open `http://localhost:4321/milan-website/` (base path).
    3. Homepage: confirm Recente-artikelen shows 3 placeholder cards with PlaceholderBadge; the newsletter signup shows a single email field and a disabled "Schrijf je in" affordance (not clickable, no fake success); the Samenwerken "Nieuwsbrief" card CTA is a live yellow button.
    4. Click the Samenwerken "Nieuwsbrief" card CTA, the header "Nieuwsbrief" nav item, and the footer "Nieuwsbrief" link — all three should land on `/milan-website/nieuwsbrief`.
    5. On /nieuwsbrief: confirm a single H1 hero with a primary CTA, a value-prop row (Eerlijk/Praktisch/Maandelijks), the signup section, and a "Eerder verschenen" archive of placeholder cards. Confirm the Nieuwsbrief nav item shows its active state. Confirm NO social-proof/testimonial section.
    6. Confirm Boek (nav + footer) is still a disabled placeholder and the NL|EN switch is still a placeholder.
    7. Resize to mobile width: confirm the mobile nav panel also has the enabled Nieuwsbrief item and layouts are responsive.
    8. (Optional, to see live state) Temporarily set TODO_SUBSTACK_URL in src/config.ts to a real publication URL, rebuild, and confirm real article cards + a working new-tab redirect signup; then revert.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nav/Footer/Samenwerken links → /nieuwsbrief | Internal navigation (build-time static hrefs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-07 | Tampering | Internal href construction for Nieuwsbrief links | low | mitigate | Hrefs built from `import.meta.env.BASE_URL` (static, build-time) — no user input; never a bare or user-controlled path. Enforced via grep in acceptance criteria. |
| T-3-08 | Spoofing | Accidental enabling of Boek/NL-EN placeholders | low | accept | Scope limited to the Nieuwsbrief entry; Boek/NL-EN left untouched and asserted still-disabled. No external surface introduced by this plan. |
</threat_model>

<verification>
- `npm run build` exits 0.
- Nieuwsbrief reachable from nav (desktop + mobile), footer, and Samenwerken card; all resolve under the BASE_URL subpath.
- Boek + NL|EN remain disabled placeholders.
- Human-verify checkpoint approved.
</verification>

<success_criteria>
Every navigation entry point into /nieuwsbrief is live and correct; the phase is verified end-to-end. NEWS-01 fully reachable; the phase ships integration-ready behind the sentinel (NEWS-02/NEWS-03 go live on URL supply).
</success_criteria>

<output>
Create `.planning/phases/03-newsletter-substack-integration/03-03-SUMMARY.md` when done.
</output>
