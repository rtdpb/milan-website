---
phase: 03-newsletter-substack-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/rss.ts
  - src/config.ts
  - src/components/sections/Articles.astro
  - .github/workflows/deploy.yml
autonomous: true
requirements: [NEWS-03]
user_setup:
  - service: substack
    why: "Live subscribe + real recent-articles feed (NEWS-02/NEWS-03) go live the instant the real URL replaces the sentinel. Ships fully wired without it (honest placeholder)."
    env_vars: []
    dashboard_config:
      - task: "Supply the Substack publication URL and grep-replace TODO_SUBSTACK_URL in src/config.ts (strip any trailing slash)"
        location: "src/config.ts — TODO_SUBSTACK_URL constant"
estimate:
  tokens: 58000
  raw_tokens: 34000
  tasks: 3
  confidence: med

must_haves:
  truths:
    # --- Goal-backward observable truths ---
    - "With TODO_SUBSTACK_URL at its sentinel value, `astro check && astro build` exits 0 and the homepage Recente-artikelen section renders the existing 3 placeholder cards with PlaceholderBadge (honest current state, D-04/D-06)."
    - "src/lib/rss.ts exports fetchSubstackFeed(feedUrl, maxItems) returning ArticleCard[], and exports the ArticleCard and PlaceholderCard types."
    - "The RSS parser is configured with removeNSPrefix: false so item['content:encoded'] is accessible (RESEARCH Pitfall 1)."
    - "A single-item feed is normalized to an array before .map() so the build does not crash when the publication has exactly one post (RESEARCH Pitfall 2)."
    - ".github/workflows/deploy.yml exists with push, schedule (daily cron), and workflow_dispatch triggers and a concurrency group so scheduled rebuilds do not collide with push deploys."
    - "D-02: the recent-articles feed uses build-time RSS fetch (static HTML, zero runtime JS, no CORS proxy) refreshed by the scheduled GitHub Action cron rebuild — client-side runtime fetch is explicitly rejected."
    - "D-07: the homepage Recente-artikelen section shows the latest 3 posts mapped to the ArticleCard contract (title, link→post URL, pubDate→date, description→text excerpt, readTime derived from word count, neutral 'Artikel' category — never fabricated); 'Alle artikelen' → Substack publication home; isPlaceholder dropped when real data present."
    # --- Edge-coverage report (spec-less probe fallback) — no silent drops ---
    - "NEWS-01 ordering: RSS items render newest-first as delivered by Substack (Substack feed is reverse-chronological); order is stable across rebuilds for the same feed."
    - "NEWS-03 ordering: same reverse-chronological, stable ordering for the homepage 3-card slice."
    - { statement: "NEWS-01 empty: empty/null RSS input (0 items, unreachable feed, or sentinel) yields the honest placeholder cards, never a crash or fabricated post", verification: explicit }
    - { statement: "NEWS-03 empty: empty feed / single-item / null → placeholder fallback (0 items) or correctly-normalized single card (1 item); build never breaks", verification: explicit }
    - { statement: "NEWS-01 adjacency: same-day posts and duplicate titles each render as distinct cards (keyed by RSS link/guid, not by title)", verification: backstop }
    - { statement: "NEWS-03 adjacency: two posts with identical titles or identical pubDate render as two separate cards", verification: backstop }
    - { statement: "NEWS-01 idempotency: a rebuild against an unchanged feed produces byte-identical article HTML (deterministic mapping — no timestamps, no randomness in rss.ts)", verification: backstop }
    - { statement: "NEWS-03 idempotency: build-time fetch + scheduled rebuild produce the same output for the same feed; a rebuild with no new posts is a no-op diff", verification: backstop }
    - { statement: "NEWS-01 concurrency: the scheduled cron rebuild and a push-triggered deploy do not collide — deploy.yml declares a concurrency group so runs serialize", verification: explicit }
    - { statement: "NEWS-03 concurrency: same concurrency-group guarantee covers the feed-refresh rebuild path", verification: explicit }
    # --- UI-SPEC ## UI Considerations lifted (no silent drops) ---
    - { statement: "empty (Articles 0 RSS items): sentinel guard renders existing 3 placeholder cards + PlaceholderBadge; build-log warning emitted; page never breaks", verification: explicit }
    - { statement: "loading (RSS fetch at build time): no loading state exists — build-time fetch either succeeds (real cards) or falls back (placeholders); zero runtime loading state", verification: explicit }
    - { statement: "error (RSS fetch failure network/4xx/5xx): try/catch + res.ok check in rss.ts returns []; D-06 fallback to placeholders; console.warn in build log only, no error rendered on page", verification: explicit }
    - { statement: "populated (Articles 1-3 items from RSS): grid renders 1-3 cards; a 1- or 2-card row does not break the 3-column grid layout", verification: backstop }
    - { statement: "overflow (article title long from RSS): text-wrap:balance on H3; visual-test an ~80-char title at each breakpoint for no overflow/collapsed card height", verification: backstop }
    - { statement: "overflow (article excerpt long from RSS): excerpt capped at 160 chars in rss.ts; visual-test a 160-char excerpt at 300px min grid width", verification: backstop }
    - { statement: "Article feed (E3) zero-one-many: homepage caps at 3; grid reads correctly at 0 (placeholder), 1, and many; 1-item/2-item rows do not stretch awkwardly at each breakpoint", verification: backstop }
  artifacts:
    - "src/lib/rss.ts — fetchSubstackFeed utility + ArticleCard/PlaceholderCard types"
    - "src/components/sections/Articles.astro — RSS-fed (sentinel-guarded) homepage recent-articles section"
    - "src/config.ts — TODO_SUBSTACK_URL with derived-URL documentation block"
    - ".github/workflows/deploy.yml — GitHub Pages deploy + scheduled rebuild"
    - "package.json / package-lock.json — fast-xml-parser@^5.11.0 dependency"
  key_links:
    - "Articles.astro imports TODO_SUBSTACK_URL from config and fetchSubstackFeed from lib/rss — the sentinel decides placeholder vs live feed."
    - "deploy.yml runs `astro build` which executes the Articles.astro build-time fetch — the CI cron is what makes NEWS-03 'automatic'."
  prohibitions:
    - "must never fabricate article content or metadata when the feed is empty or unreachable"
    - "must never render RSS content:encoded (or any RSS-sourced HTML) with set:html"
    - "must never fetch RSS at runtime in the browser (Substack RSS has no CORS headers)"
    - "must never use fast-xml-parser < 5.3.5 (CVE-2026-25896)"
---

<objective>
Prove the entire Phase 3 data architecture end-to-end with the thinnest possible vertical slice: install the RSS parser, build the shared `src/lib/rss.ts` fetch/transform utility, wire the homepage `Articles.astro` section to consume it behind the `TODO_SUBSTACK_URL` sentinel, and stand up the GitHub Actions deploy+cron workflow that makes the feed refresh automatically.

Because `TODO_SUBSTACK_URL` is still the sentinel, the honest observable behavior after this plan is: the site builds green and the homepage shows the existing 3 placeholder cards — exactly the current live state — but every seam (config → fetch → parse → ArticleCard → static HTML → scheduled rebuild) is now real and verified. The moment Milan supplies the URL, one grep-replace flips the homepage feed live and the cron keeps it fresh.

Purpose: this is the tracer. It touches every layer NEWS-03 will use (config seam, Node build-time fetch, XML parse, ArticleCard contract, Astro static render, CI freshness) so an architectural dead-end surfaces after one commit, not after the page and nav are also built on top of it.
Output: `src/lib/rss.ts`, RSS-fed `Articles.astro`, documented `config.ts`, `.github/workflows/deploy.yml`, `fast-xml-parser` dependency.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-newsletter-substack-integration/03-RESEARCH.md
@.planning/phases/03-newsletter-substack-integration/03-PATTERNS.md
@.planning/phases/03-newsletter-substack-integration/03-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end RSS tracer — install parser, build rss.ts utility, wire Articles.astro behind the sentinel</name>
  <files>package.json, package-lock.json, src/lib/rss.ts, src/config.ts, src/components/sections/Articles.astro</files>
  <read_first>
    - src/config.ts (TODO_SUBSTACK_URL sentinel at line 29 — the exact string 'TODO_SUBSTACK_URL')
    - src/components/sections/Articles.astro (self — current ArticleCard comment block lines 22-31, data source lines 40-43, markup loop, disabled "Lees verder" span lines 97-106, disabled "Alle artikelen" span lines 120-130)
    - .planning/phases/03-newsletter-substack-integration/03-RESEARCH.md (§Pattern 1 rss.ts, §Pattern 2 sentinel guard, Pitfalls 1/2, §RSS → ArticleCard Field Mapping)
    - .planning/phases/03-newsletter-substack-integration/03-PATTERNS.md (src/lib/rss.ts full pattern lines 37-115; Articles.astro modification lines 302-423)
    - src/i18n/nl.ts (nl.articles.items placeholder array — the sentinel/fallback data source)
  </read_first>
  <action>
    Install the parser: `npm install fast-xml-parser@^5.11.0` (must resolve to >= 5.3.5 per CVE-2026-25896; RESEARCH verified 5.11.0). Commit both package.json and package-lock.json. Do NOT add a package-legitimacy human checkpoint — RESEARCH §Package Legitimacy Audit assessed the SUS flag as a false positive (8-year-old, 69M weekly downloads, patched) and explicitly instructs the planner to proceed without a gate.

    Create src/lib/rss.ts per 03-PATTERNS.md lines 37-115. Export `type ArticleCard` (fields: category, title, excerpt, date, readTime, isPlaceholder: false, href: string) and `type PlaceholderCard` (same fields, isPlaceholder: true, no href). Construct the module-level XMLParser with `ignoreAttributes: false`, `attributeNamePrefix: '@_'`, and `removeNSPrefix: false` (mandatory — RESEARCH Pitfall 1, so item['content:encoded'] resolves). Implement helper `wordCountToReadTime(html)` (strip tags via `.replace(/<[^>]+>/g, ' ')`, split on whitespace, `Math.max(1, Math.round(words/200))`, return `${minutes} min leestijd`) and `formatPubDate(pubDate)` (parse with `new Date`, format `${dutchMonth} ${year}` using a hard-coded Dutch month-name array). Implement async `fetchSubstackFeed(feedUrl, maxItems = 3): Promise<ArticleCard[]>` wrapped in try/catch: `fetch`, on `!res.ok` `console.warn` the status and return []; parse XML; read `parsed?.rss?.channel?.item ?? []`; normalize with `Array.isArray(raw) ? raw : [raw]` (RESEARCH Pitfall 2); `.slice(0, maxItems).map(...)` to ArticleCard using category = neutral 'Artikel' label (never a fabricated topic), title = item.title, excerpt = `(item.description ?? '').replace(/<[^>]+>/g, ' ').slice(0, 160)`, date = formatPubDate(item.pubDate), readTime = wordCountToReadTime(item['content:encoded'] ?? item.description ?? ''), isPlaceholder = false, href = item.link; on any thrown error `console.warn` and return []. rss.ts must be pure and deterministic — no Date.now(), no random, no timestamps in output.

    Extend src/config.ts: keep `export const TODO_SUBSTACK_URL = 'TODO_SUBSTACK_URL';` unchanged in value, but replace its doc comment with the derived-URL documentation block from 03-PATTERNS.md lines 475-484 (documents Feed = `${TODO_SUBSTACK_URL}/feed`, Subscribe = `${TODO_SUBSTACK_URL}/subscribe`, the sentinel check expression, and the strip-trailing-slash note). Do NOT export new constants — derived URLs are computed inline in components.

    Rewire src/components/sections/Articles.astro per 03-PATTERNS.md lines 302-423: add imports `import { TODO_SUBSTACK_URL } from '../../config';` and `import { fetchSubstackFeed, type ArticleCard, type PlaceholderCard } from '../../lib/rss';`. Remove the ArticleCard comment-type block (lines 22-31) since the type is now imported. Replace the data-source assignment (lines 40-43) with the sentinel guard: `const IS_SENTINEL = TODO_SUBSTACK_URL === 'TODO_SUBSTACK_URL';` and `const substackPublicationUrl = TODO_SUBSTACK_URL;`, then `let articles: ArticleCard[] | PlaceholderCard[];` — if IS_SENTINEL, map nl.articles.items to placeholder cards (isPlaceholder: true as const); else fetch `${TODO_SUBSTACK_URL}/feed` with maxItems 3, and if the result is empty length, `console.warn('[Phase 3] Substack feed returned 0 items — rendering placeholder cards.')` and fall back to the same placeholder mapping, otherwise use the fetched array (D-06). Update the markup loop: only render the PlaceholderBadge row when `article.isPlaceholder`; render "Lees verder" as the existing disabled `<span role="link" aria-disabled tabindex="-1">` when isPlaceholder, else as `<a href={(article as ArticleCard).href} target="_blank" rel="noopener noreferrer" class="articles__read-more articles__read-more--enabled" aria-label={`Lees "${article.title}" op Substack`}>`; render "Alle artikelen" as the existing disabled span when IS_SENTINEL, else as `<a href={substackPublicationUrl} target="_blank" rel="noopener noreferrer" class="articles__see-all articles__see-all--enabled">`. Add the enabled-state CSS from 03-PATTERNS.md lines 396-423 (opacity 1, cursor pointer, hover underline / arrow translate, focus-visible outline). Keep the placeholder card key stable on the RSS link/guid where a key is needed so same-day/duplicate-title posts stay distinct (adjacency edge). Remove the two stale `TODO ... Phase 3` inline comments that this task fulfills. Preserve the existing role="list"/role="listitem" grid semantics.

    Every external anchor added MUST carry `rel="noopener noreferrer"` with `target="_blank"` (T-01-04 / threat T-3-01). Never use `set:html` on any RSS field (threat T-3-02). The excerpt uses `description` stripped to text — never `content:encoded` for display.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (which runs `astro check && astro build`) exits 0 with TODO_SUBSTACK_URL at its sentinel value.
    - The build output homepage (dist) Recente-artikelen section contains 3 placeholder cards each with a PlaceholderBadge (sentinel state renders placeholders, D-04).
    - src/lib/rss.ts exports `fetchSubstackFeed`, `ArticleCard`, and `PlaceholderCard`; grep shows `removeNSPrefix: false` and `Array.isArray(` present in rss.ts.
    - rss.ts contains no `Date.now(`, no `Math.random(`, and no `set:html` (deterministic + safe).
    - package.json dependencies include `fast-xml-parser` at a version >= 5.3.5; `grep -v '^#' package.json | grep -c 'fast-xml-parser'` >= 1.
    - Articles.astro imports fetchSubstackFeed and TODO_SUBSTACK_URL; grep shows `IS_SENTINEL` and `rel="noopener noreferrer"` present in Articles.astro.
    - config.ts still exports `TODO_SUBSTACK_URL = 'TODO_SUBSTACK_URL'` (value unchanged); its doc block documents the `/feed` and `/subscribe` derivations.
  </acceptance_criteria>
  <done>Homepage recent-articles is fully wired to the RSS pipeline behind the sentinel; build is green and shows honest placeholders; the config seam, fetch/parse utility, and ArticleCard contract are proven end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions deploy + scheduled rebuild workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .planning/phases/03-newsletter-substack-integration/03-RESEARCH.md (§Pattern 4 full YAML; assumption A4 action versions; Pitfall 5 60-day inactivity)
    - .planning/phases/03-newsletter-substack-integration/03-PATTERNS.md (deploy.yml full YAML lines 590-631)
    - astro.config.mjs (site: 'https://rtdpb.github.io', base: '/milan-website/', output: 'static' — confirms GitHub Pages project-site deploy)
  </read_first>
  <action>
    Create .github/workflows/deploy.yml from 03-PATTERNS.md lines 590-631 (the directory does not exist yet — create it). Triggers: `on.push.branches: [ main ]`, `on.schedule` with `- cron: '0 6 * * *'` (daily 06:00 UTC — refreshes the Substack RSS feed for NEWS-03), and `on.workflow_dispatch` (manual re-enable / test). Declare top-level `permissions:` (`contents: read`, `pages: write`, `id-token: write`). Declare a `concurrency:` block with `group: "pages"` and `cancel-in-progress: false` so a scheduled rebuild and a push deploy serialize instead of colliding (concurrency edge, NEWS-01/NEWS-03). Two jobs: `build` (runs-on ubuntu-latest; steps: `actions/checkout@v4` then `withastro/action@v3`) and `deploy` (needs: build; environment github-pages; step `actions/deploy-pages@v4`). Include the YAML comment near the schedule noting GitHub disables scheduled workflows after 60 days of repo inactivity and that workflow_dispatch re-enables them (RESEARCH Pitfall 5).

    Before finalizing action versions: RESEARCH marked `withastro/action@v6` / `actions/checkout@v7` / `actions/deploy-pages@v5` as ASSUMED (A4). Verify current major versions at github.com/withastro/action and github.com/actions/deploy-pages; pin to the confirmed current majors. If verification is not possible in the execution environment, use the widely-current stable majors `actions/checkout@v4`, `withastro/action@v3`, `actions/deploy-pages@v4` and note the version choice in the SUMMARY. This YAML is not executed at build time, so it does not affect the local build verify.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && test -f .github/workflows/deploy.yml && grep -q 'schedule:' .github/workflows/deploy.yml && grep -q 'workflow_dispatch:' .github/workflows/deploy.yml && grep -q 'cancel-in-progress:' .github/workflows/deploy.yml && echo WORKFLOW_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy.yml` exists.
    - It contains a `schedule:` trigger with a daily `cron:` expression, a `workflow_dispatch:` trigger, and a `push:` trigger on `main`.
    - It contains a `concurrency:` block with `cancel-in-progress: false` (serializes cron rebuild vs push deploy — concurrency edge).
    - It declares `permissions:` with `pages: write` and `id-token: write`.
    - Action versions are pinned to confirmed current majors (or the documented stable fallback majors).
  </acceptance_criteria>
  <done>The deploy workflow builds and deploys to GitHub Pages on push, refreshes the RSS feed daily via cron, can be manually triggered, and serializes concurrent runs — this is what makes NEWS-03 "automatic".</done>
</task>

<task type="auto">
  <name>Task 3: Feed-failure fallback + single-item smoke check (deterministic RSS behavior)</name>
  <files>src/lib/rss.ts</files>
  <read_first>
    - src/lib/rss.ts (self — the utility created in Task 1)
    - .planning/phases/03-newsletter-substack-integration/03-RESEARCH.md (Pitfalls 2/4; D-06 failure behavior; §Validation Architecture — manual-only project, no test framework)
  </read_first>
  <action>
    This project has no test framework (package.json has no test script; RESEARCH §Validation Architecture confirms manual-only). Rather than add a framework mid-phase, add a self-contained, throwaway-free Node assertion smoke script inline in the verify command that exercises rss.ts against fixture XML strings — this is the highest-value target RESEARCH flagged (RSS parse → ArticleCard mapping). Do NOT create a persistent test file or add test tooling; the assertions run entirely from the verify command below against the compiled/importable module.

    Confirm (do not re-implement) that fetchSubstackFeed already: (a) returns [] on a thrown fetch/parse error (try/catch), (b) returns [] on !res.ok, (c) normalizes a single `<item>` object to an array so a one-post feed maps to exactly one ArticleCard (Pitfall 2), and (d) reads item['content:encoded'] for readTime. If any of (a)-(d) is missing after Task 1, fix it here. The observable guarantees this task locks in: empty/unreachable feed → 0 items (caller falls back to placeholders, D-06); single-item feed → 1 card (no crash); the mapping is deterministic (same XML in → same ArticleCard out, verifiable by running the parse twice and deep-comparing).

    Keep rss.ts free of side effects other than `console.warn` on failure and the outbound `fetch`.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && node --input-type=module -e "import{XMLParser}from'fast-xml-parser';const p=new XMLParser({ignoreAttributes:false,attributeNamePrefix:'@_',removeNSPrefix:false});const one='<rss><channel><item><title>A</title><link>https://x/1</link><pubDate>Mon, 04 Aug 2026 10:00:00 GMT</pubDate><description>hi</description><content:encoded>full body</content:encoded></item></channel></rss>';const r=p.parse(one);const raw=r?.rss?.channel?.item??[];const items=Array.isArray(raw)?raw:[raw];if(items.length!==1)throw new Error('single-item not normalized to 1');if(items[0]['content:encoded']!=='full body')throw new Error('content:encoded not accessible with removeNSPrefix:false');console.log('RSS_SMOKE_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The Node smoke assertion prints `RSS_SMOKE_OK` (single `<item>` normalizes to a 1-element array; `item['content:encoded']` is accessible → confirms Pitfalls 1 & 2 are handled).
    - rss.ts's fetchSubstackFeed returns `[]` (not throw) on both `!res.ok` and a thrown fetch/parse error (grep shows a `try` and a `catch` and at least one `return []`).
    - `npm run build` still exits 0 (no regression from Task 1).
  </acceptance_criteria>
  <done>RSS parsing is proven deterministic and crash-safe for the empty, single-item, and failure edges (NEWS-01/NEWS-03 empty + idempotency edges); the D-06 fallback path is confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Substack RSS feed → build process | Untrusted third-party XML content enters the Node build at fetch time |
| Substack post URLs / publication home → user browser | External navigation from rendered anchors (new tab) |
| GitHub Actions runner → GitHub Pages | Build artifact deployed to public CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Spoofing | Article anchors (`target="_blank"` to Substack post URL + publication home) | low | mitigate | Every `target="_blank"` anchor carries `rel="noopener noreferrer"` (tab-napping). Enforced in Articles.astro acceptance criteria via grep. |
| T-3-02 | Tampering | RSS `content:encoded` / `description` rendered in cards | medium | mitigate | Never `set:html` RSS content. Excerpt derives from `description` stripped with `.replace(/<[^>]+>/g, ' ')`; `content:encoded` used only for word-count. Acceptance criterion greps that rss.ts contains no `set:html`. |
| T-3-03 | Elevation of Privilege | Build-time `fetch()` target (SSRF) | low | mitigate | Feed URL is derived solely from `TODO_SUBSTACK_URL` in static config.ts — never from user input. Sentinel guard prevents any fetch while the value is the placeholder. |
| T-3-04 | Denial of Service | RSS fetch failure / Substack outage during build | low | mitigate | try/catch + `res.ok` check → return [] → D-06 placeholder fallback; build never fails on feed error. |
| T-3-05 | Tampering | `fast-xml-parser` CVE-2026-25896 (entity-encoding XSS) | low | mitigate | Pin `fast-xml-parser@^5.11.0` (>= 5.3.5 patched); parsed output only used as plain text (no set:html) so not exploitable in this context regardless. |
| T-3-SC | Tampering | npm install of fast-xml-parser | high | mitigate | Package legitimacy audit completed in RESEARCH (§Package Legitimacy Audit): SUS flag assessed as false positive (8yr old, 69M weekly downloads, patched). RESEARCH explicitly instructs no human checkpoint. Pinned to a known-good version. |
</threat_model>

<verification>
- `npm run build` (astro check + astro build) exits 0 with the sentinel in place; homepage shows 3 placeholder cards.
- rss.ts smoke assertion prints `RSS_SMOKE_OK` (single-item + namespaced-field handling).
- deploy.yml present with push + schedule + workflow_dispatch + concurrency.
- No `set:html` and no runtime browser fetch introduced anywhere.
- All external anchors carry `rel="noopener noreferrer"`.
</verification>

<success_criteria>
The homepage recent-articles pipeline is proven end-to-end behind the sentinel: config seam → build-time fetch → XML parse → ArticleCard → static HTML, plus the CI cron that keeps it fresh. Build is green and renders honest placeholders (current live state). Contributes to NEWS-03.
</success_criteria>

<output>
Create `.planning/phases/03-newsletter-substack-integration/03-01-SUMMARY.md` when done.
</output>
