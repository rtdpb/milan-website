---
phase: 04-internationalization-english-content
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/layout/Footer.astro
  - src/components/forms/ContactForm.astro
  - src/components/ui/PlaceholderBadge.astro
  - src/pages/coaching.astro
  - src/pages/spreker.astro
  - src/pages/mijn-verhaal.astro
  - src/pages/nieuwsbrief.astro
  - src/pages/contact.astro
  - src/pages/en/coaching.astro
  - src/pages/en/speaking.astro
  - src/pages/en/about.astro
  - src/pages/en/newsletter.astro
  - src/pages/en/contact.astro
autonomous: true
requirements: [I18N-01, I18N-02]
user_setup: []

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All five remaining pages exist in English at their translated slugs: /en/coaching, /en/speaking, /en/about, /en/newsletter, /en/contact — each with English copy and <html lang=\"en\"> (I18N-02, D-06)."
    - "The NL/EN switch on every page (NL and EN) lands on the equivalent page in the other locale via the slug map, including the translated slugs (I18N-01, D-07)."
    - "Every page's <head> emits correct hreflang alternates that cross-link the matching NL and EN slugs (e.g. /spreker ↔ /en/speaking) using nlSlug/enSlug overrides (SEO criterion 3)."
    - "No source file under src/ still imports i18n/nl directly — every string comes through getStrings(Astro.currentLocale) (I18N-02)."
    - "The EN contact form carries English labels and options but its ?type= deep-links and Web3Forms routing are unchanged, still reaching the one inbox (D honesty; no new input surface)."
  artifacts:
    - src/pages/en/coaching.astro
    - src/pages/en/speaking.astro
    - src/pages/en/about.astro
    - src/pages/en/newsletter.astro
    - src/pages/en/contact.astro
  key_links:
    - "BaseHead nlSlug/enSlug overrides connect translated-slug NL and EN pages so hreflang cross-links are correct (spreker↔speaking, mijn-verhaal↔about, nieuwsbrief↔newsletter)."
    - "EN internal CTAs use getRelativeLocaleUrl('en', <en-slug>) so cross-page links stay inside the EN locale and base-path-safe."
    - "ContactForm reads getStrings(Astro.currentLocale) for labels while keeping the Web3Forms action and ?type= values unchanged."
---

<user_story>
**As a** English-reading founder browsing Milan's site,
**I want to** open any page — coaching, speaking, his story, the newsletter, or contact — in English and move between them without falling back to Dutch,
**so that** I can fully evaluate his offer and get in touch in a language I read.
</user_story>

<objective>
Expand the proven homepage slice to the remaining five pages: refactor the last components that still hardcode `nl` (Footer, ContactForm, PlaceholderBadge) and the five NL page files, then emit the five EN page wrappers at their translated slugs (`/en/coaching`, `/en/speaking`, `/en/about`, `/en/newsletter`, `/en/contact`), wire hreflang slug overrides so translated-slug pages cross-link correctly, and carry the D-02 honest labels and English contact-form copy — leaving zero `i18n/nl` imports and a green smoke suite.

Purpose: Complete I18N-02 (all pages translated) and finish I18N-01 (switch works page-to-page across the whole site) on the architecture Plan 01 proved.
Output: A fully bilingual six-page site with correct per-page multi-locale SEO and no remaining Dutch-only pages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-internationalization-english-content/04-CONTEXT.md
@.planning/phases/04-internationalization-english-content/04-RESEARCH.md
@.planning/phases/04-internationalization-english-content/04-PATTERNS.md
@.planning/phases/04-internationalization-english-content/04-VALIDATION.md
@.planning/phases/04-internationalization-english-content/04-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates the five EN page wrappers (Plan 01 created `en.ts`, `utils.ts`, `smoke-i18n.js`, `en/index.astro`):
- `src/pages/en/coaching.astro` → `/en/coaching`
- `src/pages/en/speaking.astro` → `/en/speaking` (NL slug `spreker`)
- `src/pages/en/about.astro` → `/en/about` (NL slug `mijn-verhaal`)
- `src/pages/en/newsletter.astro` → `/en/newsletter` (NL slug `nieuwsbrief`)
- `src/pages/en/contact.astro` → `/en/contact`
- Completes the NL↔EN slug map wiring (`spreker↔speaking`, `mijn-verhaal↔about`, `nieuwsbrief↔newsletter`) end-to-end through hreflang.
</artifacts_this_phase_produces>

<assumption_delta_decision>
Continues Plan 01's **promote** decision: the last three components (Footer, ContactForm, PlaceholderBadge) and all five NL page files migrate from `import { nl }` to `getStrings(Astro.currentLocale)`, completing the singular→plural (nl → active-locale-parameter) transition. Post-plan invariant holds: a project-wide grep for `i18n/nl` outside `utils.ts` returns zero.
</assumption_delta_decision>

<reversibility_notes>
D-06 (translated EN slugs) — rated **reversible** per-route (rename the page file + its slug-map entry + hreflang override). No gate.
</reversibility_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finish the component refactor — Footer, ContactForm, PlaceholderBadge → getStrings (zero remaining nl imports in components)</name>
  <files>src/components/layout/Footer.astro, src/components/forms/ContactForm.astro, src/components/ui/PlaceholderBadge.astro</files>
  <read_first>
    - src/components/layout/Footer.astro (still `import { nl }`; reuses `nav.*` + `footer.*` strings)
    - src/components/forms/ContactForm.astro (still `import { nl }`; data-* bridge passes strings into its script island; Web3Forms action + honeypot + `?type=` logic must stay unchanged)
    - src/components/ui/PlaceholderBadge.astro (still `import { nl }`; reads `common.placeholderLabel`/`common.disabledTooltip`)
    - src/i18n/utils.ts (getStrings), src/i18n/en.ts (en.contact.*, en.footer.*, en.common.*)
    - .planning/phases/04-internationalization-english-content/04-PATTERNS.md (§Shared Component Refactor, §Honesty rule carried forward)
    - .planning/phases/04-internationalization-english-content/04-RESEARCH.md (§Common Pitfall 4; §Architectural Responsibility Map — ContactForm locale row)
  </read_first>
  <behavior>
    - Footer renders NL on root pages and EN on `/en/*` pages from `getStrings(Astro.currentLocale)`.
    - ContactForm renders English labels/options on `/en/contact` and Dutch on `/contact`; the Web3Forms `action`, honeypot, 7-state machine, and `?type=` deep-link values are byte-for-byte unchanged (no new input surface).
    - PlaceholderBadge shows the localized placeholder label / disabled tooltip.
  </behavior>
  <action>
    In each of `Footer.astro`, `ContactForm.astro`, `PlaceholderBadge.astro`: replace `import { nl } from '<depth>/i18n/nl'` with `import { getStrings } from '<depth>/i18n/utils'` + `const t = getStrings(Astro.currentLocale);` (keep the existing relative-path depth), and change every `nl.<key>.*` reference to `t.<key>.*`. In `ContactForm.astro`, feed the localized strings through the SAME existing `data-*` bridge on the `<form>` element that currently passes `nl.contact` into the TypeScript island — do NOT change the Web3Forms `action` URL, the honeypot field, the state-machine logic, or the `?type=algemeen|lezing|coaching` option values (those route to the one inbox regardless of locale). In `Footer.astro`, keep the existing href-or-reason nav shape and active-state logic; only the string source changes. In `PlaceholderBadge.astro`, the honest disabled-placeholder behavior (aria-disabled, tooltip) is unchanged — only the label text comes from `t.common.*` now.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -c "i18n/nl" src/components/layout/Footer.astro` === 0, `grep -c "i18n/nl" src/components/forms/ContactForm.astro` === 0, `grep -c "i18n/nl" src/components/ui/PlaceholderBadge.astro` === 0.
    - `grep -c "getStrings" src/components/forms/ContactForm.astro` ≥ 1.
    - The Web3Forms `action` value and the `?type=` option values in `ContactForm.astro` are unchanged from before this task (diff shows only the string-source swap, not the endpoint/logic).
  </acceptance_criteria>
  <done>The last three shared components are locale-parameterized; only the NL page files remain to migrate before EN pages can be emitted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the five NL page files to getStrings + add hreflang slug overrides for translated-slug pages</name>
  <files>src/pages/coaching.astro, src/pages/spreker.astro, src/pages/mijn-verhaal.astro, src/pages/nieuwsbrief.astro, src/pages/contact.astro</files>
  <read_first>
    - src/pages/coaching.astro, src/pages/spreker.astro, src/pages/mijn-verhaal.astro, src/pages/nieuwsbrief.astro, src/pages/contact.astro (each still `import { nl }`; each renders `nl.<page>.*` and has internal CTAs/hrefs)
    - src/components/layout/BaseHead.astro (nlSlug/enSlug props added in Plan 01 — how overrides feed hreflang)
    - src/layouts/BaseLayout.astro (how it forwards nlSlug/enSlug to BaseHead — confirm the prop path exists from Plan 01; if BaseLayout does not forward these, add the pass-through)
    - src/i18n/utils.ts, src/i18n/en.ts
    - .planning/phases/04-internationalization-english-content/04-PATTERNS.md (§EN page wrappers — nlSlug/enSlug override rule)
    - .planning/phases/04-internationalization-english-content/04-RESEARCH.md (§Pattern 6; §Common Pitfall 3)
  </read_first>
  <behavior>
    - Each NL page renders from `getStrings(Astro.currentLocale)` (resolves `'nl'` at root) — no hardcoded `nl`.
    - The three translated-slug NL pages (`spreker`, `mijn-verhaal`, `nieuwsbrief`) pass `nlSlug`/`enSlug` to BaseLayout so their hreflang `en` alternate points at the correct EN slug (`/en/speaking`, `/en/about`, `/en/newsletter`) rather than a same-name guess.
    - `coaching` and `contact` need no slug override (NL slug == EN slug).
  </behavior>
  <action>
    In each of the five NL page files: replace `import { nl } from '../i18n/nl'` with `import { getStrings } from '../i18n/utils'` + `const t = getStrings(Astro.currentLocale);`, and change `nl.<page>.*` to `t.<page>.*`. Leave the NL internal CTA hrefs as the existing `${import.meta.env.BASE_URL}<dutch-slug>` form (root-locale pages keep Dutch-slug links). For the three translated-slug pages, add explicit `nlSlug`/`enSlug` props on their `<BaseLayout>` call so BaseHead builds correct cross-locale hreflang: `spreker.astro` → `nlSlug="spreker" enSlug="speaking"`; `mijn-verhaal.astro` → `nlSlug="mijn-verhaal" enSlug="about"`; `nieuwsbrief.astro` → `nlSlug="nieuwsbrief" enSlug="newsletter"`. `coaching.astro` and `contact.astro` need no override. If BaseLayout does not already forward `nlSlug`/`enSlug` to BaseHead (verify against Plan 01's edit), add that pass-through in `BaseLayout.astro`.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -rc "i18n/nl" src/pages/coaching.astro src/pages/spreker.astro src/pages/mijn-verhaal.astro src/pages/nieuwsbrief.astro src/pages/contact.astro` returns 0 for every file.
    - `src/pages/spreker.astro` contains `enSlug="speaking"`, `src/pages/mijn-verhaal.astro` contains `enSlug="about"`, `src/pages/nieuwsbrief.astro` contains `enSlug="newsletter"`.
    - Built `dist/spreker/index.html` (or `dist/spreker.html`) contains an `hreflang="en"` alternate whose href ends in `/en/speaking` (and analogously for about/newsletter).
  </acceptance_criteria>
  <done>All NL pages are locale-parameterized with correct hreflang cross-links; the site has zero `i18n/nl` imports outside `utils.ts`, and the EN page wrappers can now be emitted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit the five EN page wrappers at translated slugs + full smoke green + end-of-phase human-verify</name>
  <files>src/pages/en/coaching.astro, src/pages/en/speaking.astro, src/pages/en/about.astro, src/pages/en/newsletter.astro, src/pages/en/contact.astro</files>
  <read_first>
    - src/pages/coaching.astro, src/pages/spreker.astro, src/pages/mijn-verhaal.astro, src/pages/nieuwsbrief.astro, src/pages/contact.astro (the NL analogs — mirror their component trees; note their internal CTA targets)
    - src/pages/en/index.astro (Plan 01's EN wrapper pattern — import depth, getStrings, BaseLayout usage)
    - src/i18n/utils.ts (getRelativeLocaleUrl usage rule; nlToEn), src/i18n/en.ts
    - scripts/smoke-i18n.js (what it asserts)
    - .planning/phases/04-internationalization-english-content/04-PATTERNS.md (§EN page wrappers — the 4 transformation rules + internal-href rule)
    - .planning/phases/04-internationalization-english-content/04-RESEARCH.md (§Pattern 3; §Common Pitfalls 3 and 5)
  </read_first>
  <behavior>
    - `/en/coaching`, `/en/speaking`, `/en/about`, `/en/newsletter`, `/en/contact` each build to `dist/en/<slug>/index.html` (or equivalent) with `lang="en"` and English copy.
    - Internal CTAs on EN pages stay inside the EN locale (e.g. EN coaching's "schedule intro call" points to `/en/contact`, EN mijn-verhaal→about's "read about speaking" points to `/en/speaking`) via `getRelativeLocaleUrl('en', <en-slug>)`.
    - Translated-slug EN pages pass matching `nlSlug`/`enSlug` to BaseLayout for correct hreflang.
    - The EN contact page renders the English form; its Web3Forms routing is unchanged.
    - `npm run build && node scripts/smoke-i18n.js` is green.
  </behavior>
  <action>
    Create each EN page wrapper by mirroring its NL analog's component tree with four transformations (per 04-PATTERNS.md §EN page wrappers): (1) import paths go one level deeper (`'../../…'`); (2) `import { getStrings } from '../../i18n/utils'` + `const t = getStrings(Astro.currentLocale)` (resolves `'en'` automatically); (3) `nl.<page>.*` → `t.<page>.*`; (4) internal CTA hrefs that pointed at Dutch slugs use `getRelativeLocaleUrl('en', <en-slug>)` (never manual `${BASE_URL}en/…` concatenation) — e.g. `/en/coaching`'s CTAs → `getRelativeLocaleUrl('en','contact')` and `getRelativeLocaleUrl('en','about')`; `/en/about` (mijn-verhaal) → `getRelativeLocaleUrl('en','contact')` and `getRelativeLocaleUrl('en','speaking')`; `/en/speaking` → `getRelativeLocaleUrl('en','contact')`. Pass `nlSlug`/`enSlug` on `<BaseLayout>` for the three translated-slug pages (`speaking`: `nlSlug="spreker" enSlug="speaking"`; `about`: `nlSlug="mijn-verhaal" enSlug="about"`; `newsletter`: `nlSlug="nieuwsbrief" enSlug="newsletter"`); `coaching`/`contact` need none. Use each page's English `pageTitle`/`pageDesc` from `en.ts` for `<BaseLayout>`. Do not alter the shared section components (already locale-aware from Plan 01) or the Web3Forms endpoint. After building, run the smoke script and resolve any failure.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build && node scripts/smoke-i18n.js</automated>
    <human-check>
      Run `npm run dev` and confirm the complete bilingual site (end-of-phase verification):
      1. On homepage `/`, click the language switch → land on `/en/` in English.
      2. From `/en/`, navigate to Coaching, Speaking, About, Newsletter, Contact via the nav → each stays English and uses the translated slug (`/en/speaking`, `/en/about`, `/en/newsletter`).
      3. On an EN subpage, click the switch → land on the equivalent Dutch page (e.g. `/en/speaking` → `/spreker`), not the homepage.
      4. The honest "written in Dutch" note (D-02) reads naturally (not an apology) on the EN newsletter + homepage newsletter sections.
      5. Numbers are verbatim in EN ("12+ years", "180 employees", "9 markets"); testimonial names unchanged.
      6. Skim EN copy for tone/quality — flag anything for Milan (draft per D-03, not a blocker).
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and `node scripts/smoke-i18n.js` prints `I18N_SMOKE_OK`.
    - All five files exist: `dist/en/coaching`, `dist/en/speaking`, `dist/en/about`, `dist/en/newsletter`, `dist/en/contact` each produce an HTML file containing `lang="en"`.
    - Project-wide, `grep -rl "i18n/nl" src/ | grep -v "src/i18n/utils.ts"` returns no files (every direct `nl` import is gone except the one inside `utils.ts`).
    - `dist/en/speaking/index.html` (or equivalent) contains an `hreflang="nl"` alternate whose href ends in `/spreker`, and `hreflang="en"` ending in `/en/speaking`.
    - `dist/en/coaching/index.html` contains an internal link to `/en/contact` (EN-locale CTA, not `/contact`).
  </acceptance_criteria>
  <done>All six pages exist in both locales, cross-linked with correct hreflang and locale-internal CTAs; the smoke suite is green; the site is fully bilingual with zero Dutch-only pages remaining.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor → static HTML | All six EN pages are build-time static; no server-side locale logic. |
| EN contact form → Web3Forms | Unchanged from Phase 2; the EN form reuses the same hardened endpoint, honeypot, and validation — only labels are localized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Spoofing | EN-page internal CTAs / switch | low | mitigate | Hrefs built via `getRelativeLocaleUrl` from fixed slugs; no user-supplied URL reflected — no open redirect. |
| T-04-05 | Tampering | EN ContactForm labels | low | accept | Only static label strings change; the Web3Forms action, honeypot, and input validation from Phase 2 are unchanged (no new input surface). |
| T-04-06 | Information Disclosure | hreflang absolute URLs | low | accept | `getAbsoluteLocaleUrl` emits only public site URLs; nothing sensitive exposed. |

**No high-severity threat is introduced by this phase.** No new external API, auth, database, or payment surface; the EN contact form reuses the existing hardened Web3Forms integration unchanged. ASVS L1 block-on-`high` gate: PASS.
</threat_model>

<verification>
- `npm run build` exits 0; `node scripts/smoke-i18n.js` prints `I18N_SMOKE_OK`.
- Project-wide grep confirms zero `i18n/nl` imports outside `src/i18n/utils.ts`.
- Every translated-slug page cross-links its NL/EN counterpart correctly via hreflang.
- End-of-phase human-verify confirms the switch, EN navigation, and honest D-02 note in a browser.
</verification>

<success_criteria>
- All six pages exist in NL and EN; EN pages use the translated slugs (D-06).
- The NL/EN switch works page-to-page across the whole site, URL-path-persistent (I18N-01).
- Per-page multi-locale SEO is correct: hreflang cross-links matching NL/EN slugs, localized metadata (criterion 3).
- Quality EN translations cover the homepage and all pages (I18N-02), flagged as Milan-review draft (D-03).
</success_criteria>

<output>
Create `.planning/phases/04-internationalization-english-content/04-02-SUMMARY.md` when done.
</output>
