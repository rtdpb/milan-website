---
quick_id: 260824-jd5
type: quick
autonomous: true
files_modified:
  - src/config.ts
  - src/components/layout/Nav.astro
  - src/components/layout/Footer.astro
  - src/components/forms/ContactForm.astro
  - src/pages/nieuwsbrief.astro
  - src/components/sections/Testimonials.astro
  - src/pages/coaching.astro
  - src/pages/en/coaching.astro
  - src/i18n/nl.ts
  - src/i18n/en.ts
estimate:
  tokens: 90000
  raw_tokens: 60000
  tasks: 4
  confidence: med
must_haves:
  truths:
    - LinkedIn icon in Nav (desktop + mobile) and Footer is a real clickable anchor to Milan's profile, opening in a new tab with rel=noopener noreferrer
    - Contact form has no subject dropdown, no "mail direct" fallback line, plus optional Naam bedrijf + Telefoonnummer fields and a required privacy-consent checkbox linking to the privacy page
    - Contact submit button is a yellow pill CTA with an arrow icon matching the site's other prominent CTAs
    - Nieuwsbrief hero has an inline email subscribe form (same pattern as Newsletter.astro) instead of a single button
    - Testimonials show real photos for Yang Soo Kloosterhof and Ruud Koornstra; Oranjewoud stays a monogram
    - Coaching page (NL + EN) uses a full-bleed background-image header mirroring spreker's .shero, using milan-networking.jpg
  artifacts:
    - src/config.ts (real LinkedIn URL)
    - src/components/forms/ContactForm.astro (overhauled)
    - src/pages/nieuwsbrief.astro (hero email form)
    - src/components/sections/Testimonials.astro (photo avatars)
    - src/pages/coaching.astro + src/pages/en/coaching.astro (full-bleed header)
  key_links:
    - Nav/Footer LinkedIn anchors read TODO_LINKEDIN_URL sentinel to decide disabled-span vs real anchor
    - Privacy checkbox link href follows Footer.astro pattern (NL ${base}privacybeleid / EN getRelativeLocaleUrl('en','privacy'))
    - New i18n keys added to BOTH nl.ts and en.ts with identical object shapes (as const type-check)
---

<objective>
Apply feedback round 2 to the Astro static site: wire the real LinkedIn URL, overhaul the contact form (remove dropdown + mailto-fallback line, add optional company/phone fields, add a required privacy-consent checkbox, restyle submit to a yellow pill CTA), add an inline email subscribe form to the nieuwsbrief hero, add real testimonial photos, and convert the coaching page header (NL + EN) to a full-bleed background-image header.

Purpose: Close the client's second feedback batch on the homepage/subpages.
Output: 10 modified files; `npm run build` passes (astro check + build, type-checks the `as const` i18n objects).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@src/config.ts
@src/components/forms/ContactForm.astro
@src/components/sections/Newsletter.astro
@src/pages/nieuwsbrief.astro
@src/components/sections/Testimonials.astro
@src/pages/coaching.astro
@src/pages/en/coaching.astro
@src/pages/spreker.astro
@src/components/layout/Nav.astro
@src/components/layout/Footer.astro
@src/i18n/nl.ts
@src/i18n/en.ts

Key constraints (from CLAUDE.md + planning_context):
- NL default at root, EN under /en/*. i18n strings in src/i18n/nl.ts + en.ts are `as const` — keep object shapes IDENTICAL between locales. Every new NL string needs an EN counterpart in the same position.
- Honesty rules: no fake success/network states.
- Yellow accent var(--color-accent) = #FFDD11 used sparingly.
- Design tokens only: var(--color-*), --space-*, --text-*, --radius-*, --duration-*, --ease-*. Respect prefers-reduced-motion.
- Verify EVERY task with `npm run build`.

IMPORTANT discovery finding: Nav.astro AND Footer.astro currently render LinkedIn as a HARDCODED aria-disabled <span> — they do NOT read the TODO_LINKEDIN_URL sentinel today. So Task 1 must actually rewire all three LinkedIn spots (Nav desktop icon, Nav mobile icon, Footer icon) to render a real <a> when the constant is no longer the sentinel.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: LinkedIn URL + real anchors in Nav & Footer</name>
  <files>src/config.ts, src/components/layout/Nav.astro, src/components/layout/Footer.astro</files>
  <action>
In src/config.ts, change the TODO_LINKEDIN_URL export value from 'TODO_LINKEDIN_URL' to 'https://www.linkedin.com/in/milan-van-der-meulen-04b92643/'. Type it as `string` (like TODO_SUBSTACK_URL on line 36) so the `=== 'TODO_LINKEDIN_URL'` sentinel comparison stays a valid comparison. Do NOT touch any other TODO_* constant.

In Nav.astro: import TODO_LINKEDIN_URL from '../../config' (add to the existing config import). Compute `const linkedinLive = TODO_LINKEDIN_URL !== 'TODO_LINKEDIN_URL';`. For BOTH the desktop `.linkedin-icon` span (~line 152) and the mobile `.mobile-nav__linkedin` span (~line 270): when linkedinLive render a real `<a href={TODO_LINKEDIN_URL} target="_blank" rel="noopener noreferrer" class="linkedin-icon" aria-label={t.nav.linkedin} title={t.nav.linkedin}>` wrapping the same inline SVG + sr-only label; when not live keep the existing aria-disabled span exactly as-is. Use an Astro conditional (`{linkedinLive ? (<a …>…</a>) : (<span …>…</span>)}`) so the SVG markup is shared/duplicated per branch. Remove the stale "TODO: wire href" comment block since it is now wired. Keep the `.linkedin-icon` / `.mobile-nav__linkedin` CSS; the anchor reuses the same class so styling carries over (verify cursor is not `not-allowed` on the live anchor — add a small rule so the live `a.linkedin-icon` / `a.mobile-nav__linkedin` uses `cursor: pointer` and normal opacity if the base rule sets not-allowed).

In Footer.astro: import TODO_LINKEDIN_URL from '../../config' (add to existing config import). Compute the same `linkedinLive`. For the `.ft__linkedin` span (~line 87) render a real `<a href={TODO_LINKEDIN_URL} target="_blank" rel="noopener noreferrer" class="ft__linkedin" aria-label={t.nav.linkedin} title={t.nav.linkedin}>` wrapping the same SVG + sr-only label when live; else the existing aria-disabled span. Add a rule so the live `a.ft__linkedin` uses `cursor: pointer` (base `.ft__linkedin` sets `cursor: not-allowed`) and picks up a hover color (e.g. `a.ft__linkedin:hover { color: var(--color-accent); border-color: var(--line-on-dark-strong); }`).

No i18n changes needed (reuse t.nav.linkedin). No new strings.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build</automated>
  </verify>
  <done>Build passes. TODO_LINKEDIN_URL holds the real profile URL. Nav (desktop + mobile) and Footer render a real target=_blank rel=noopener noreferrer anchor to the profile; the disabled-span branch remains only as the sentinel fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Contact form overhaul + i18n</name>
  <files>src/components/forms/ContactForm.astro, src/i18n/nl.ts, src/i18n/en.ts</files>
  <action>
i18n first (BOTH src/i18n/nl.ts AND src/i18n/en.ts, contact block — keep shapes identical). In the `contact:` object:
- REMOVE keys: labelOnderwerp, optionAlgemeen, optionLezing, optionCoaching, mailtoFallbackPre — from BOTH locales.
- ADD keys (both locales, same order): labelBedrijf (NL 'Naam bedrijf' / EN 'Company name'); labelTelefoon (NL 'Telefoonnummer' / EN 'Phone number'); privacyConsentPre (NL 'Ik ga akkoord met het ' / EN 'I agree to the '); privacyConsentLink (NL 'privacybeleid' / EN 'privacy policy'). (No trailing-text key needed — the sentence ends at the link.)

In ContactForm.astro:
1. Update the destructure from t.contact: drop labelOnderwerp, optionAlgemeen, optionLezing, optionCoaching, mailtoFallbackPre; add labelBedrijf, labelTelefoon, privacyConsentPre, privacyConsentLink.
2. Compute the privacy href using the Footer.astro pattern: import getRelativeLocaleUrl from 'astro:i18n'; `const isEn = (Astro.currentLocale ?? 'nl') === 'en'; const base = import.meta.env.BASE_URL; const privacyHref = isEn ? getRelativeLocaleUrl('en','privacy') : `${base}privacybeleid`;` (internal link — same tab, no target=_blank).
3. REMOVE the whole `.form-group` block containing the `#contact-onderwerp` <select> (lines ~69-83). Remove the `data-l-subject` attribute on the <form>.
4. ADD an optional Naam bedrijf field after the Naam field (or after Email — your call, keep required Naam/Email/Bericht order sensible): a `.form-group` with `<label for="contact-bedrijf" class="form-label">{labelBedrijf}</label>` (NO asterisk) and `<input id="contact-bedrijf" type="text" name="company" autocomplete="organization" class="form-input" />` (no required, no aria-required). Add `data-l-company={labelBedrijf}` on the form.
5. ADD an optional Telefoonnummer field similarly: label {labelTelefoon} (no asterisk), `<input id="contact-telefoon" type="tel" name="phone" autocomplete="tel" class="form-input" />`. Add `data-l-phone={labelTelefoon}` on the form.
6. ADD a required privacy-consent checkbox as the last field before the submit button: a `.form-consent` row with `<input type="checkbox" id="contact-privacy" name="privacy" required aria-required="true" class="form-consent__box" />` and a `<label for="contact-privacy" class="form-consent__label">{privacyConsentPre}<a href={privacyHref} class="form-consent__link">{privacyConsentLink}</a></label>`. Horizontal row, touch-friendly (checkbox ~20px, min tap target). The existing JS `f.checkValidity()` naturally enforces the required checkbox.
7. REMOVE the `.contact-mailto-fallback` <p> block entirely (lines ~143-147) and its CSS rules (.contact-mailto-fallback, .contact-mailto-link, .contact-mailto-link:focus-visible). Keep the native `action="mailto:…"` form and the aria-live result region — the no-JS path still opens a mail client via the native mailto action (acceptable per client). CONTACT_EMAIL import stays (still used by the form action + JS mailto builder).
8. In the <script>: remove `selectEl` and the whole query-param `?type=` pre-selection block; remove `lSubject`/`data-l-subject` usage; remove `topicText` and the topic line from the body builder. subject becomes just `subjectBase` (drop the `+ (topicText ? ' — ' + topicText : '')`). Add company + phone to the mailto body builder ONLY when non-empty, using the new labels via data attributes (read `f.dataset.lCompany`, `f.dataset.lPhone`): e.g. read `String(fd.get('company')||'')` and `String(fd.get('phone')||'')`; append `lCompany + ': ' + company + '\n'` and `lPhone + ': ' + phone + '\n'` only if the value is truthy, positioned in the body after email and before message.
9. SUBMIT BUTTON: restyle `.contact-submit` to a yellow pill CTA matching spreker's `.shero__cta` / Samenwerken `.sw__cta`: pill radius (var(--radius-pill)), background var(--color-accent), color var(--color-accent-ink), height ~54px, padding-inline var(--space-xl), inline-flex + gap, font-weight 700, no border; hover translateY(-2px) + background var(--color-accent-dark). Add an inline arrow SVG inside the <button> after the {ctaSubmit} text (same 18x18 arrow SVG used in spreker.astro / Newsletter.astro), with `.contact-submit svg` transition + hover translateX(3px). Keep it `<button type="submit">`. Remove the old `.btn .btn--primary` classes from the button if they conflict (or keep but ensure the pill styles win). Wrap all transitions in `@media (prefers-reduced-motion: reduce)` disabling them.

Do NOT change the coaching/spreker CTAs that link to /contact?type=coaching etc. — that param is now simply ignored by the form (fine).
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build</automated>
  </verify>
  <done>Build passes (i18n shapes match across locales). Contact form has no dropdown and no mailto-fallback line; optional company + phone fields present; a required privacy checkbox links to the privacy page (NL /privacybeleid, EN /en/privacy) in the same tab; submit is a yellow pill with arrow. The JS mailto body includes company/phone only when filled and no longer references topic.</done>
</task>

<task type="auto">
  <name>Task 3: Nieuwsbrief hero email form + Testimonials photos</name>
  <files>src/pages/nieuwsbrief.astro, src/components/sections/Testimonials.astro, src/i18n/nl.ts, src/i18n/en.ts</files>
  <action>
NIEUWSBRIEF HERO (src/pages/nieuwsbrief.astro):
Replace the single `<Button>` inside `.nieuwsbrief-hero__ctas` (lines ~76-85) with the inline email-form pattern from Newsletter.astro. Add a labelEmail string to the nieuwsbrief i18n block (see below) and reuse the page's existing IS_SENTINEL + subscribeUrl (already computed at top: `subscribeUrl = ${TODO_SUBSTACK_URL}/subscribe`, but strip a trailing slash to match Newsletter: use `${TODO_SUBSTACK_URL.replace(/\/$/,'')}/subscribe`). Render:
- when IS_SENTINEL: keep an honest disabled affordance (a disabled span/button styled like the CTA, aria-disabled, with a short "binnenkort via Substack" note) — mirror Newsletter.astro's sentinel branch. Substack is live so this branch will not render, but keep it for correctness/parity.
- when live: `<form class="nieuwsbrief-hero__form" action={subscribeUrl} method="get" target="_blank">` with an sr-only `<label for="nb-hero-email">{labelEmail}</label>`, `<input id="nb-hero-email" type="email" name="email" required placeholder={labelEmail} autocomplete="email" class="nieuwsbrief-hero__input" />` and a `<button type="submit" class="nieuwsbrief-hero__cta">{t.nieuwsbrief.cta}<svg …arrow…/></button>`.
Add a small <script> mirroring Newsletter.astro that fires `plausible('Newsletter: Subscribe')` on the form's submit event (guard `typeof (window as any).plausible === 'function'`). Style the input + yellow submit consistent with the site (pill input + yellow pill button, tokens only, prefers-reduced-motion guard) — you may closely adapt Newsletter.astro's `.nl__form/.nl__input/.nl__cta` CSS under the new class names. Keep the `<Newsletter/>` component lower on the page unchanged.

i18n for nieuwsbrief (BOTH nl.ts + en.ts, nieuwsbrief block, identical shape): ADD `labelEmail` — NL 'Mailadres' / EN 'Email address'. Place it in the same position in both locales.

TESTIMONIALS (src/components/sections/Testimonials.astro):
Photos exist at src/assets/photos/testimonials/yang-soo-kloosterhof.jpg and ruud-koornstra.jpg. 
- Import the two images at the top and `import { Image } from 'astro:assets';`.
- Build a map keyed by the EXACT testimonial name string: `const photoMap: Record<string, ImageMetadata> = { 'Yang Soo Kloosterhof': yangSooPhoto, 'Ruud Koornstra': ruudPhoto };` (import the ImageMetadata type from 'astro' if needed, or use a looser typed record). Names must match the i18n testimonial `name` values verbatim — do NOT change the i18n strings; verify the exact names by reading t.testimonials.items in nl.ts (and en.ts if names differ per locale — they are proper names so should be identical, but confirm).
- In the avatar markup (`.tst__avatar` span, ~line 54): when `photoMap[item.name]` exists, render `<Image src={photoMap[item.name]} width={88} height={88} alt="" class="tst__avatar-img" />` filling the circle (object-fit: cover, width/height 100%, border-radius: 50% via the parent's overflow or on the img). Else keep the existing `.tst__avatar-monogram` span. Keep `aria-hidden="true"` on the `.tst__avatar` wrapper (name is in the figcaption). The third testimonial ('Oranjewoud Export Academy') has no photo → monogram fallback.
- Add CSS: `.tst__avatar-img { width: 100%; height: 100%; object-fit: cover; border-radius: 50%; display: block; }` and ensure `.tst__avatar` has `overflow: hidden` so the image clips to the circle. Keep the carousel + semantics intact.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build</automated>
  </verify>
  <done>Build passes. Nieuwsbrief hero shows an inline email subscribe form (yellow submit + arrow) posting to Substack /subscribe with the Plausible goal; the lower Newsletter section is unchanged. Testimonials render photos for Yang Soo Kloosterhof + Ruud Koornstra and keep the monogram for Oranjewoud. New labelEmail key added to both locales.</done>
</task>

<task type="auto">
  <name>Task 4: Coaching full-bleed header (NL + EN mirrored)</name>
  <files>src/pages/coaching.astro, src/pages/en/coaching.astro</files>
  <action>
Convert the coaching header from the split `.chero` (title left + portrait right) to a full-bleed background-image header mirroring spreker.astro's `.shero`. Apply IDENTICAL changes to BOTH src/pages/coaching.astro and src/pages/en/coaching.astro (keep them mirrored; only strings/href helpers differ per file as they already do).

1. Change the hero image import: from `import heroSrc from '../assets/photos/milan-story-portrait.jpg'` to `import heroSrc from '../assets/photos/milan-networking.jpg'` (EN file: `'../../assets/photos/milan-networking.jpg'`). milan-networking.jpg exists in src/assets/photos/.
2. Replace the entire `<header class="chero">…</header>` block (the split-header markup, lines ~44-62 NL / ~30-48 EN) with the spreker `.shero` structure:
```
<header class="chero anim-kenburns">
  <Picture src={heroSrc} formats={['avif','webp']} widths={[768,1200,1600,2000,2400]} sizes="100vw" alt={c.heroImageAlt} priority class="chero__img" />
  <div class="chero__scrim"></div>
  <div class="chero__inner">
    <p class="chero__eyebrow">{c.eyebrow}</p>
    <h1 class="chero__title">{c.heroTitle}</h1>
  </div>
</header>
```
(No CTA in the header — coaching's CTAs live lower. Keep eyebrow + title only. Do NOT reference a `networkingAlt` key that may not exist — reuse `c.heroImageAlt`.)
3. Replace the old `.chero`/`.chero__inner`/`.chero__text`/`.chero__eyebrow`/`.chero__title`/`.chero__figure`/`.chero__img` CSS with the full-bleed variant adapted from spreker's `.shero*` (rename to `.chero*`):
   - `.chero { position: relative; width: 100%; min-height: clamp(420px, 66vh, 620px); display: flex; align-items: flex-end; overflow: clip; }`
   - `.chero__img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; object-position: 50% 30%; }`
   - `.chero__scrim { position: absolute; inset: 0; background: linear-gradient(180deg, rgba(16,18,21,0.15) 0%, rgba(16,18,21,0.35) 45%, rgba(16,18,21,0.85) 100%); }`
   - `.chero__inner { position: relative; width: 100%; max-width: var(--max-width); margin-inline: auto; padding: var(--gutter); padding-bottom: clamp(var(--space-xl), 4vw, var(--space-3xl)); display: flex; flex-direction: column; gap: var(--space-md); align-items: flex-start; }`
   - `.chero__eyebrow { font-size: var(--text-xs); font-weight: 700; letter-spacing: 0.14em; text-transform: uppercase; color: var(--color-accent); margin: 0; }`
   - `.chero__title { font-size: clamp(2.4rem, 1.2rem + 4vw, 4.25rem); line-height: 1.02; letter-spacing: -0.03em; font-weight: 730; color: var(--color-dark-text); margin: 0; max-width: 18ch; text-wrap: balance; }`
   Remove the now-unused `@media (min-width: 820px) { .chero__inner { grid-template-columns … } }` split rules and the `.chero__figure`/`.chero__text` rules.
4. Keep the rest of the coaching page (PressStrip, intro, packages, reviews, traject, faq, cta, Newsletter) untouched.
Confirm both files remain byte-parallel in the header region (aside from EN vs NL import depth `../` vs `../../`).
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npm run build</automated>
  </verify>
  <done>Build passes. Both /coaching and /en/coaching render a full-bleed background-image header (milan-networking.jpg) with a dark scrim and bottom-left overlaid eyebrow + h1, mirroring the spreker header. The old split portrait header markup + .chero split CSS are gone.</done>
</task>

</tasks>

<verification>
- `npm run build` passes after each task (astro check + build; type-checks the `as const` i18n objects — shape mismatches between nl.ts/en.ts fail the build).
- Manual/visual spot-check after build: LinkedIn icons link out; contact form has company/phone/consent + pill submit and no dropdown/fallback line; nieuwsbrief hero has an email field; testimonials show 2 photos + 1 monogram; coaching header is full-bleed on NL and EN.
- Honesty rules preserved (no fake success states). Yellow used only on CTAs/accents.
</verification>

<success_criteria>
All 4 tasks complete, `npm run build` passes (0 errors), NL/EN i18n shapes identical, feedback items 1-5 delivered, items 6 (Articles) and 7 (Story signature) untouched.
</success_criteria>

<output>
Create `.planning/quick/260824-jd5-feedbackronde-2-linkedin-url-contactform/260824-jd5-SUMMARY.md` when done.
</output>
