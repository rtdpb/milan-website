---
phase: quick-260821-m2d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - src/styles/global.css
  - src/styles/tokens.css
  - src/components/ui/ScrollProgress.astro
  - src/components/sections/Timeline.astro
  - src/pages/index.astro
  - src/pages/en/index.astro
  - src/i18n/nl.ts
  - src/i18n/en.ts
  - src/components/ui/GrowthCurve.astro
autonomous: false
requirements: [M2D-LIVELY]
estimate:
  tokens: 62000
  raw_tokens: 41000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "A thin accent (#FFDD11) progress bar sits fixed at the very top of the viewport and fills left-to-right as the user scrolls the homepage."
    - "The 'Het verhaal in cijfers' stats/timeline band is visible on the homepage between Story and Newsletter, and its numbers (9 markten, ~180 medewerkers, ~1M zonnepanelen) count up from 0 when scrolled into view."
    - "At least one self-made SVG line-art figure (growth curve) draws itself via stroke-dashoffset when scrolled into view, thin-stroke and editorial with yellow used only as a single accent."
    - "Scroll reveals across the homepage have directional/scale variants (from-left, from-right, scale-in) and a subtle parallax lift, not only the single fade+rise."
    - "Under prefers-reduced-motion every new animation is fully bypassed: progress bar static/hidden, counters show final values, SVGs show final drawn state, reveals show final position — no motion runs and no content is ever hidden."
  artifacts:
    - src/components/ui/ScrollProgress.astro
    - src/components/ui/GrowthCurve.astro
    - src/components/sections/Timeline.astro
    - src/layouts/BaseLayout.astro
    - src/styles/global.css
  key_links:
    - "ScrollProgress mounted once in BaseLayout above Nav so it appears on every page including homepage."
    - "Timeline imported and rendered in index.astro (and en/index.astro) between Story and Newsletter — it is currently built but unmounted."
    - "New reveal variants driven by data-anim-variant + the existing [data-anim] IntersectionObserver in BaseLayout (no second observer, no new dependency)."
    - "Count-up '~1M' value driven by a data-countup-max/format seam so the counter animates 0→final without ticking through a million integers."
---

<objective>
Make the Dutch homepage feel alive and distinctive through a single coherent scroll narrative — a top scroll-progress bar, richer directional/scale/parallax reveals, building counters on Milan's real Soly numbers, and self-drawing editorial SVG line-art — while extending the existing motion primitives rather than duplicating them, and fully honouring prefers-reduced-motion.

Purpose: The client finds the current homepage too static/boring. This is a motion + illustration enhancement pass over the EXISTING homepage. Do NOT rewrite copy/content.

Output: ScrollProgress.astro + GrowthCurve.astro (new), extended reveal system (global.css + BaseLayout observer), extended count-up (BaseLayout), Timeline mounted on the homepage with a third ~1M zonnepanelen stat and a draw-on-scroll growth curve, plus new motion tokens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

# Existing motion primitives — MATCH these patterns exactly, do not invent parallel systems:
@src/layouts/BaseLayout.astro
@src/styles/global.css
@src/styles/tokens.css
@src/components/ui/StickyCTA.astro

# The stats/timeline band already exists but is NOT mounted on the homepage — this is the editorial stats strip the brief asks for:
@src/components/sections/Timeline.astro
@src/pages/index.astro
@src/i18n/nl.ts

# Reference only (older reveal island; the canonical system is [data-anim] in BaseLayout):
@src/components/ui/RevealOnScroll.astro
</context>

<key_findings>
Read before planning tasks — these change the naive approach:

1. **Canonical reveal system = `[data-anim]`** in `global.css` + the IIFE `<script>` at the bottom of `BaseLayout.astro`. It fades+rises (translateY 22px→0, --duration-slow) and staggers children via inline `--i` (`transition-delay: calc(var(--i) * 90ms)`). `RevealOnScroll.astro` (`[data-reveal]`) is an OLDER parallel component — do NOT extend it, do NOT add a second observer. Extend the `[data-anim]` system.

2. **Count-up already exists** (`[data-countup]` in the BaseLayout script, threshold 0.6, cubic ease-out over 1200ms). Hero stats (12+, 180, 9) already use it. It does `parseFloat(data-countup)` then renders `Math.round(eased*target)+suffix`. A raw `data-countup="1000000"` would tick through a million integers — the plan adds a small formatting seam for the ~1M value.

3. **`Timeline.astro` is built but UNMOUNTED.** It is a navy documentary band with count-up stats and a static 9-market dot-map SVG. It has i18n copy in `nl.timeline` / `en.timeline` (9 markten, 180 medewerkers). It contains NO ~1M zonnepanelen stat yet. Mounting it IS the "editorial stats strip near the Story/verhaal section" the brief requests.

4. **No scroll progress bar exists.** Net-new. Mount once in BaseLayout so it covers every page.

5. **Motion tokens** live in `tokens.css`: `--duration-fast 160ms`, `--duration-base 340ms`, `--duration-slow 760ms`, `--ease-out cubic-bezier(0.16,1,0.3,1)`, `--ease-in-out cubic-bezier(0.65,0,0.35,1)`. Reuse these; add new ones rather than hardcoding.

6. **Both locales:** `src/pages/index.astro` (NL) and `src/pages/en/index.astro` (EN) both assemble the homepage. Timeline must be mounted in both. i18n strings live in `nl.ts` + `en.ts` (same shape contract, `DeepWiden`).
</key_findings>

<tasks>

<task type="tracer">
  <name>Task 1: Scroll progress bar + motion tokens — one end-to-end scroll-driven animation, wired site-wide</name>
  <files>src/components/ui/ScrollProgress.astro, src/layouts/BaseLayout.astro, src/styles/tokens.css</files>
  <action>
Create `src/components/ui/ScrollProgress.astro` — a thin fixed progress indicator at the very top of the viewport that fills left→right tracking document scroll. This is the tracer: it proves the scroll-driven-motion path end-to-end (token → element → vanilla island → reduced-motion guard) that Tasks 2–3 build on.

Markup: a single fixed bar element (e.g. `<div class="scroll-progress" role="presentation" aria-hidden="true"><span class="scroll-progress__fill"></span></div>`). It is decorative only — keep it out of the accessibility tree (aria-hidden, not a `<progressbar>` role; it duplicates native scroll and would be noise for AT).

Styling (scoped `<style>`): fixed, top:0, left:0, right:0, height 3px (add a token, see below), `z-index` ABOVE the Nav header. Nav header is z-index 100 and the mobile nav panel is 200 (see StickyCTA docblock) — set the progress bar to `z-index: 300` so it stays visible over both. The `.scroll-progress__fill` is `height:100%; width:100%; background: var(--color-accent); transform: scaleX(0); transform-origin: 0 50%; will-change: transform;`. Drive fill via `transform: scaleX(p)` (0→1), NOT width — transform-only so there is zero layout cost and zero CLS. The track itself is transparent (no yellow band across the page — accent stays on the moving fill only, honouring the sparing-accent rule).

Motion island (vanilla IIFE `<script>`, match the StickyCTA / BaseLayout IIFE style — no framework, no define:vars needed): read `prefers-reduced-motion`; if reduced, leave `scaleX(0)` and return early (bar reads as an empty hairline, effectively invisible — acceptable static state). Otherwise, on `scroll` and `resize`, compute `p = scrollTop / (scrollHeight - clientHeight)` clamped 0..1 and write `fill.style.transform = 'scaleX(' + p + ')'`. Throttle writes with `requestAnimationFrame` (a `ticking` boolean flag — do the read+write inside the rAF callback, reset the flag there) so scroll never triggers more than one style write per frame. Use passive scroll listener. Guard against divide-by-zero when the page is not scrollable (denominator <= 0 → set scaleX(0) and skip).

Mount it once in `BaseLayout.astro`: import ScrollProgress and render it as the FIRST child inside `<body>`, before the skip-link/Nav, so it layers over everything and appears on every page (homepage included). Do not add it per-page.

Add motion tokens to `tokens.css` under the `── Motion ──` block (reuse existing eases; add only what is new): `--progress-height: 3px;` and a longer draw/parallax easing seam for later tasks, e.g. `--ease-draw: cubic-bezier(0.33, 1, 0.68, 1);` and `--duration-draw: 1100ms;`. Reference these tokens from the component rather than hardcoding.

Do NOT duplicate or touch the existing `[data-anim]` / `[data-countup]` script — this task only ADDS the progress-bar island and tokens.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npx astro build 2>&1 | grep -iE "error|Complete!|built in" | tail -5</automated>
  </verify>
  <done>Build succeeds with 0 errors. ScrollProgress.astro exists and is rendered once in BaseLayout as the first body child. Grep confirms the fill uses `transform: scaleX` (not `width`) and references `var(--color-accent)`; the island early-returns under prefers-reduced-motion and uses requestAnimationFrame throttling. New tokens `--progress-height`, `--ease-draw`, `--duration-draw` exist in tokens.css.</done>
</task>

<task type="auto">
  <name>Task 2: Richer reveal variants (direction + scale + parallax) on the existing [data-anim] observer</name>
  <files>src/styles/global.css, src/layouts/BaseLayout.astro, src/pages/index.astro, src/pages/en/index.astro</files>
  <action>
Extend the EXISTING `[data-anim]` reveal system — do not add a second IntersectionObserver and do not touch RevealOnScroll.astro.

In `global.css`, under the existing `[data-anim]` block, add opt-in variant modifiers selected by a `data-anim-variant` attribute so authors write `<div data-anim data-anim-variant="left">`. The base `[data-anim]` (no variant) keeps its current fade+rise (translateY 22px) unchanged. Add:
  - `[data-anim-variant="left"]` initial `transform: translateX(-28px)` → `.is-in { transform: none; }`
  - `[data-anim-variant="right"]` initial `transform: translateX(28px)` → none
  - `[data-anim-variant="scale"]` initial `transform: scale(0.94)` + `opacity:0` → `.is-in { transform: none; opacity:1; }`
Keep the SAME transition properties/durations already defined on `[data-anim]` (opacity + transform, `--duration-slow`, `--ease-out`) so timing stays consistent and editorial. All variants must also respect the existing stagger seam (`[data-anim][style*='--i']`) — verify the `transition-delay` rule still applies to variant elements (it selects on `[data-anim]`, so it does; confirm by keeping variants as additional attributes on the same element, never a separate wrapper).

Add a subtle PARALLAX for opt-in elements via `data-parallax` (a separate, cheaper effect from reveals). In the BaseLayout IIFE script, after the existing reveal/count-up observers, add a parallax pass:
  - Select `document.querySelectorAll('[data-parallax]')`. If `reduce` is true OR the list is empty, do nothing (elements stay at their natural position — parallax is purely decorative offset, so no CSS "final state" is needed; default position IS the final state).
  - On scroll (rAF-throttled, reuse the same ticking pattern as Task 1 — a single shared rAF flag for the parallax loop), for each element compute a small translateY based on its distance from viewport centre: `offset = (rect.top + rect.height/2 - viewportH/2) * strength`, where `strength` comes from `data-parallax` (default 0.06; clamp the resulting offset to about ±24px so it stays SUBTLE — never a big drifting move). Write `el.style.transform = 'translate3d(0,' + offset + 'px,0)'`. Only run when the element is near the viewport (skip if fully off-screen) to save work.
  - IMPORTANT: an element must not be BOTH `data-anim` (which writes transform via class) and `data-parallax` (which writes transform via inline style) — they would fight. In the plan's usage, put parallax on a decorative inner wrapper or a distinct element (e.g. the Story portrait figure or a Timeline photo), never on the same node as a reveal.

Update the reduced-motion block in `global.css`: ensure the new `[data-anim-variant=...]` initial transforms are also neutralised (the existing `@media (prefers-reduced-motion: reduce) [data-anim] { transform: none !important; ... }` selector already covers any element carrying `data-anim`, since variants are additional attributes on the same `[data-anim]` node — confirm this and add an explicit `[data-parallax] { transform: none !important; }` rule inside the same reduced-motion media block so parallax elements are pinned to their natural spot).

Apply the new variants tastefully in `index.astro` and `en/index.astro` to build ONE coherent top-to-bottom narrative (motion attributes only — do NOT edit any copy). Suggested, restrained mapping (executor may fine-tune but keep it subtle and alternating, not every section moving the same way):
  - Testimonials cards: alternate `data-anim-variant="left"` / `"right"` across the mosaic for a gentle converging feel.
  - Samenwerken cards: `data-anim-variant="scale"` with the existing `--i` stagger.
  - Story portrait figure (`.story__portrait`, which is currently always-visible and NOT a reveal): add `data-parallax="0.05"` for a slow lift as it scrolls — this keeps the portrait always visible (no reveal clip) while adding life.
Because Story/Samenwerken/Testimonials render the same components in both locales, editing the section components OR the page files is acceptable — prefer editing the section components so NL+EN both get it from one change, but if a section already hardcodes `data-anim` you only ADD the `data-anim-variant`/`data-parallax` attribute. Do not duplicate reveal wrappers.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npx astro build 2>&1 | grep -iE "error|built in" | tail -5 && grep -c "data-anim-variant" src/styles/global.css</automated>
  </verify>
  <done>Build succeeds with 0 errors. global.css defines left/right/scale variants and the reduced-motion block neutralises both variants and [data-parallax]. BaseLayout script has a single rAF-throttled parallax pass gated behind the reduced-motion check. Homepage sections carry a restrained, alternating mix of variants; no element carries both data-anim and data-parallax.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mount the stats/timeline band on the homepage with a ~1M counter and a draw-on-scroll growth-curve SVG</name>
  <files>src/components/sections/Timeline.astro, src/components/ui/GrowthCurve.astro, src/layouts/BaseLayout.astro, src/pages/index.astro, src/pages/en/index.astro, src/i18n/nl.ts, src/i18n/en.ts</files>
  <action>
Mount the existing (currently unmounted) `Timeline.astro` on the homepage and enrich it with the third real number and a self-drawing growth-curve figure. Do NOT rewrite Timeline's existing copy — it is source-locked verbatim.

(a) MOUNT: In `index.astro`, import `Timeline` and render it between `<Story />` and `<Newsletter />` (the brief asks for the stats strip near the Story/verhaal section; Story is the dark band, Timeline is a navy documentary band — placing Timeline right after Story reads as one continuous "verhaal → verhaal in cijfers" beat). Do the identical mount in `src/pages/en/index.astro`. Add a matching HTML comment in the section-order docblock at the top of each page file.

(b) THIRD STAT (~1M zonnepanelen) + count-up formatting seam: The brief requires all three numbers to build — 9 markten, ~180 medewerkers, ~1M zonnepanelen — but `timeline.items` only encodes 9 and 180. Add a compact editorial stats row (or a third timeline milestone) surfacing "~1M zonnepanelen". Represent it WITHOUT ticking through a million integers:
  - Extend the count-up island in `BaseLayout.astro`: support an optional `data-countup-display` seam. When present, the counter animates a normalized 0→1 progress and interpolates toward a DISPLAY string. Concretely: keep `data-countup` as the numeric magnitude used for easing (e.g. `data-countup="1"`), add `data-countup-suffix="M"` and a `data-countup-prefix="~"` (add prefix support — currently only suffix exists). For "~1M": render `~` + counted value (0→1) + `M`. For "~180": `data-countup="180"` `data-countup-prefix="~"` counts 0→180. For "9": unchanged (no prefix).
  - Implementation detail for the island: read `prefix = el.getAttribute('data-countup-prefix') || ''`; in the `step()` function set `el.textContent = prefix + Math.round(eased*target) + suffix;` and the final write `el.textContent = prefix + target + suffix;`. This is purely additive — existing `data-countup` (no prefix) is unchanged, so Hero stats keep working. Reduced-motion / no-JS path already shows the authored final value in the markup, so author the visible text as the final formatted string (`~1M`, `~180`, `9`).
  - Add the ~1M zonnepanelen label to i18n: add a `zonnepanelenLabel` (NL: `zonnepanelen`) / `zonnepanelenValue` (`~1M`) pair and an `employeesValue`/`marketsValue` if needed to `nl.timeline` and `en.timeline` (EN: `solar panels`). Keep the same-shape contract (both locales get the same keys). Do NOT invent new facts — 9 / ~180 / ~1M come straight from CLAUDE.md and the existing story copy ("bijna één miljoen geplaatste zonnepanelen").

(c) DRAW-ON-SCROLL SVG (`GrowthCurve.astro`): Create `src/components/ui/GrowthCurve.astro` — a self-made, editorial line-art growth curve that draws itself via `stroke-dashoffset` when scrolled into view. This is the "figures that build up as you scroll" element.
  - SVG: thin single-stroke path (an upward growth curve, e.g. a gently accelerating line from bottom-left to top-right), `viewBox` ~ `0 0 320 180`, `fill="none"`, `role="img"` with an `aria-label` (localized, e.g. "Groeicurve van Soly" / "Soly's growth curve"). Stroke uses `currentColor` for the main line (so it inherits the navy band's light text) with ONE small yellow accent — e.g. a single `var(--color-accent)` end-dot/marker at the curve's top, NOT the whole line yellow (sparing-accent rule). Thin stroke (`stroke-width` ~2, `stroke-linecap="round"`).
  - Draw mechanic: give the path a class, set `stroke-dasharray` and `stroke-dashoffset` equal to the path length in CSS so it starts invisible, and transition `stroke-dashoffset` to 0 when a `.is-drawn` class is added. Trigger `.is-drawn` by reusing the EXISTING `[data-anim]` observer: give the SVG wrapper `data-anim` and, in `global.css`, add a rule so `[data-anim].is-in .growth-curve__path { stroke-dashoffset: 0; }` with `transition: stroke-dashoffset var(--duration-draw) var(--ease-draw);`. This means NO new observer — the draw is powered by the same `.is-in` toggle. (If measuring exact path length is impractical, hardcode a `stroke-dasharray`/`dashoffset` large enough to cover the path, e.g. 600, and reset to 0 on draw; slight over/undershoot on the dash is invisible for a decorative line.)
  - Reduced-motion fallback (MANDATORY): in a `@media (prefers-reduced-motion: reduce)` block set `.growth-curve__path { stroke-dashoffset: 0 !important; transition: none !important; }` so the curve is shown fully drawn with no motion. Because the draw is gated on `.is-in`, also confirm the existing reduced-motion path adds `.is-in` (the BaseLayout script's else-branch adds `is-in` to all `[data-anim]` when reduced OR no IntersectionObserver) — so the curve ends drawn even with JS. Belt-and-suspenders: the `!important` reduced-motion rule guarantees the final state regardless.
  - Place `<GrowthCurve />` inside `Timeline.astro` header area (near the existing dot-map figure) so the two custom figures read as a set. Keep it small/restrained.

Every new animated element in this task (counter, growth curve) MUST show its final state under prefers-reduced-motion and without JS. Transform/opacity/stroke-dashoffset only — no layout-shifting properties; reserve SVG box size via viewBox + CSS width so there is no CLS.
  </action>
  <verify>
    <automated>cd "C:/Users/PC/Desktop/claude/andere milan website" && npx astro build 2>&1 | grep -iE "error|built in" | tail -5 && grep -c "Timeline" src/pages/index.astro && grep -c "Timeline" src/pages/en/index.astro && grep -c "data-countup-prefix" src/layouts/BaseLayout.astro</automated>
  </verify>
  <done>Build succeeds with 0 errors. Timeline is rendered on both index.astro and en/index.astro between Story and Newsletter. The band shows three building numbers — 9, ~180, ~1M (zonnepanelen). GrowthCurve.astro exists, draws via stroke-dashoffset gated on the existing [data-anim].is-in toggle (no new observer), uses currentColor with a single yellow accent, and shows fully drawn under prefers-reduced-motion. The count-up prefix seam is additive (Hero stats still render 12+/180/9 unchanged). No CLS.</done>
</task>

</tasks>

<verification>
Overall phase checks (run after all three tasks):

1. `npx astro build` completes with 0 errors and both `/` and `/en/` build.
2. Manual/visual (checkpoint): on the homepage, scrolling fills the top yellow progress bar; the "verhaal in cijfers" band appears after Story and its numbers count up (9, ~180, ~1M) on entry; the growth-curve SVG draws itself; Testimonials/Samenwerken reveal with directional/scale variants; the Story portrait lifts subtly (parallax).
3. Reduced-motion: with OS "reduce motion" enabled (or emulated in DevTools), NO motion runs — progress bar static, counters show final `9 / ~180 / ~1M`, growth curve shown fully drawn, all sections shown in final position/opacity, portrait un-shifted. Nothing is ever hidden.
4. No new npm dependency was added (grep package.json diff is empty). No second IntersectionObserver was introduced; RevealOnScroll.astro is untouched.
5. Hero LCP not regressed: Hero markup/animation unchanged; progress bar is transform-only and mounted without blocking. No layout shift from any new element.
</verification>

<success_criteria>
- Thin accent (#FFDD11) scroll progress bar fixed at top, fills on scroll, transform-only, above nav (z-index 300).
- Homepage shows the stats/timeline band (mounted from the previously-unmounted Timeline.astro) between Story and Newsletter with three building counters: 9 markten, ~180 medewerkers, ~1M zonnepanelen.
- At least one self-made editorial SVG (growth curve) draws itself on scroll via stroke-dashoffset, thin stroke, single yellow accent.
- Reveal system extended IN PLACE with left/right/scale variants + subtle parallax, applied tastefully to form one coherent top-to-bottom narrative — no duplicate observers, RevealOnScroll.astro untouched.
- Every new animation has a prefers-reduced-motion fallback that shows final state and runs no motion; no content ever hidden for reduced-motion/no-JS users.
- Vanilla JS islands only (no new dependency); no CLS; Hero LCP not regressed; no homepage copy changed.
</success_criteria>

<output>
Create `.planning/quick/260821-m2d-maak-de-homepage-levendig-scroll-progres/260821-m2d-SUMMARY.md` when done.
</output>